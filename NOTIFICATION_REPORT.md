# گزارش — زنگوله‌ی اعلان‌ها

نسخه `26.0804` · کامیت `b3bbc8f` · ۱۳ مرداد ۱۴۰۵

---

## مسئله

زنگوله‌ی بالای صفحه **کاملاً تزئینی بود** — یک دکمه با آیکون و **بدون هیچ
`onClick`**. کاربر رویش می‌زد و هیچ اتفاقی نمی‌افتاد.

شما خواستید: «آپدیت‌های جدید که میدی بیاد در قالب یه پیام بگه فلان امکان اضافه
شده یا بگه تو اپدیت فلان باگ امنیتی برطرف شد».

---

## چه ساختم

زنگوله حالا پنلی باز می‌کند با **دو منبع کنار هم**:

### ۱. یادداشت انتشار — همراه خودِ نسخه

هر نسخه فهرست تغییراتش را دارد، با برچسب رنگی:

| برچسب | معنی |
|---|---|
| **قابلیت جدید** | چیزی که قبلاً نبود |
| **رفع اشکال** | باگی که درست شد |
| **امنیت** | مسئله‌ی امنیتی که برطرف شد |
| **بهبود** | چیزی که بهتر شد |

کنار هر نسخه شماره‌اش (`۲۶.۰۸۰۴`) و تاریخ شمسی نوشته می‌شود.

### ۲. اعلان سراسری — از دیتابیس

همان چیزی که از «مدیریت پلتفرم ← اعلان‌ها» منتشر می‌کنید. حالا علاوه بر نوار
بالای صفحه، در زنگوله هم دیده می‌شود.

---

## چرا یادداشت انتشار در کد است نه دیتابیس

این متن توضیح می‌دهد **یک دیپلوی مشخص چه چیزی را عوض کرده**. اگر در دیتابیس
بود، دو منبع حقیقت داشتیم: کدی که واقعاً دیپلوی شده، و رکوردی که ممکن است کسی
یادش برود اضافه کند یا اشتباه بنویسد.

حالا در همان کامیتی که قابلیت را اضافه می‌کند، توضیحش هم نوشته می‌شود و در گیت
می‌ماند.

**اعلان سراسری برعکس** — «فردا سرویس قطع است» نباید منتظر بیلد بماند، پس در
دیتابیس ماند.

---

## نکات ریز که تصمیم گرفته شد

**علامت‌گذاری هنگام باز شدن، نه بستن.** اگر موقع بستن بود و شما پنل را باز
می‌گذاشتید و تب را می‌بستید، همان اعلان‌ها دفعه‌ی بعد دوباره خوانده‌نشده
می‌ماندند.

**کاربر تازه فقط جدیدترین را می‌بیند.** نمایش کل تاریخچه به کسی که همین الان
ثبت‌نام کرده، بیشتر شبیه خرابی است تا خوش‌آمدگویی.

**شمارنده در برچسب صفحه‌خوان هم هست** — «اعلان‌ها، ۲ مورد خوانده‌نشده».

---

## 🔴 چهار ایراد که با اندازه‌گیری پیدا شدند

**۱. در موبایل اصلاً دیده نمی‌شد.** دکمه‌ی تزئینی قبلی `hidden … sm:flex` بود و
من همان کلاس را به ارث بردم. یعنی **کاربران موبایل هرگز هیچ اعلانی نمی‌دیدند** —
و بخش بزرگی از کاربران یک نرم‌افزار فروشگاهی روی موبایل کار می‌کنند.

**۲. پنل ۲۰ پیکسل از صفحه‌ی موبایل بیرون می‌زد.** اندازه‌گیری در ۳۹۰px:
`right=410`. علتش این بود که پنل به دکمه‌ای می‌چسبید که خودش نزدیک لبه است.
حالا زیر `sm` ثابت و تمام‌عرض با حاشیه می‌شود.

**۳. ناحیه‌ی اسکرول با صفحه‌کلید در دسترس نبود** — کاربر بدون ماوس نمی‌توانست
به اعلان‌های پایین برسد. (axe `serious`)

**۴. کنتراست تاریخ‌ها کافی نبود** — `text-muted-foreground/80`. (axe `serious`)

---

## تست واقعی

```
npx tsc --noEmit  → تمیز
npx vitest run    → ۳۸۰/۳۸۰ (۲۲ فایل)
npm run build     → Compiled successfully
```

| بررسی | نتیجه |
|---|---|
| نشان خوانده‌نشده ظاهر می‌شود | ✅ |
| پنل باز می‌شود و یادداشت‌ها را با نسخه و برچسب نشان می‌دهد | ✅ |
| نشان بعد از خواندن پاک می‌شود | ✅ |
| وضعیت خوانده‌شده بعد از رفرش می‌ماند | ✅ |
| کلیک بیرون و Escape می‌بندند | ✅ |
| **اعلان واقعی درج‌شده در دیتابیس ظاهر شد و نشان را برگرداند** | ✅ |
| زنگوله در موبایل دیده می‌شود | ✅ |
| پنل موبایل داخل صفحه جا می‌گیرد (۱۲ تا ۳۷۸ از ۳۹۰) | ✅ |
| axe در ۳۹۰ و ۱۴۴۰ | صفر تخلف |

همین مجموعه بعد از دیپلوی **روی سایت زنده هم اجرا شد**. اعلان تستی بلافاصله
حذف و جدول خالی تأیید شد.

---

## نگهداری

برای هر نسخه‌ی بعدی، یک بلوک به بالای `RELEASES` در `lib/changelog.ts` اضافه
می‌شود. یک تست خودکار بررسی می‌کند که ترتیب درست باشد، شماره‌ها یکتا باشند و
هر نسخه حداقل یک تغییر داشته باشد.

---

## مرحله‌ی بعد

طبق فهرست تحقیق، دو مورد بعدی:

1. **قفل خودکار حساب** پس از تلاش‌های ناموفق ورود
2. **مدیریت نشست‌ها** — دیدن دستگاه‌های واردشده و «خروج از همه»
   (الان بعد از تغییر رمز، نشست‌های قدیمی همچنان معتبر می‌مانند)

بعد از آن **نقش سفارشی ادمین** — همان «شخصی‌سازی دسترسی‌ها» که خواستید.
