# گزارش: قیمت درصدی، تخفیف در خرید و پیشنهاد نصب کم‌مزاحمت

نسخه‌ی `26.0811` · مهاجرت `0042_purchase_line_discount.sql`

---

## ۱. پیغام نصب — «هربار که صفحه رفرش میشه نیازه پیغام بیاد؟»

**نه.** و باگ واقعی بود، نه فقط سلیقه.

### چه چیزی خراب بود

نسخه‌ی قبلی بنر نصب را در **هر صفحه**، روی **هر اندازه‌ی صفحه** و با
**هر بار بارگذاری** نشان می‌داد. تنها ترمز، کلیک روی «بعداً» بود؛ کسی
که بنر را نادیده می‌گرفت و صفحه را رفرش می‌کرد، دوباره همان را می‌دید.
یعنی مزاحم‌ترین حالت ممکن برای کاربری که هنوز تصمیم نگرفته.

### حالا چه شد

بنر خودکار فقط وقتی می‌آید که **هر چهار شرط** برقرار باشد:

| شرط | چرا |
|---|---|
| مسیر `/dashboard` | خواسته‌ی صریح شما — نه وسط ثبت فاکتور |
| عرض صفحه ≤ ۱۰۲۳px | روی دسکتاپ اپ نصب‌شده ارزش کمی دارد |
| در این نشست نشان داده نشده | **پاسخ مستقیم به سؤال شما** |
| در ۳۰ روز اخیر «بعداً» نزده | احترام به تصمیم قبلی |

پرچم نشست در `sessionStorage` است نه `localStorage` — تا بسته‌شدن تب
باقی می‌ماند. یعنی رفرش و جابه‌جایی بین صفحه‌ها بنر را برنمی‌گرداند،
ولی فردا که دوباره برنامه را باز کنید یک‌بار (و فقط یک‌بار) می‌بینید.

نکته‌ی ریز ولی مهم: پرچم **پیش از نمایش** گذاشته می‌شود نه هنگام بستن.
اگر منتظر بستن می‌ماندیم، کاربری که بنر را نادیده می‌گیرد و رفرش می‌کند
دوباره همان را می‌دید — یعنی دقیقاً همان باگ.

### دکمه‌ی همیشگی کنار زنگوله

پیشنهاد خودتان بود و درست بود: بدون یک راه دائمی، کسی که یک‌بار «بعداً»
زده تا ۳۰ روز هیچ راهی برای نصب نداشت جز اینکه منوی مرورگر را بلد باشد.

دکمه‌ی «نصب برنامه» حالا در هدر کنار زنگوله است و قوانین بنر را ارث
نمی‌برد — همه‌جا، همیشه. اگر نصب ممکن نباشد (اپ از قبل نصب است یا
مرورگر اینستاگرام) **اصلاً رندر نمی‌شود**، نه اینکه خاکستری شود.

### 🔴 باگی که موقع همین کار پیدا شد

رویداد `beforeinstallprompt` در کل عمر صفحه **یک‌بار** شلیک می‌شود. حالا
دو مصرف‌کننده داریم (بنر و دکمه). اگر هرکدام جدا گوش می‌داد، آنکه دیرتر
mount می‌شد رویداد را از دست می‌داد و دکمه‌اش کلیک می‌خورد ولی هیچ
اتفاقی نمی‌افتاد — همان «دکمه‌ای که وعده می‌دهد و عمل نمی‌کند».

راه‌حل: `components/shared/install-store.ts` — یک شنونده در سطح ماژول
که رویداد را نگه می‌دارد و به هر تعداد مصرف‌کننده می‌دهد.

### اندازه‌گیری واقعی

| سناریو | بنر | دکمه |
|---|---|---|
| آیفون، `/dashboard`، بار اول | ✅ می‌آید | ✅ |
| همان تب، **رفرش** | ❌ نمی‌آید | ✅ |
| تب جدید، `/sales` | ❌ نمی‌آید | ✅ |
| تب جدید، `/dashboard` | ✅ می‌آید | ✅ |
| دسکتاپ ۱۴۴۰px | ❌ نمی‌آید | ✅ |
| اندروید (کروم) موبایل | ✅ می‌آید | ✅ |

---

## ۲. تغییر قیمت با درصد — در فروش و خرید

کنار کادر قیمت یک دکمه‌ی **تومان ⇄ درصد** اضافه شد، دقیقاً همان الگوی
آشنای کادر تخفیف.

### مبنای درصد: قیمت اصلی کالا، نه قیمت فعلی

این تصمیم مهم‌ترین بخش کار است. اگر مبنا قیمت فعلی بود:

```
۱۰٪  →  ۱٬۷۴۹٬۰۰۰
۱۰٪ دوباره  →  ۱٬۹۲۳٬۹۰۰   ← ۲۱٪ شد!
```

کاربری که فقط می‌خواست عدد را اصلاح کند، با هر تصحیح یک پله دورتر
می‌شد و راهی برای بازگشت نداشت. با مبنای ثابت:

```
۱۰٪  →  ۱٬۷۴۹٬۰۰۰
۱۰٪ دوباره  →  ۱٬۷۴۹٬۰۰۰   ← همان
۰٪   →  ۱٬۵۹۰٬۰۰۰          ← بازگشت به قیمت اصلی
```

در فروش، مبنا قیمتی است که کالا با آن وارد سبد شد — یعنی اگر مشتری روی
«لیست قیمت عمده» باشد، «۱۰٪ بیشتر» نسبت به قیمت عمده حساب می‌شود نه
قیمت خرده‌فروشی.

### عدد منفی مجاز است

«۱۰-» یعنی ده درصد ارزان‌تر. سقف پایین صفر است تا قیمت منفی ساخته نشود.

### در حالت درصد با دکمه اعمال می‌شود

نه با هر ضربه‌ی کیبورد. همان درسی که در «درصد سود» گرفتیم: اعمال
لحظه‌ای باعث می‌شد برای رسیدن به «۳۰» اول روی «۳» بپرد و قیمت خراب شود.
حالا Enter یا ترک فیلد یا دکمه.

### آزمون واقعی روی «شومیز کتیبه»

| گام | نتیجه |
|---|---|
| قیمت اولیه | ۱٬۵۹۰٬۰۰۰ |
| تعویض به درصد | ۰ نشان داد ✅ |
| ورود ۱۰ و Enter | ۱۰ ✅ |
| بازگشت به تومان | **۱٬۷۴۹٬۰۰۰** ✅ |
| ورود ۰ | **۱٬۵۹۰٬۰۰۰** ✅ (بازگشت کامل) |
| تخفیف ۹۰٬۰۰۰ | مجموع **۱٬۵۰۰٬۰۰۰** ✅ |

---

## ۳. 🔴 در موبایلِ فروش، قیمت اصلاً قابل ویرایش نبود

باگ واقعی که خودتان گزارش کردید. در نسخه‌ی موبایل پنجره‌ی فروش، قیمت
واحد فقط یک **متن ثابت** بود:

```tsx
<div className="text-xs font-bold text-primary">
  {formatToman(c.unit_price, false)} تومان     ← فقط نمایش
</div>
```

هیچ `<input>`ی وجود نداشت. کاربر موبایل فقط می‌توانست تعداد و تخفیف را
عوض کند. حالا کادر واقعی دارد (با همان دکمه‌ی تومان⇄درصد) و اگر قیمت را
تغییر داده باشید، قیمت اصلی با خط‌خوردگی زیرش می‌آید تا بدانید چقدر
جابه‌جا کرده‌اید.

---

## ۴. تخفیف هر قلم در فاکتور خرید

فروش از ابتدا `sale_items.discount` داشت؛ خرید نداشت. تنها راه این بود
که خودتان قیمت خرید را دستی کم کنید — یعنی قیمت واقعیِ توافق‌شده با
تأمین‌کننده در سند گم می‌شد و گزارش «چقدر تخفیف گرفتیم» غیرممکن بود.

مهاجرت `0042` ستون `purchase_items.discount` را اضافه کرد و هر دو تابع
`create_purchase` و `update_purchase_invoice` را به‌روز کرد.

### چرا سازگاری قدیم نشکست

- ستون `default 0 not null` → همه‌ی ردیف‌های موجود صفر گرفتند
- **امضای هیچ تابعی عوض نشد** — تخفیف داخل همان `p_items` jsonb می‌آید

دومی عمدی است: درس مهاجرت ۰۰۳۰. `create or replace` روی امضای متفاوت
جایگزین نمی‌کند بلکه یک تابع دوم می‌سازد و PostgREST نمی‌تواند انتخاب
کند (`PGRST203`) — یعنی ثبت خرید در کل برنامه از کار می‌افتد. تأیید شد:

```
select count(*) from pg_proc where proname='create_purchase'  →  1
```

### دیتابیس به ورودی کلاینت اعتماد نمی‌کند

```sql
least(greatest(coalesce((it->>'discount')::bigint, 0), 0), v_line)
```

آزمون روی سرور واقعی (فقط SELECT، هیچ نوشتنی):

| قیمت × تعداد | تخفیف ورودی | تخفیف اعمال‌شده | خالص |
|---|---|---|---|
| ۹۷۰٬۰۰۰ | ۹۰۰٬۰۰۰ | ۹۰۰٬۰۰۰ | ۷۰٬۰۰۰ |
| ۲٬۶۷۰٬۰۰۰ | ۰ | ۰ | ۲٬۶۷۰٬۰۰۰ |
| ۲۰۰٬۰۰۰ | ۹۹۹٬۹۹۹٬۹۹۹ | **۲۰۰٬۰۰۰** (سقف) | ۰ |
| ۲۰۰٬۰۰۰ | **۵۰۰۰-** | **۰** | ۲۰۰٬۰۰۰ |
| ۵۰٬۰۰۰ | (کلید نبود) | ۰ | ۵۰٬۰۰۰ |

بدون این سقف، جمع فاکتور منفی می‌شد.

---

## ۵. 🔴 باگ چیدمانی که موقع اندازه‌گیری پیدا شد

این را کسی گزارش نکرده بود و **از قبل هم وجود داشت**.

جدول اقلام با `container query` تصمیم می‌گرفت جدول باشد یا کارت، ولی
مبنایش عرض **کل فرم** بود. مشکل: فرم از ۸۶۰px به بعد دوستونی می‌شود و
۳۴۰px از عرضش را به ستون کناری می‌دهد. یعنی درست در لحظه‌ای که فرم
پهن‌تر می‌شد، فهرست **باریک‌تر** می‌شد:

```
عرض فرم ۸۰۲px  →  عرض فهرست ۸۰۲px  →  جدول ۷۶۸px  →  ✅
عرض فرم ۸۶۲px  →  عرض فهرست ۴۷۲px  →  جدول ۷۱۴px  →  🔴 سرریز ۲۴۲px
عرض فرم ۹۲۲px  →  عرض فهرست ۵۳۲px  →  جدول ۸۵۴px  →  🔴 سرریز ۳۲۲px (خرید)
```

راه‌حل: یک container دوم (`pos-list-scope`) مستقیماً روی خودِ فهرست.
حالا تصمیم به عرض واقعی همان عنصر بستگی دارد، پس هر تغییری در چیدمان
فرم خودبه‌خود درست کار می‌کند.

اندازه‌گیری پس از اصلاح — **صفر سرریز در همه‌ی عرض‌ها**:

```
SALE      ۷۰۰ کارت · ۷۸۰ جدول(۷۴۸/۷۴۸) · ۸۶۲ کارت · ۱۴۰۲ جدول(۱۰۱۲/۱۰۱۲)
PURCHASE  ۹۰۰ کارت · ۹۲۲ کارت · ۱۴۰۲ جدول(۱۰۱۲/۱۰۱۲)
```

---

## ۶. 🔴 پنل کشویی برای صفحه‌خوان بی‌نام بود

ایراد `aria-dialog-name` (شدت serious) در **هر شش ترکیب** موبایل/تبلت/
دسکتاپ × فروش/خرید. کاربر صفحه‌خوان فقط می‌شنید «dialog» و نمی‌دانست چه
چیزی باز شده. مال کد این نوبت نبود ولی درست شد.

```
پیش از اصلاح:  6 صفحه × 1 ایراد serious
پس از اصلاح:   0
```

---

## سنجش‌ها

```
./node_modules/.bin/tsc --noEmit   → پاک
npx vitest run                      → ۱۰۹۰/۱۰۹۰ (۴۱ فایل)
npm run build                       → Compiled successfully
axe-core (۶ ترکیب)                  → صفر ایراد
سرریز چیدمان (۸ عرض × ۲ سند)        → صفر
```

تست‌ها راستی‌آزمایی شدند — باگ عمداً برگردانده شد و تست شکست:

| باگ برگردانده‌شده | تستی که شکست |
|---|---|
| حذف `shownThisSession` | «با رفرش دوباره نمی‌آید» |
| حذف `Math.max(0, ...)` از قیمت | «قیمت هرگز منفی نمی‌شود» |
| بازگرداندن `!isPurchase &&` روی ستون تخفیف | «ستون تخفیف دیگر مخصوص فروش نیست» |
| `poslist` → `invoiceform` | «مبنا عرض خودِ فهرست است» |

سه تست موجود شکستند و در هر سه مورد بررسی نشان داد **تست عقب مانده بود
نه کد** — اصلاح شدند، نه کد:

- `purchase-panel`: به رشته‌ی `aria-label` چسبیده بود که به کامپوننت
  مشترک منتقل شد
- `purchase-panel`: به عددهای ثابت ۶۸۰/۸۲۰ چسبیده بود که خودشان باگ بودند
- `pwa`: به شرطی چسبیده بود که به تابع خالص منتقل شد

---

## صادقانه: چه چیزی نیست

- **تخفیف سطری خرید در گزارش‌ها تفکیک نشده.** ستون در دیتابیس هست و
  `line_total` درست است، ولی گزارشی که بگوید «امسال چقدر از
  تأمین‌کننده‌ها تخفیف گرفتیم» ساخته نشده.
- **فاکتورهای خرید موجود صفر تخفیف دارند** — چون جدول `purchases` اصلاً
  خالی است (۰ ردیف). یعنی این قابلیت هنوز روی داده‌ی واقعی امتحان نشده،
  فقط روی سبد و روی محاسبات سرور.
- **درصد قیمت در فرم ویرایش فاکتور خرید موجود نیست.** آنجا فقط کادر
  ساده‌ی تخفیف اضافه شد (بدون دکمه‌ی تومان⇄درصد) — فرم ویرایش یک مودال
  جدا و قدیمی است که از `PosCartList` استفاده نمی‌کند.
- **`base_price` فقط در حافظه است.** اگر پنجره را ببندید و دوباره باز
  کنید، قیمت اصلی از کارت کالا خوانده می‌شود. برای سبد نیمه‌کاره‌ای که
  ذخیره نمی‌شود اشکالی ندارد.
- **آیکون maskable هنوز درست نشده** — روی بعضی اندرویدها داخل دایره‌ی
  سفید می‌نشیند.
