# سیستم ورود — ممیزی، معماری و گام اول

نسخه‌ی زنده: `26.0819` · `b5dc7b2` · ۲۸ مرداد ۱۴۰۵

---

## اول ممیزی، بعد ساخت

پیش از نوشتن هر خط کد، وضعیت موجود اندازه‌گیری شد. نتیجه مهم بود:
**پایه‌ی احراز هویت از آنچه انتظار می‌رفت پخته‌تر بود** و بازنویسی‌اش
اشتباه می‌شد.

### آنچه از قبل درست کار می‌کرد — دست نخورد

| لایه | جزئیات |
|---|---|
| ورود از سمت سرور | نه مستقیم از مرورگر، پس نقطه‌ی کنترل وجود دارد |
| کندسازی نمایی | ۵ تلاش آزاد، بعد ۲→۴→۸… سقف ۱۵ دقیقه |
| محدودیت per-IP | دو لایه: Edge middleware + خود روت |
| ضد user-enumeration | پیام و کد وضعیت یکسان برای هر حالت |
| محافظت مسیرها | allowlist صریح، نه blocklist |
| مدیریت نشست | فهرست دستگاه‌ها + بستن از راه دور |
| سیاست رمز | حداقل ۸ نویسه، فهرست رمزهای رایج، رد رمز فقط-عددی |

🔴 **قفل حساب عمداً وجود ندارد** و این درست است. OWASP توصیه‌اش
نمی‌کند چون خودش ابزار حمله می‌شود: مهاجم با ۵ رمز غلط حساب رقیب را
قفل می‌کند.

---

## شکاف واقعی: بازیابی رمز اصلاً وجود نداشت

- FAQ می‌گفت: «با پشتیبانی تماس بگیرید»
- صفحه‌ی ورود **حتی لینک فراموشی نداشت**

یعنی کاربری که رمزش را گم می‌کرد، از داده‌ی مالی خودش بیرون می‌ماند
تا وقتی کسی جواب تلفن بدهد.

### دو اندازه‌گیری که معماری را تعیین کرد

```
rate_limit_email_sent = 2      ← دو ایمیل در ساعت، برای کل پروژه
smtp_host             = None   ← SMTP اختصاصی تنظیم نشده
```

```sql
select count(*) filter (where email like '%@hesabyar.app') from auth.users;
-- ۴ از ۶
```

🔴 **۴ از ۶ کاربر ایمیل ساختگی دارند.** با شماره‌ی موبایل ثبت‌نام
کرده‌اند و `09121234567@hesabyar.app` صندوق واقعی نیست.

**نتیجه: بازیابی فقط با ایمیل کار نمی‌کند.** مسیر دوم یک ضرورت است،
نه تجمل. سومین کاربری که در یک ساعت رمزش را فراموش کند، هیچ ایمیلی
نمی‌گیرد و دلیلش را هم نمی‌فهمد.

---

## آنچه ساخته شد

### دو مسیر بازیابی

```
«رمز عبور را فراموش کرده‌ام»
│
├─ ایمیل واقعی؟  → لینک بازیابی Supabase
│                  + هشدار صادقانه درباره‌ی تأخیر احتمالی
│
└─ ایمیل ساختگی؟ → «از مدیر مجموعه کد بگیرید»
                    مدیر از «کاربران و دسترسی‌ها» کد می‌سازد
                    ۸ رقم · ۳۰ دقیقه · یک‌بار مصرف · سقف ۵ تلاش
```

### 🔴 چرا مدیر مستقیم رمز را عوض نکند؟

می‌تواند — آن روت از قبل هست. ولی آن‌وقت **مدیر رمز کاربر را
می‌داند** و می‌تواند بعداً به‌جای او سند مالی ثبت کند. انکارناپذیری
از بین می‌رود. با کد یک‌بارمصرف، رمز نهایی را فقط خود کاربر
می‌داند.

### تصمیم‌های امنیتی

| تصمیم | چرا |
|---|---|
| کد **۸** رقمی نه ۶ | ۱۰⁶ حالت با اعتبار ۳۰ دقیقه قابل حدس است |
| ذخیره‌ی **هش** نه کد خام | نشت پشتیبان = تصاحب فوری همه‌ی حساب‌ها |
| `randomInt` نه `Math.random` | دومی از نظر رمزنگاری قابل پیش‌بینی است |
| کد تازه، کد قبلی را باطل می‌کند | وگرنه کدی که در پیام‌رسان دیده شده هنوز کار می‌کند |
| سقف ۵ تلاش | با ۸ رقم عملاً غیرقابل حدس می‌شود |
| پاسخ یکسان برای «کد غلط» و «کاربر ناموجود» | وگرنه فهرست حساب‌ها استخراج می‌شود |
| ارقام فارسی نرمال‌سازی می‌شوند | کاربر که «۱۲۳۴۵۶۷۸» می‌زند کد **درست** را زده |

### سابقه‌ی ورود

جدول `login_events`، **جدا از** `login_attempts`.

آن یکی یک شمارنده‌ی حالت است که با ورود موفق **پاک می‌شود** — پس
هرگز نمی‌تواند بگوید چه کسی کِی از کجا وارد شد. دو مسئولیت متفاوت،
دو جدول. نگهداری ۹۰ روز.

---

## سه باگ که حین کار پیدا شد

### ۱) 🔴 ریدایرکت که بازیابی را ناممکن می‌کرد

اگر `/reset-password` را جزو `isAuthPage` می‌گذاشتم، کاربری که از
**لینک ایمیل** می‌آید — و Supabase برایش نشست ساخته — مستقیم به
داشبورد پرت می‌شد و **هرگز** نمی‌توانست رمزش را عوض کند. با اینکه
دقیقاً برای همین آمده.

`isRecovery` از `isAuthPage` جدا نگه داشته شد.

### ۲) 🔴 باگی که فقط `next build` گرفت

```
UnhandledSchemeError: Reading from "node:crypto" is not handled
```

صفحه‌ی کلاینت `RESET_CODE_LENGTH` را از فایلی می‌خواند که
`node:crypto` داشت، و وب‌پک کل ماژول را داخل باندل مرورگر می‌کشید.

⚠️ **انتقال `require` به داخل بدنه‌ی تابع کافی نبود** — وب‌پک آن را
هم ایستا تحلیل می‌کند. تنها راه: جداکردن فایل بر اساس مرز
سرور/کلاینت.

**نه `tsc` گرفتش، نه ۱۴۳۸ تست.** حالا یک تست این مرز را قفل می‌کند.

### ۳) 🔴 باگی که فقط اسکرین‌شات گرفت

نوشتم «تنظیمات ← کاربران» ولی روی صفحه **«تنظیمات → کاربران»** دیده
می‌شد — فلش در متن راست‌به‌چپ بازچینش می‌شود و ترتیب مسیر برعکس
به‌نظر می‌رسد.

در DOM متن درست بود. با واژه‌ی «سپس» که جهت ندارد جایگزین شد.

---

## اندازه‌گیری‌ها

```
تست‌ها          ۱۴۳۸ / ۱۴۳۸ (۵۲ فایل) — ۵۵ تای آن auth
tsc --noEmit    پاک
build           موفق (محلی + Vercel)
axe-core        صفر ایراد — دسکتاپ، تبلت، موبایل — محلی و زنده
خطای JS         صفر
راستی‌آزمایی    ۹ باگ عمدی برگردانده شد، هر ۹ تا توسط تست گرفته شد
```

**آزمون رفتار امنیتی روی دیتابیس واقعی** (در تراکنشی که rollback شد):

| آزمون | نتیجه |
|---|---|
| کد غلط | `invalid` + شمارنده ۱ ✅ |
| **کاربر ناموجود** | `invalid` — **همان پاسخ**، بدون نشت ✅ |
| کد درست | `true` ✅ |
| استفاده‌ی دوباره | `invalid` ✅ |
| منقضی | `expired` ✅ |
| سقف تلاش | `too_many` ✅ |

**آزمون انتها به انتها روی مرورگر:** مدیر وارد شد ← کد `50074325`
ساخته و نمایش داده شد ← جستجوی همان کد در دیتابیس: **۰ مورد**، فقط
هش ۶۴ نویسه‌ای.

**محدودیت نرخ واقعاً کار می‌کند:** تست موبایل `429` گرفت چون ۶ بار
درخواست زده بود. با IP متفاوت بلافاصله کار کرد.

**داده‌ی زنده دست‌نخورده:** `users 6 · sales 30 · products 383`

---

## صادقانه: چه چیزی هنوز نیست

| مورد | وضعیت |
|---|---|
| **ورود دومرحله‌ای (TOTP)** | ساخته **نشد**. سنجیدم که در پلن رایگان **در دسترس است** (`mfa_totp_enroll_enabled = True`) — گام بعدی است. |
| **بازیابی با پیامک** | سرویس پیامکی وصل نیست. جای اتصالش در معماری باز گذاشته شده. |
| **خروج خودکار پس از بی‌کاری** | طراحی شده، ساخته نشده. |
| **صفحه‌ی «امنیت حساب»** | سابقه‌ی ورود در دیتابیس ثبت می‌شود و تابع `my_login_history` آماده است، ولی **هنوز صفحه‌ای برای دیدنش نیست**. |
| **SMTP اختصاصی** | سقف ۲ ایمیل در ساعت باقی است. تا وصل نشود، مسیر ایمیل شکننده می‌ماند. |
| **کدهای پشتیبان 2FA** | همراه TOTP خواهد آمد. |

⚠️ **مهم‌ترین شکاف باقی‌مانده:** سابقه‌ی ورود ثبت می‌شود ولی کاربر
جایی برای دیدنش ندارد. داده هست، رابط نیست.

---

## گام بعد

۱. صفحه‌ی «امنیت حساب» — سابقه‌ی ورود + نشست‌های فعال یکجا
۲. ورود دومرحله‌ای TOTP + کدهای پشتیبان
۳. خروج خودکار پس از بی‌کاری (پیش‌فرض خاموش)
