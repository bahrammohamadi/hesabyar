# گزارش: قیمت تمام‌شده با هزینه‌ی حمل

مهاجرت `0046_landed_cost.sql` · مستقر روی `05770c1`

---

## 🔴 باگی که رفع شد: گزارش سود غلط بود

`create_purchase` پارامتر `p_extra_total` می‌گرفت و آن را به **جمع
فاکتور** اضافه می‌کرد:

```sql
v_total := v_subtotal + p_extra_total - v_discount + p_tax
```

ولی `purchase_price` هر کالا فقط `unit_price` می‌شد. یعنی:

> اگر ۵۰۰ هزار تومان کرایه‌ی حمل می‌دادید، از جیبتان رفته بود ولی
> قیمت تمام‌شده‌ی کالا همان قیمت خرید خام می‌ماند.

و چون `sale_items.cost_price` از همان `purchase_price` کپی می‌شود،
**سود هر فروش به‌اندازه‌ی سهم آن کالا از هزینه‌ی حمل بیشتر گزارش
می‌شد.**

جدول `purchase_extra_costs` با ستون `allocation` از **مهاجرت ۰۰۰۱**
وجود داشت و در جستجوی کد معلوم شد **هرگز در هیچ فایلی استفاده نشده** —
نه نوشته می‌شد نه خوانده. صفر ردیف.

بدتر: در پنجره‌ی خرید جدید `p_extra_total: 0` **ثابت** بود، یعنی اصلاً
نمی‌شد هزینه‌ی حمل وارد کرد.

---

## چه ساخته شد

### ستون `landed_cost` روی قلم خرید

`unit_price` دست‌نخورده می‌ماند چون مبنای تسویه با تأمین‌کننده است.
قیمت تمام‌شده کنارش می‌نشیند نه جایش.

### دو روش سرشکن

| روش | مناسب برای |
|---|---|
| **به نسبت ارزش** | بیمه، کارمزد بانکی |
| **به نسبت تعداد** | کرایه‌ی حمل (کامیون به تعداد کارتن کار دارد نه قیمتشان) |

آزمون روی سرور واقعی با سه قلم و هزینه‌ی ۵٬۰۰۰٬۰۰۰ ریال:

| قلم | خالص | تعداد | سهم (ارزش) | سهم (تعداد) |
|---|---|---|---|---|
| الف | ۶٬۰۰۰٬۰۰۰ | ۲ | ۳٬۰۰۰٬۰۰۰ | ۱٬۲۵۰٬۰۰۰ |
| ب | ۳٬۰۰۰٬۰۰۰ | ۱ | ۱٬۵۰۰٬۰۰۰ | ۶۲۵٬۰۰۰ |
| ج | ۱٬۰۰۰٬۰۰۰ | ۵ | ۵۰۰٬۰۰۰ | ۳٬۱۲۵٬۰۰۰ |
| **جمع** | | | **۵٬۰۰۰٬۰۰۰** ✅ | **۵٬۰۰۰٬۰۰۰** ✅ |

جمع سهم‌ها دقیقاً برابر هزینه‌ی کل است — اگر گرد کردن باعث اختلاف
می‌شد، قیمت تمام‌شده‌ی کل انبار به‌مرور از واقعیت فاصله می‌گرفت.

### قیمت تمام‌شده **واحدی** است نه کل سطر

`cost_price` در `sale_items` واحدی است؛ اگر کل سطر را می‌گذاشتیم، سود
کالاهای چندتایی چند برابر غلط می‌شد.

---

## 🔴 باگی که تست سختگیرانه‌تر در کد خودم پیدا کرد

نسخه‌ی اولم فقط `extraRial` را با `Math.max(0, ...)` کف می‌زد. تست
اولیه هم فقط همان را می‌سنجید و **سبز بود**.

وقتی تست را سختگیرانه‌تر کردم (همه‌ی ترکیب‌های ورودی منفی)، معلوم شد
`lineNetRial` منفی **سهم منفی** می‌دهد — یعنی قیمت تمام‌شده کمتر از
قیمت خرید می‌شود و سود دوباره غلط می‌شود.

همان باگ در SQL هم بود. هر دو رفع شدند و روی سرور واقعی تأیید شد:

```
neg_line=0 · neg_extra=0 · neg_qty=0
```

⚠️ درس: تست ضعیف بدتر از نبودن تست است، چون اطمینان کاذب می‌دهد.

---

## سنجش‌ها

```
tsc --noEmit   → پاک
vitest         → ۱۲۶۴/۱۲۶۴ (۴۷ فایل)
build          → Vercel موفق
axe-core       → صفر (دسکتاپ و موبایل)
سایت زنده      → ۹۷٬۰۰۰ + ۵۰٬۰۰۰ = ۱۴۷٬۰۰۰ ✅
```

**راستی‌آزمایی:** شش باگ عمداً برگردانده شد؛ پنج مورد بلافاصله گرفته
شد و ششمی (حذف گارد منفی) نگرفت — که خودش باعث شد تست را تقویت کنم و
باگ واقعی بالا را پیدا کنم.

⚠️ **build محلی در این نوبت کار نکرد** — محیط sandbox تحت فشار بود و
`next build` دو بار تایم‌اوت شد. اعتبارسنجی نهایی با build خود Vercel
انجام شد که موفق بود، به‌علاوه‌ی `tsc` تمیز و ۱۲۶۴ تست.

---

## صادقانه: چه چیزی نیست

- **جدول `purchases` صفر ردیف دارد**، پس این قابلیت هرگز روی یک خرید
  واقعی اجرا نشده. منطقش روی سرور و در تست سنجیده شد، ولی اولین خرید
  واقعی را حتماً بررسی کنید.
- **فاکتورهای گذشته بازمحاسبه نمی‌شوند** — چون خریدی وجود ندارد،
  مسئله‌ای هم نیست.
- **`purchase_extra_costs` هنوز استفاده نمی‌شود.** هزینه یک عدد کلی
  است، نه چند ردیف با عنوان («کرایه»، «باربری»). برای تفکیک، آن جدول
  باید وصل شود.
- **روش سرشکن برای همه‌ی اقلام یکی است.** در واقعیت ممکن است کرایه به
  تعداد و بیمه به ارزش باشد؛ فعلاً یک روش برای کل فاکتور.
- **گزارش «چقدر هزینه‌ی جانبی دادیم» ساخته نشده.**

---

## قدم بعدی

طبق نقشه‌ی راه به‌روزشده:

1. **سطح قیمت پیش‌فرض هر مشتری** — یک ستون روی `contacts`
2. **پیش‌فاکتور با تبدیل یک‌کلیکی** — `sales_orders` آماده است
3. **چک: هشدار سررسید و خرج کردن**
