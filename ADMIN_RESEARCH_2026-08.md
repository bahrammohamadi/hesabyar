# تحقیق پنل سوپرادمین — چه چیزی کم داریم

۱۳ مرداد ۱۴۰۵ · بر اساس بررسی الگوهای رایج SaaS و رقبای ایرانی

---

## روش

دو دسته منبع بررسی شد:

**الگوهای جهانی SaaS** — راهنماهای ساخت پنل ادمین، چک‌لیست‌های امنیتی SaaS،
و مستندات WorkOS (که همین را به‌عنوان محصول می‌فروشد).

**رقبای ایرانی** — حسابفا، سپیدار آنلاین، فرداد، هلو، فینتو. الگوی مشترکشان:
اشتراک‌محور، چند سطح دسترسی، تریال ۱۵ روزه، و پنل مدیریت داخلی برای پشتیبانی.

---

## آنچه الان داریم

| قابلیت | وضعیت |
|---|---|
| ۴ نقش ادمین (`super_admin`/`support`/`finance`/`readonly`) | ✅ |
| ماتریس مجوز در دیتابیس (تک‌منبع حقیقت) | ✅ |
| جعل هویت با سقف ۳۰ دقیقه و دلیل اجباری | ✅ |
| گزارش ممیزی تغییرناپذیر | ✅ |
| تأیید/رد/تعلیق کسب‌وکار | ✅ |
| تمدید تریال | ✅ |
| جستجوی کاربران در کل پلتفرم | ✅ *(امروز از ۴۰۳ نجات یافت)* |
| اعلان سراسری | ✅ *(همان)* |
| **بازنشانی رمز کاربر** | ✅ **امروز اضافه شد** |
| **تغییر رمز توسط خود کاربر** | ✅ **امروز اضافه شد** |

---

## ۱۲ چیزی که کم داریم — به ترتیب اولویت

### 🔴 اولویت بالا

**۱. مدیریت اشتراک و پلن**
جدول‌های `plans` و `subscriptions` وجود دارند ولی **خالی‌اند** و هیچ رابطی
ندارند. الان فقط تریال ۱۴ روزه داریم؛ بعد از انقضا هیچ اتفاقی نمی‌افتد.
لازم: تعریف پلن، فعال‌سازی دستی، تمدید، ارتقا/تنزل، و تاریخ انقضا.

> این را گفتید بعداً تصمیم می‌گیرید. تا آن موقع کسب‌وکارها بعد از ۱۴ روز
> همچنان دسترسی کامل دارند.

**۲. تایمر باقی‌مانده‌ی اشتراک برای همه**
الان فقط تریال شمارنده دارد (`TrialCountdown`). اشتراک پولی شمارنده ندارد
چون اصلاً اشتراکی وجود ندارد. بعد از مورد ۱ معنا پیدا می‌کند.

**۳. اعلان درون‌برنامه‌ای با زنگوله**
زنگوله‌ی بالای صفحه **کاملاً تزئینی است** — هیچ `onClick` ندارد. شما خواستید
تغییرات هر نسخه به‌صورت پیام نمایش داده شود. زیرساخت `announcements` هست ولی
به زنگوله وصل نیست و «یادداشت انتشار» ندارد.

**۴. قفل خودکار حساب پس از تلاش‌های ناموفق**
الان محدودیت نرخ داریم ولی قفل حساب نداریم. اگر کسی ۵۰ بار رمز اشتباه بزند،
حساب قفل نمی‌شود — فقط کند می‌شود.

### 🟠 اولویت متوسط

**۵. مدیریت نشست‌ها**
دیدن اینکه یک کاربر از چه دستگاه‌هایی وارد شده و امکان «خروج از همه‌ی
دستگاه‌ها». الان بعد از بازنشانی رمز، نشست‌های قدیمی همچنان معتبرند.

**۶. صادرات داده‌ی یک کسب‌وکار**
برای پشتیبانی («فاکتورهایم را می‌خواهم») و انطباق قانونی. الان راهی نیست جز
کوئری دستی.

**۷. حذف نرم و بازیابی سازمان**
تعلیق داریم ولی حذف نداریم. اگر کاربری بخواهد حسابش پاک شود، فقط دستی
ممکن است — و برگشت‌پذیر نیست.

**۸. آمار مصرف به‌ازای کسب‌وکار**
تعداد فاکتور، کالا، کاربر و حجم داده. هم برای سقف پلن لازم است هم برای
تشخیص کسب‌وکارهای فعال از رهاشده.

**۹. رابط تیکت پشتیبانی**
جدول‌ها و RLS از قبل ساخته شده‌اند ولی هیچ صفحه‌ای ندارند.

### 🟡 اولویت پایین

**۱۰. نقش سفارشی برای ادمین‌ها**
الان ۴ نقش ثابت است. شما گفتید «خیلی امکانات شخصی‌سازی دسترسی‌ها» می‌خواهید —
یعنی ساخت نقش دلخواه با انتخاب تک‌تک مجوزها. ماتریس فعلی در SQL هاردکد است.

**۱۱. پرچم قابلیت (feature flag)**
روشن/خاموش کردن یک قابلیت برای یک کسب‌وکار خاص — مثلاً دادن دسترسی زودهنگام
به امکان جدید، یا خاموش کردن چیزی که برایش مشکل‌ساز شده.

**۱۲. داشبورد درآمد**
MRR، نرخ ریزش، تعداد اشتراک فعال. بعد از مورد ۱ معنا پیدا می‌کند.

---

## 🔴 یک باگ جدی که حین همین تحقیق کشف شد

هنگام بررسی ماتریس مجوز متوجه شدم **سه مجوز که روت‌ها استفاده می‌کنند، اصلاً
تعریف نشده بودند**:

```
users.view            → /api/admin/users/search
impersonate           → /api/admin/impersonate
announcements.manage  → /api/admin/announcements
```

چون تعریف نشده بودند، به `else false` می‌افتادند. یعنی **روی سایت زنده حتی
خود شما هم HTTP 403 می‌گرفتید** و این سه بخش کاملاً از کار افتاده بودند:

- جستجوی کاربران پلتفرم
- «ورود به حساب کاربر» (جعل هویت)
- مدیریت اعلان‌ها

صفحه بدون خطا رندر می‌شد و فقط همیشه خالی می‌ماند — به همین دلیل تا حالا دیده
نشده بود.

**با فراخوانی واقعی endpoint پیدا شد، نه با خواندن کد.** الان درست شده و یک
تست خودکار نوشتم که همه‌ی روت‌های ادمین را می‌گردد و مطمئن می‌شود هر مجوزی که
می‌خواهند واقعاً در ماتریس وجود دارد — این دسته باگ دیگر نمی‌تواند بی‌صدا
برگردد.

---

## پیشنهاد ترتیب کار

با توجه به اینکه اشتراک را فعلاً کنار گذاشتید:

1. **اعلان زنگوله** (مورد ۳) — خودتان خواستید، مستقل از اشتراک است
2. **قفل حساب + مدیریت نشست** (۴ و ۵) — تکمیل کار امنیتی امروز
3. **نقش سفارشی ادمین** (۱۰) — همان «شخصی‌سازی دسترسی‌ها» که خواستید
4. **آمار مصرف + صادرات داده** (۸ و ۶)
5. **اشتراک** (۱، ۲، ۱۲) — هر وقت پلن‌ها را تعیین کردید

مرحله‌ی بعد سراغ **اعلان زنگوله** می‌روم مگر اینکه ترتیب دیگری بخواهید.
