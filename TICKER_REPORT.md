# گزارش — نوار قیمت و آب‌وهوا

نسخه `26.0804` · کامیت `11986f6` · ۱۳ مرداد ۱۴۰۵

---

## منبع را با آزمایش انتخاب کردم، نه با خواندن تبلیغ سایت‌ها

پنج سرویس ایرانی را واقعاً صدا زدم:

| سرویس | نتیجه |
|---|---|
| `nerkh.io` | ❌ تایم‌اوت کامل، ۱۲ ثانیه بدون پاسخ |
| `brsapi.ir` | ❌ تایم‌اوت کامل |
| `alanchand.com` | ❌ HTTP 401 — توکن می‌خواهد |
| `nerkh-api.ir` | ❌ «Invalid API» — توکن می‌خواهد |
| **`tgju.org`** | ✅ **۶/۶ موفق، میانگین ۶۵ms، ۷۵۶ نماد، بدون کلید** |

هر دو سرویسی که «رایگان و بدون ثبت‌نام» تبلیغ می‌کردند، اصلاً بالا نبودند.

برای آب‌وهوا: **`open-meteo`** — بدون کلید، با دما، رطوبت، باد و بیشینه/کمینه روز.

tgju چهار دامنه‌ی هم‌ارز دارد (`call`, `call1`, `call2`, `call3`) که همگی جواب
دادند؛ اگر یکی بیفتد بعدی امتحان می‌شود.

**قابل تعویض ساخته شد** همان‌طور که خواستید: هر منبع یک `MarketProvider` است.
برای جایگزینی با سرویس پولی در آینده، فقط یک فایل اضافه می‌شود و رابط کاربری
اصلاً خبردار نمی‌شود.

---

## چرا از سرور می‌خوانم نه مستقیم از مرورگر

سه دلیل، به ترتیب اهمیت:

1. **کش مشترک.** اگر ۵۰ کاربر آنلاین باشند و هر مرورگر خودش هر دقیقه بپرسد،
   ۵۰ درخواست در دقیقه به منبع رایگان می‌رود و به‌سرعت مسدود می‌شویم. با کش
   ۹۰ ثانیه‌ای سروری، ۵۰ کاربر = یک درخواست.
2. tgju هدر CORS نمی‌دهد، پس فراخوانی مستقیم از مرورگر مسدود است.
3. **CSP دست‌نخورده ماند** — چون از دامنه‌ی خودمان می‌خوانیم، لازم نشد
   `connect-src` را برای دامنه‌های خارجی باز کنم.

### یک تصمیم امنیتی

روت‌های `/api/market` و `/api/weather` را **عمداً عمومی نکردم**. نوار فقط داخل
پنل (بعد از ورود) دیده می‌شود؛ اگر عمومی بودند، سایت شما به یک پراکسی رایگان
برای tgju و open-meteo تبدیل می‌شد که هر کسی می‌توانست سهمیه‌مان را بسوزاند.

به همین دلیل روت آب‌وهوا هم **شناسه‌ی شهر** از فهرست داخلی می‌گیرد، نه
مختصات آزاد — وگرنه یک پراکسی باز می‌شد.

---

## 🔴 سه تله که پیش از انتشار گرفته شدند

**۱. واحد ارز دیجیتال فرق دارد.** tgju قیمت ارز و طلا را به **ریال** می‌دهد
ولی ارز دیجیتال را به **دلار**. اگر تقسیم بر ۱۰ را یکسان اعمال می‌کردم،
بیت‌کوین ۶٬۳۶۸ تومان نشان داده می‌شد به‌جای ۶۳٬۶۸۵ دلار.

**۲. جهت تغییر در فیلد دیگری است.** مقدار `dp` همیشه بدون علامت است و جهت در
`dt` می‌آید. بدون خواندن آن، **افت قیمت سبز نشان داده می‌شد** — دقیقاً برعکس
واقعیت.

**۳. بازار ایران شب‌ها بسته است.** این را با اندازه‌گیری داده‌ی زنده فهمیدم:

```
ساعت ۱۰ صبح ۱۳ مرداد:
  دلار، سکه، طلا  → ts دیروز، dp=0     (بازار بسته)
  بیت‌کوین، انس    → ts همان دقیقه       (زنده)
```

کسی که می‌خواهد بر مبنای این عدد قیمت‌گذاری کند و فکر می‌کند «لحظه‌ای» است،
در واقع عدد ۱۸ ساعت پیش را می‌بیند. حالا هر قیمت پرچم «امروز/دیروز» دارد و
قیمت‌های بسته یک **نقطه‌ی خاکستری** و توضیح در tooltip می‌گیرند.

---

## شخصی‌سازی

از دکمه‌ی چرخ‌دنده کنار نوار:

- **نمایش/عدم نمایش کل نوار** (با راه بازگشت، تا گیر نکنید)
- **آب‌وهوا** روشن/خاموش + **انتخاب شهر از ۳۶ شهر ایران**
- **افزودن و حذف قیمت‌ها** — ۱۶ گزینه: دلار، یورو، پوند، درهم، لیر، سکه امامی،
  سکه بهار، نیم، ربع، طلای ۱۸ و ۲۴، مثقال، انس، بیت‌کوین، اتریوم، تتر
- **تغییر ترتیب** با دکمه‌های راست/چپ

ترتیب و انتخاب در همان مرورگر ذخیره می‌شود و بعد از رفرش می‌ماند.

> جابه‌جایی با **دکمه** پیاده شد نه drag-and-drop: کشیدن با ماوس روی موبایل و
> با صفحه‌کلید عملاً کار نمی‌کند.

مقادیر ذخیره‌شده هنگام خواندن پاک‌سازی می‌شوند — اگر نسخه‌ی بعدی نمادی را حذف
کند، شناسه‌ی قدیمی باعث نمی‌شود نوار خالی به نظر برسد.

---

## دو باگ که خودِ تست‌ها پیدا کردند

**axe در موبایل:** نوار اسکرول می‌شود (۶۵۹px محتوا در ظرف ۳۳۰px) ولی
فوکوس‌پذیر نبود — کاربری که ماوس ندارد هیچ راهی برای دیدن قیمت‌های پنهان
نداشت. (`serious / scrollable-region-focusable`)

**تست واحد:** دمای منفی از خط تیره‌ی ASCII استفاده می‌کرد ولی درصد تغییر از
منهای یونیکد — دو علامت متفاوت کنار هم در یک نوار.

---

## تست واقعی

```
npx tsc --noEmit  → تمیز
npx vitest run    → ۳۲۳/۳۲۳ (۲۰ فایل)
npm run build     → Compiled successfully
```

| بررسی | نتیجه |
|---|---|
| API قیمت: ۱۶ نماد، منبع tgju | ✅ |
| دلار ۱۹۲٬۸۸۰ تومان — با نرخ منتشرشده‌ی امروز می‌خواند | ✅ |
| بیت‌کوین ۶۳٬۶۸۵ **دلار** (نه تومان) | ✅ |
| آب‌وهوا: تهران ۳۲° کمی ابری، بیشینه/کمینه ۳۶/۲۳ | ✅ |
| کش سروری: فراخوانی دوم سریع‌تر | ✅ |
| نوار در `/sales`، `/products`، `/reports/sales` | ✅ |
| افزودن ویجت → بعد از رفرش می‌ماند | ✅ |
| حذف ویجت / تغییر ترتیب / تغییر شهر | ✅ |
| خاموش کردن نوار + راه بازگردانی | ✅ |
| ارتفاع نوار ۴۱px، بدون اسکرول افقی صفحه | ✅ |
| axe در ۳۹۰/۸۳۴/۱۴۴۰ + پنجره‌ی تنظیمات باز | صفر تخلف |

همین مجموعه بعد از دیپلوی **روی `tarazooapp.vercel.app` هم اجرا شد** و پاس شد.

---

## از درخواست شما چه ماند

این‌ها را بررسی کردم ولی طبق توافق در پیام‌های بعدی انجام می‌شوند:

| مورد | وضعیت فعلی |
|---|---|
| تعلیق حساب کاربر | ✅ **از قبل هست** (روی سازمان) |
| تمدید تریال | ✅ **از قبل هست** |
| اشتراک ماهانه/سالانه | ❌ جدول‌ها هست ولی خالی، UI ندارد — **منتظر تصمیم شما درباره‌ی پلن‌ها** |
| تایمر باقی‌مانده‌ی اشتراک | 🟡 فقط برای تریال هست، برای اشتراک نه |
| تغییر رمز توسط خود کاربر | ❌ **اصلاً وجود ندارد** — نقص جدی |
| تغییر رمز توسط سوپرادمین | ❌ فقط موقع *ساخت* کاربر ممکن است |
| اعلان آپدیت با زنگوله | ❌ زنگوله‌ی بالای صفحه فعلاً تزئینی است (بدون عملکرد) |

پیشنهاد من برای مرحله‌ی بعد: **تغییر رمز عبور** (هم برای کاربر، هم سوپرادمین)،
چون نبودنش یعنی اگر کسی رمزش را فراموش کند هیچ راهی ندارد.
