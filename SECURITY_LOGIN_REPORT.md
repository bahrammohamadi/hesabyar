# گزارش — محافظت از ورود و مدیریت دستگاه‌ها

نسخه `26.0804` · کامیت `3d4a32e` · ۱۳ مرداد ۱۴۰۵

---

## پرسیدید «هرچی استاندارد است» — این پاسخ با سند

### نشست‌ها: چرا و چقدر

**استاندارد NIST SP 800-63B** سه سطح تعریف می‌کند. ما در **AAL1** هستیم
(ورود تک‌عاملی با رمز):

| سطح | انقضای مطلق | انقضای بی‌فعالیتی |
|---|---|---|
| **AAL1** — رمز تنها (ما) | **۳۰ روز** | اختیاری |
| AAL2 — دوعاملی | ۲۴ ساعت | ۱ ساعت |
| AAL3 — کلید سخت‌افزاری | ۱۲ ساعت | ۱۵ دقیقه |

پس استاندارد برای وضعیت فعلی ما ۳۰ روز است — نه سخت‌گیرانه‌تر. اگر روزی
ورود دومرحله‌ای اضافه کنیم، به AAL2 می‌رویم و آن‌وقت ۲۴ ساعت لازم می‌شود.

### 🔴 ولی یک مانع واقعی هست

```
PATCH config/auth { sessions_timebox: 2592000 }
  → "User sessions can only be configured on Pro Plans and up."
```

**تنظیم انقضای نشست فقط در پلن Pro موجود است.** پس فعلاً نمی‌توانم آن را
اعمال کنم. به‌جایش کاری کردم که در دسترس بود و در عمل مهم‌تر است: **فهرست
دستگاه‌ها با امکان بستن دستی**.

وضعیت واقعی که پیدا کردم:

```
۹۴ نشست فعال برای فقط ۴ کاربر
هیچ‌کدام تاریخ انقضا نداشتند
```

بدتر: **تغییر رمز عبور این نشست‌ها را نمی‌بست.** یعنی اگر کسی رمز شما را
می‌دانست و شما عوضش می‌کردید، او همچنان وارد بود و هیچ راهی برای
بیرون‌انداختنش نبود.

---

## قفل حساب: چرا **قفل** نکردم

**توصیه‌ی OWASP**: قفل ثابت (مثلاً «۵ خطا = ۳۰ دقیقه قفل») خودش به ابزار
حمله تبدیل می‌شود. مهاجم با پنج رمز غلط می‌تواند **عمداً حساب رقیبتان را
قفل کند** — یک انکار سرویس رایگان.

الگوی توصیه‌شده **تأخیر نمایی سقف‌دار** است. اندازه‌گیری واقعی روی دیتابیس:

```
تلاش ۱ تا ۵  →   ۰ ثانیه     (اشتباه تایپی رایگان است)
تلاش ۶       →   ۲ ثانیه
تلاش ۷       →   ۴ ثانیه
تلاش ۸       →   ۸ ثانیه
تلاش ۹       →  ۱۶ ثانیه
…               سقف ۱۵ دقیقه
```

کاربری که رمزش را اشتباه تایپ می‌کند تقریباً چیزی حس نمی‌کند. مهاجمی که
می‌خواهد هزار رمز امتحان کند، عملاً متوقف می‌شود.

**دو لایه دارد:**
- **per-account** — تأخیر نمایی بالا
- **per-IP** — ۳۰ تلاش در ۵ دقیقه، برای مهاجمی که هر بار حساب *متفاوتی*
  را امتحان می‌کند و هرگز به آستانه‌ی حساب نمی‌رسد

### افشا نکردن وجود حساب

اگر پاسخ «این حساب قفل است» بدهیم، به مهاجم گفته‌ایم این ایمیل واقعی است.
پس پاسخ برای هر سه حالت **دقیقاً یکسان** است:

- حساب وجود ندارد
- رمز غلط است
- در حال کندسازی است

در تست تأیید شد: حساب موجود و ناموجود هر دو `401` با همان متن.

شمارنده هم بر اساس **شناسه‌ی ورود** است نه `user_id` — وگرنه فقط برای
حساب‌های موجود رکورد ساخته می‌شد و همان تفاوت زمان پاسخ، اطلاعات را لو
می‌داد.

---

## مدیریت دستگاه‌ها

در «تنظیمات ← حساب کاربری» حالا می‌بینید:

- چه دستگاهی، چه مرورگری، چه سیستم‌عاملی
- آخرین فعالیت و آدرس IP
- کدام یکی دستگاه فعلی شماست
- دکمه‌ی بستن برای هرکدام
- «خروج از سایر دستگاه‌ها»

نشست فعلی عمداً از «خروج از همه» مستثناست — وگرنه از همان صفحه‌ای که
ایستاده‌اید بیرون می‌افتید و فکر می‌کنید چیزی خراب شده.

**بررسی مالکیت در خود تابع SQL** است (`p_user_id` در شرط `where`). بدون آن،
حدس‌زدن یک UUID یعنی بستن نشست کاربر دیگر. در تست واقعی امتحان شد: `404`
گرفت و نشست دست‌نخورده ماند.

---

## 🔴 شش ایراد که حین کار پیدا شدند

**۱. مسیر ورود پشت middleware احراز هویت بود.** یعنی حتی رمز درست هم
`Unauthorized` می‌گرفت — تناقض منطقی: روت ورود نمی‌تواند نیاز به ورود داشته
باشد. با تست واقعی بازتولید شد.

**۲. پاسخ ۲۰۰ وقتی هیچ نشستی حذف نشده بود.** کاربر فکر می‌کرد نشست بسته شده
در حالی که نشده. حالا `404`.

**۳. `HeadlessChrome` شناسایی نمی‌شد** — این رشته `chrome/` ندارد. ۸۰ نشست
«مرورگر ناشناس» نمایش داده می‌شدند.

**۴. `curl` و نشست‌های سرور** هم «مرورگر ناشناس» بودند؛ حالا برچسب درست
می‌گیرند.

**۵. کنتراست دکمه‌ی «خروج از سایر دستگاه‌ها»** کافی نبود (axe `serious`).

**۶. تغییر رمز شمارنده‌ی خطا را پاک نمی‌کرد.** سناریوی واقعی: کاربر رمزش را
فراموش کرده، چند بار غلط زده، با پشتیبانی تماس گرفته و رمز جدید گرفته — ولی
هنوز در حال کندسازی بود. حالا هر دو مسیر تغییر رمز شمارنده را صفر می‌کنند.

---

## تست واقعی

```
npx tsc --noEmit  → تمیز
npx vitest run    → ۴۰۴/۴۰۴ (۲۳ فایل)
npm run build     → Compiled successfully
```

| بررسی | نتیجه |
|---|---|
| منحنی تأخیر ۰,۰,۰,۰,۰,۲,۴,۸ روی دیتابیس واقعی | ✅ |
| پیام حساب موجود و ناموجود کاملاً یکسان | ✅ |
| ورود موفق شمارنده را پاک می‌کند | ✅ |
| ورود از رابط کاربری با مسیر جدید | ✅ |
| شمارش معکوس و دکمه‌ی غیرفعال | ✅ |
| فهرست نشست‌ها با تشخیص دستگاه | ✅ |
| **تلاش برای بستن نشست کاربر دیگر → ۴۰۴** | ✅ |
| نشست فعلی از این مسیر بسته نمی‌شود | ✅ |
| axe در ۳۹۰ و ۱۴۴۰ + صفحه‌ی ورود در حالت انتظار | صفر تخلف |

همین مجموعه بعد از دیپلوی **روی سایت زنده هم اجرا شد** — روی یک شناسه‌ی
ساختگی، نه حساب شما.

**پاکسازی:** ۸۸ نشست خودکارِ تست‌های من حذف شد؛ ۱۶ نشست واقعی دست‌نخورده
ماند. جدول `login_attempts` خالی شد.

```
users 5 · orgs 3 · contacts 548 · products 382 · sales 21
```

---

## آنچه هنوز نمی‌توانیم

**انقضای خودکار نشست** نیاز به پلن Pro دارد. اگر روزی ارتقا دادید، یک خط
تنظیم است:

```
sessions_timebox = 2592000   # ۳۰ روز، مطابق NIST AAL1
```

تا آن موقع، فهرست دستگاه‌ها این خلأ را پر می‌کند — با این تفاوت که دستی است
نه خودکار.

---

## مرحله‌ی بعد

طبق فهرست تحقیق، مورد باقی‌مانده‌ی مهم:

**نقش سفارشی ادمین** — همان «شخصی‌سازی دسترسی‌ها» که خواستید. الان چهار نقش
ثابت در SQL هاردکد است و نمی‌شود نقش دلخواه با ترکیب دلخواهی از مجوزها ساخت.
