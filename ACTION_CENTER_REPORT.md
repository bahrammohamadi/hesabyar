# گزارش: «کارهای امروز»

مهاجرت `0043_action_center.sql` · مستقر روی `14b1b3b`

---

## چرا این را اول انتخاب کردم

از فهرست قابلیت‌های نداشته، این بیشترین نسبت «اثر به هزینه» را داشت:
همه‌ی داده‌اش از قبل در دیتابیس بود و روزانه دیده می‌شود.

رقبا (معین، چرتکه) مرکز هشدار دارند: سررسید چک، بدهی مشتری، کسری کالا،
اقساط. ما فقط یک ویجت «موجودی کم» داشتیم.

---

## 🔴 یافته‌ی اصلی: هشدار قبلی عملاً بی‌فایده بود

پیش از نوشتن هر کدی، داده‌ی زنده را شمردم:

```
تنوع فعال                        ۳۸۶
در فهرست «موجودی کم»            ۳۶۱   ← ۹۴٪ کاتالوگ
از این تعداد، موجودی صفر        ۳۴۴
آستانه‌ی پیش‌فرض low_stock        ۳
```

یعنی ویجت «هشدار موجودی» تقریباً **کل کاتالوگ** را قرمز می‌کرد. هشداری
که ۹۴٪ موارد را علامت بزند، هشدار نیست — نویز است، و کاربر ظرف یک هفته
یاد می‌گیرد نادیده‌اش بگیرد.

**راه‌حل:** کالای تمام‌شده فقط وقتی «کار امروز» است که **سابقه‌ی فروش
داشته باشد**. کالایی که هیچ‌وقت فروش نرفته و موجودی‌اش صفر است،
احتمالاً داده‌ی اولیه است نه کسری واقعی.

```sql
and exists (select 1 from public.sale_items si where si.variant_id = v.id)
```

نتیجه روی داده‌ی واقعی: **۳۶۱ → ۱۱**

---

## چه چیزی ساخته شد

یک RPC به‌نام `action_center(p_org, p_days)` که **پنج گروه را در یک
رفت‌وبرگشت** برمی‌گرداند:

| گروه | فوریت | منبع |
|---|---|---|
| چک سررسیدگذشته | 🔴 خطر | `checks` با `status in (pending, deposited)` |
| چک تا ۷ روز آینده | 🟡 هشدار | همان، بازه‌ی آینده |
| کالای تمام‌شده‌ی پرفروش | 🟡 هشدار | `product_variants` + شرط سابقه‌ی فروش |
| فاکتور نسیه‌ی تسویه‌نشده | 🔵 اطلاع | `sales.paid_credit > 0` |
| سفارش در انتظار | 🔵 اطلاع | `sales_orders.status = pending` |

**چرا یک تابع و نه پنج کوئری؟** داشبورد هر پنج عدد را هم‌زمان لازم دارد
و پنج رفت‌وبرگشت شبکه برای یک ویجت اسراف است، مخصوصاً روی موبایل.

**هر سطر لینک دارد.** هشداری که نشود رویش کاری کرد، فقط اضطراب می‌سازد.

---

## امنیت

الگوی `dashboard_summary` دنبال شد:

- `security definer` (چون روی چند جدول با RLS متفاوت کوئری می‌زند)
- **اولین دستور بدنه** چک عضویت سازمان است — بدون آن، هر کاربری داده‌ی
  هر سازمانی را می‌دید
- `revoke all ... from public, anon` + `grant execute to authenticated`
- هر گروه `limit 20` — بدون آن سازمانی با هزار فاکتور نسیه پاسخ
  چندمگابایتی می‌گرفت
- `p_days` بین ۱ و ۹۰ کلمپ می‌شود تا `p_days=100000` کل تاریخ را اسکن نکند

تأیید عملی: صدا زدن تابع از کنسول SQL (که کاربر لاگین‌شده نیست) خطای
`دسترسی غیرمجاز` داد. گارد کار می‌کند.

---

## 🔴 باگی که فقط از روی اسکرین‌شات پیدا شد

در نسخه‌ی موبایل، زیرنویس فاکتورها این‌طور دیده می‌شد:

```
۴۴۰۱۴۰۵/۰۴/۰۶ روز پیش
```

عدد روز به تاریخ چسبیده بود. علت: رشته‌ی
`` `${toJalali(s.date)} · ${toFaDigits(s.days_old)} روز پیش` `` را
الگوریتم bidi در متن راست‌به‌چپ بازچینش می‌کرد.

**در DOM متن کاملاً درست بود** (`'۱۴۰۵/۰۴/۰۶ · ۴۴ روز پیش'`) و فقط در
رندر خراب می‌شد — یعنی هیچ تست رشته‌ای روی محتوا نمی‌گرفتش.

راه‌حل: دو عنصر جدا در یک `flex` تا هر تکه ظرف bidi مستقل خودش را
داشته باشد. همان الگویی که قبلاً برای خلاصه‌ی عددی نمودار داشبورد
استفاده کردیم.

تستش هم به‌جای محتوا، **الگوی کد** را می‌سنجد:
```ts
expect(tsx).not.toMatch(/subtitle=\{`[^`]*·[^`]*`\}/);
```

---

## سنجش‌ها

```
tsc --noEmit          → پاک
npx vitest run        → ۱۱۲۷/۱۱۲۷ (۴۳ فایل)
npm run build         → موفق
axe-core              → صفر، در سه اندازه (موبایل/تبلت/دسکتاپ)
داده‌ی زنده            → ۱۷ مورد واقعی (۱۱ کالا + ۶ فاکتور)
شمارش دیتابیس         → بدون تغییر نسبت به خط پایه
```

**راستی‌آزمایی تست‌ها** — شش باگ عمداً برگردانده شد، هر شش مورد گرفته شد:

| باگ برگردانده‌شده | تستی که شکست |
|---|---|
| حذف گردکردن تاریخ به نیمه‌شب | «سررسید امروز صفر است» |
| حذف شرط سابقه‌ی فروش | «کالای تمام‌شده فقط با سابقه‌ی فروش» |
| حذف گارد عضویت سازمان | «عضویت سازمان چک می‌شود» |
| حذف `normalizeActionCenter` | «خروجی RPC نرمال‌سازی می‌شود» |
| حذف دو `limit 20` | «هر گروه سقف تعداد دارد» |
| جابه‌جایی ویجت به پایین | «بالاتر از هشدار موجودی» |
| برگرداندن رشته‌ی «·» | «تاریخ و عدد روز در دو عنصر جدا» |

⚠️ نکته‌ی روش‌شناسی: در اولین دور راستی‌آزمایی، تابع bash من معیوب بود
و همه‌چیز «سبز» نشان می‌داد. اگر به همان اکتفا می‌کردم، نتیجه‌ی غلط
گزارش می‌شد. با اجرای مستقیم دوباره سنجیدم.

---

## صادقانه: چه چیزی نیست

- **چک‌ها صفر ردیف دارند.** جدول `checks` خالی است، پس دو گروه اول روی
  داده‌ی واقعی آزمایش نشده‌اند — فقط منطقشان تست شده. تا وقتی چکی ثبت
  نکنید، آن بخش را نخواهید دید.
- **`sales_orders` هم خالی است** (صفر ردیف)، پس گروه پنجم هم همین وضع
  را دارد.
- **دو گروه واقعاً کار می‌کنند:** کالای تمام‌شده (۱۱) و فاکتور نسیه (۶).
- **ویجت قدیمی «هشدار موجودی» هنوز سر جایش است** و همچنان ۳۶۱ را نشان
  می‌دهد. عمداً دست نزدم تا این تغییر بزرگ‌تر از لازم نشود؛ ولی
  پیشنهاد می‌کنم در نوبت بعد یا حذف شود یا آستانه‌اش اصلاح شود.
- **«اقساط» ندارد** چون اصلاً قابلیت اقساط نداریم.
- عدد «۷ روز» برای چک‌های نزدیک ثابت است و از تنظیمات قابل تغییر نیست.

---

## قدم بعدی (پیشنهاد من)

۱. **سطح قیمت پیش‌فرض هر مشتری** — یک ستون روی `contacts`، خطای انسانی
   را حذف می‌کند
۲. **پیش‌فاکتور با تبدیل یک‌کلیکی** — جدولش هست، فرمش نیست
۳. **سرشکن هزینه‌ی حمل روی قیمت تمام‌شده** — الان گزارش سود را
   خوش‌بینانه‌تر از واقعیت نشان می‌دهد
