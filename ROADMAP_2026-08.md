# نقشه‌ی راه ترازو — مرداد ۱۴۰۵

این سند دو بخش دارد:
**الف)** یافته‌های واقعی (چه چیزی واقعاً خراب است، چه چیزی نیست)
**ب)** ده سوپر-پرامپت آماده‌ی اجرا

---

# الف) یافته‌های واقعی — قبل از هر کاری بخوانید

## ۱. سه موردی که «باگ» گزارش کردید، باگ نبودند

هر سه از قبل حل شده بودند ولی روی سایت زنده نرفته بودند. علت: پس از
`git filter-repo` تاریخچه‌ی محلی با remote جد مشترک نداشت و force-push
انجام نشده بود. آخرین دیپلوی مربوط به ۱ آگوست بود.

**در تاریخ ۲ آگوست پوش و دیپلوی شد.** تست روی `tarazooapp.vercel.app`:

| مورد | نتیجه‌ی تست زنده |
|---|---|
| F2 در داشبورد | ✅ پنل باز می‌شود (`?panels=invoice:sale:create:new`) |
| F2 در سایر صفحات | ✅ روی `/products` هم کار می‌کند |
| منوی سه‌نقطه | ✅ کاملاً داخل کادر (پایین منو ۶۱۱ در ارتفاع ۹۰۰) |
| دسترسی به آیتم‌های منو | ✅ **۱۰ از ۱۰** با `elementFromPoint` |
| فرم فروش جدید | ✅ هر دو مسیر یک کامپوننت مشترک |

> اگر باز هم این‌ها را خراب می‌بینید، **Ctrl+Shift+R** بزنید — نسخه‌ی
> کش‌شده‌ی مرورگر است.

## ۲. یک خطر امنیتی جدی که پیدا شد و رفع شد

هنگام آماده‌سازی پوش کشف شد که `filter-repo` قبلی **ناقص** عمل کرده بود:

```
کلید service_role کامل:   ۲ نسخه در تاریخچه  →  ۰
شماره موبایل مشتریان:    ۵۱۶ شماره        →  ۳ (هر سه بی‌خطر)
فایل‌های PII:            ۶ فایل           →  ۰
```

سه شماره‌ی باقی‌مانده بررسی شدند: یکی شماره‌ی خودتان به‌عنوان
placeholder فرم ورود، یکی داده‌ی تستی («نام تست»)، یکی اصلاً شماره نیست
(بخشی از هش کامیت). تاریخچه‌ی قبلی در شاخه‌ی
`backup/pre-filter-repo-2026-08-02` روی گیت‌هاب نگه داشته شد.

> ⚠️ **این یعنی کلید `service_role` تا امروز روی گیت‌هاب عمومی بوده.**
> آخرین بررسی هنوز HTTP 200 برمی‌گرداند. چرخاندن کلید فوری است.

## ۳. تقویم — اندازه‌گیری واقعی، نه حدس

۱۱ صفحه روی سایت زنده اسکن شد. برخلاف تصور، بیشتر صفحات درست‌اند:

```
✅ dashboard · sales · contacts · products · finance
✅ reports/sellers · reports/profitability · activity · checks
❌ reports/overview-v2   → ۸ تاریخ میلادی (2026-07-12 …)
❌ inventory/movements   → ۱ تاریخ میلادی (2026-07-05)
```

پس «همه‌چیز فارسی شود» در عمل یعنی **دو صفحه**، نه بازنویسی کل پروژه.
احتمالاً محور X نمودار و یک ستون جدول که مستقیم `date` خام را چاپ کرده‌اند.

## ۴. مدل ثبت‌نام — پیشنهاد من (تصمیم را به من سپردید)

**فعال‌سازی فوری + امکان تعلیق بعدی.** دلیل:

- تحلیل شما درست است و داده هم تأییدش می‌کند: سازمان «Vv» از ۱ آگوست
  `pending` مانده و کاربرش برنگشته. این دقیقاً همان ریزشی است که گفتید.
- تأیید دستی وقتی ارزش دارد که چیزی برای از دست دادن باشد. در تست
  رایگان، بدترین حالت یک ردیف بی‌مصرف در دیتابیس است.
- کامیکس هم همین کار را می‌کند: ثبت‌نام → دسترسی فوری.
- **پیامک را فعلاً نمی‌گذاریم** چون یک قدم به مسیر اضافه می‌کند و هزینه
  دارد؛ همان چیزی که می‌خواهیم حذف کنیم. اگر ثبت‌نام جعلی دیدید،
  بعداً اضافه‌اش می‌کنیم.
- تعلیق را نگه می‌داریم چون بازگشت‌پذیری لازم است.

---

# ب) مقایسه با رقبا

## کامیکس (kamixapp.ir) — بررسی کامل سایت

| قابلیت | کامیکس | ترازو |
|---|---|---|
| اسکن بارکد با دوربین | ✅ | ❌ |
| ثبت فاکتور با صدا | ✅ | ❌ |
| سایت تک‌صفحه‌ای فروشگاه | ✅ | ❌ |
| پنل پیامکی | ✅ | ❌ |
| اپ اندروید | ✅ | ❌ |
| انتخاب صنف + تحلیل اختصاصی | ✅ | ❌ |
| ویدیوی آموزشی (آپارات) | ✅ | ❌ |
| انبار / مشتریان / گزارش سود | ✅ | ✅ |
| چند شعبه / چند کاربر با نقش | ❌ | ✅ |
| کاردکس و گردش انبار | ❌ | ✅ |
| باشگاه مشتریان و سگمنت | ❌ | ✅ |
| چک‌ها و خزانه‌داری | ❌ | ✅ |

**قیمت:** کامیکس ۱۵۰ هزار تومان ماهانه (۳۹۰ سه‌ماهه، ۱٬۲۰۰ سالانه).
ترازو ۱۴۹ هزار تومان ماهانه — عملاً هم‌قیمت ولی بدون اسکنر و صدا.

**جمع‌بندی:** ترازو در عمق حسابداری جلوتر است، کامیکس در سهولت موبایل.
دو قابلیتی که واقعاً تفاوت ایجاد می‌کنند: **اسکن دوربین** و **فاکتور با صدا**.

## پنل سوپرادمین — استاندارد صنعت

از بررسی الگوهای مرجع SaaS، پنل ادمین معمولاً این‌ها را دارد:

**فاز ۱ (ضروری):**
- جستجوی سازمان/کاربر + نمای واحد (اشتراک، مصرف، صورت‌حساب)
- تغییر پلن، تمدید تست، تعلیق، بستن حساب
- لاگ ممیزی کامل (چه کسی، چه کاری، کِی)
- نقش‌های تفکیک‌شده‌ی ادمین (سوپرادمین / پشتیبانی / مالی / فقط‌خواندنی)

**فاز ۲:**
- ورود به‌جای کاربر (impersonate) — با لاگ اجباری و نشان بصری
- عملیات دسته‌ای، پرچم قابلیت‌ها، اعلان سراسری، تیکت پشتیبانی
- داشبورد درآمد (MRR، نرخ تبدیل تست به پولی)

**نکته‌ی امنیتی مهم درباره‌ی impersonate:** هر جلسه باید
`actor_user_id` واقعی (ادمین) را لاگ کند، نه کاربر جعل‌شده. رویداد شروع
و پایان جفت باشد و جلسه‌ی بازِ بیش از ۲ ساعت هشدار بدهد.

---

# ج) ده سوپر-پرامپت

> هر پرامپت مستقل است. ترتیب پیشنهادی همان شماره‌گذاری است.
> در هر کدام قیدهای همیشگی پروژه (فارسی، RTL، توکن معنایی، PanelManager،
> سه بریک‌پوینت، تست واقعی) برقرار است و تکرار نشده مگر جایی که خاص باشد.

---

## پرامپت ۱ — ثبت‌نام فوری و حذف انتظار تأیید 🔴 اولویت اول

```
هدف: کاربر جدید بلافاصله بعد از ثبت‌نام وارد پنل شود، بدون انتظار
برای تأیید ادمین. شواهد نشان می‌دهد سازمان «Vv» از ۱ آگوست pending
مانده و کاربرش برنگشته — همین ریزش را می‌خواهیم حذف کنیم.

وضعیت فعلی که باید تغییر کند:
- supabase/migrations/0021_platform_admin.sql بخش ۷: تابع bootstrap_org
  سازمان جدید را با approval_status='pending' می‌سازد.
- app/(app)/layout.tsx کاربر pending را به /pending-approval می‌فرستد.

کار خواسته‌شده:

۱. مایگریشن 0026:
   - bootstrap_org را طوری تغییر بده که approval_status='approved' و
     یک اشتراک trial با انقضای ۱۴ روزه بسازد.
   - ستون‌های لازم روی organizations یا subscriptions اضافه کن:
     trial_ends_at، و اگر لازم است business_type.
   - ⚠️ سازمان‌های موجود دست نخورند. «Vv» که الان pending است را هم
     تغییر نده — تصمیمش با کاربر است. فقط ثبت‌نام‌های جدید.
   - rollback بنویس.

۲. فرم onboarding بعد از ثبت‌نام (نه قبل — اصطکاک کمتر):
   - نام و نام خانوادگی
   - شماره تماس (اعتبارسنجی ۰۹xxxxxxxxx، ارقام فارسی هم قبول شود)
   - نوع کسب‌وکار: از فهرست صنف‌ها (پوشاک، کافه و رستوران، سوپرمارکت،
     داروخانه و آرایشی، موبایل، طلا و جواهر، قنادی، ابزار، لوازم‌التحریر،
     سایر) — همان الگوی کامیکس
   - نام کسب‌وکار
   حداکثر یک صفحه، قابل رد کردن نباشد ولی سریع پر شود.

۳. /pending-approval را حذف نکن — برای حالت تعلیق‌شده توسط ادمین لازم
   است. فقط متنش را متناسب کن (الان می‌گوید «منتظر تأیید»، باید بین
   «معلق شده» و «رد شده» فرق بگذارد).

۴. سوپرادمین همچنان بتواند تعلیق/رد کند. یعنی approval_status می‌ماند،
   فقط مقدار پیش‌فرضش برای ثبت‌نام جدید عوض می‌شود.

تست واقعی لازم:
- یک کاربر تستی کاملاً جدید بساز (ایمیل یک‌بارمصرف)، کل مسیر
  ثبت‌نام → onboarding → داشبورد را با Playwright طی کن و ثابت کن
  هیچ‌جا گیر نمی‌کند. بعد آن کاربر و سازمانش را کامل پاک کن.
- ثابت کن سه سازمان موجود دست‌نخورده‌اند (نام و approval_status).
- تعلیق توسط ادمین → کاربر به صفحه‌ی مناسب می‌رود → رفع تعلیق → برمی‌گردد.
```

---

## پرامپت ۲ — شمارنده‌ی گرافیکی تست ۱۴ روزه

```
هدف: کاربر در حال تست همیشه بداند چند روز اعتبار دارد.

جای نمایش: نوار بالای پنل، کنار DemoBanner موجود
(components/shared/demo-banner.tsx الگوی خوبی است — همان‌جا که
is_demo را چک می‌کند).

خواسته:
۱. کامپوننت TrialCountdown:
   - حلقه‌ی پیشرفت SVG یا نوار افقی که با گذشت روزها پر می‌شود
   - «۹ روز از ۱۴ روز باقی مانده» با ارقام فارسی
   - سه حالت رنگی با توکن معنایی: بیش از ۷ روز = success،
     ۳ تا ۷ = warning، کمتر از ۳ = destructive
   - دکمه‌ی «ارتقا به نسخه کامل» → /pricing
   - در روز آخر پیام فوریت‌دار
۲. انیمیشن با همان کی‌فریم‌های موجود globals.css. فقط transform و
   opacity. prefers-reduced-motion رعایت شود.
۳. برای سازمان‌های پولی یا سازمان‌های قدیمی که trial_ends_at ندارند،
   هیچ‌چیز رندر نشود (مثل الگوی DemoBanner).
۴. موبایل: فشرده — فقط عدد روز و حلقه، بدون متن بلند.

تست واقعی: با دستکاری trial_ends_at روی یک سازمان تستی، هر سه حالت
رنگی و حالت منقضی را اسکرین‌شات بگیر. سازمان واقعی را دست نزن.
```

---

## پرامپت ۳ — تقویم فارسی در دو صفحه‌ی باقی‌مانده

```
هدف: حذف آخرین تاریخ‌های میلادی.

⚠️ قبل از شروع بدان: من ۱۱ صفحه را روی سایت زنده اسکن کردم. برخلاف
تصور اولیه، فقط دو صفحه مشکل دارند. بقیه از قبل درست‌اند. وقت را
صرف بازنویسی چیزی که کار می‌کند نکن.

نقاط دقیق:
- /reports/overview-v2  → ۸ تاریخ به شکل 2026-07-12
- /inventory/movements  → ۱ تاریخ به شکل 2026-07-05

کار:
۱. منبع هر کدام را پیدا کن. حدس اولیه: محور X نمودار Recharts که
   مقدار خام date را می‌گیرد، و یک ستون جدول.
۲. با toJalali از lib/utils/format.ts درست کن. تابع جدید نساز.
۳. tooltip نمودار هم باید جلالی نشان بدهد، نه فقط محور.
۴. بعد از اصلاح، همان اسکریپت اسکن را روی هر ۱۱ صفحه دوباره اجرا کن
   و ثابت کن هر ۱۱ تا OK شده‌اند.
۵. اگر input[type=date] بومی جایی پیدا کردی (الان صفر است) با
   DatePicker جلالی عوضش کن.

خروجی: جدول قبل/بعد برای هر ۱۱ صفحه.
```

---

## پرامپت ۴ — نمودارهای جذاب‌تر

```
هدف: نمودارها حرفه‌ای‌تر و خواناتر شوند.

فایل‌های درگیر:
- app/(app)/dashboard/components/DashboardSalesChart.tsx
- app/(app)/reports/overview-v2/page.tsx
- app/(app)/reports/page.tsx

خواسته:
۱. گرادیان زیر خط (area gradient) با رنگ primary
۲. نقطه‌ی فعال هنگام hover + خط راهنمای عمودی
۳. tooltip سفارشی فارسی: تاریخ جلالی + مبلغ با فرمت تومان
۴. انیمیشن ورود ملایم؛ با prefers-reduced-motion خاموش
۵. حالت خالی طراحی‌شده (الان احتمالاً نمودار خالی می‌ماند)
۶. حالت بارگذاری skeleton
۷. موبایل: تعداد تیک محور X کم شود تا برچسب‌ها روی هم نیفتند
۸. مقایسه با دوره‌ی قبل به‌صورت خط نقطه‌چین کم‌رنگ، اگر داده هست

قیدها:
- فقط توکن معنایی. هیچ hex خام.
- ارقام محور و tooltip فارسی (toFaDigits).
- اندازه‌ی فونت تیک‌ها زیر ۱۲px نرود — قبلاً این مشکل بود و در JS
  تنظیم شده، نه در CSS.
- کتابخانه‌ی جدید اضافه نکن؛ Recharts موجود کافی است.

تست: اسکرین‌شات دسکتاپ/تبلت/موبایل در حالت روشن و تاریک، به‌علاوه
حالت خالی و حالت بارگذاری.
```

---

## پرامپت ۵ — اسکن بارکد با دوربین 🔴 مزیت رقابتی

```
هدف: افزودن کالا به فاکتور با اسکن بارکد از دوربین موبایل.
کامیکس این را دارد و ما نداریم؛ برای فروشگاه‌دار موبایل‌محور
تفاوت اصلی همین است.

قبل از کدنویسی، بررسی کن و گزارش بده:
- کتابخانه: BarcodeDetector API بومی مرورگر در برابر
  @zxing/browser در برابر html5-qrcode. کدام روی اندروید/کروم و
  iOS/سافاری بهتر جواب می‌دهد؟ حجم باندل هرکدام؟
- استاندارد بارکد کالا در ایران: EAN-13، و آیا IRC (کد ۱۶ رقمی
  سازمان غذا و دارو) هم باید پشتیبانی شود؟

پیاده‌سازی:
۱. دکمه‌ی اسکن در InvoiceCreateForm کنار جستجوی کالا
۲. مودال دوربین با کادر هدف و راهنمای بصری
۳. مدیریت مجوز دوربین: اگر رد شد پیام روشن فارسی و راه جایگزین
   (ورود دستی کد)
۴. HTTPS اجباری — روی لوکال‌هاست هم کار کند
۵. بعد از اسکن موفق: بازخورد صوتی/لرزشی کوتاه، کالا مستقیم به
   فاکتور اضافه شود، دوربین برای اسکن بعدی باز بماند
۶. اگر بارکد در دیتابیس نبود: پیشنهاد «ثبت کالای جدید با این بارکد»
۷. ستون barcode روی products — بررسی کن هست یا مایگریشن لازم است
۸. اسکن پشت‌سرهم بدون بستن مودال (فروشنده معمولاً چند کالا دارد)

تست واقعی: با تصویر بارکد شبیه‌سازی‌شده در Playwright
(fake media stream یا تزریق فریم) ثابت کن مسیر کامل کار می‌کند.
اگر شبیه‌سازی ممکن نبود، صادقانه بگو و دستورالعمل تست دستی بده.
```

---

## پرامپت ۶ — ثبت فاکتور با صدا 🔴 مزیت رقابتی

```
هدف: فروشنده بگوید «سه عدد شومیز وارداتی» و کالا به فاکتور اضافه شود.

قبل از کدنویسی بررسی کن و گزارش بده:
- Web Speech API با تنظیم fa-IR: کیفیت تشخیص فارسی چقدر است؟
  روی کروم اندروید در برابر سافاری iOS چه تفاوتی دارد؟
- جایگزین‌های سرویسی برای فارسی و هزینه‌شان
- آیا آفلاین ممکن است یا حتماً اینترنت لازم است؟

پیاده‌سازی:
۱. دکمه‌ی میکروفون در InvoiceCreateForm
۲. نمایش زنده‌ی متن در حال تشخیص + موج صوتی ساده
۳. تحلیل متن فارسی:
   - عدد فارسی و لاتین و حروفی («سه»، «۳»، «3»)
   - واحد: عدد، بسته، کارتن، متر، کیلو
   - تطبیق فازی نام کالا با کاتالوگ (غلط تشخیص زیاد است)
   - اگر چند کالا شبیه بود، فهرست انتخاب نشان بده — هرگز حدس نزن
۴. تأیید قبل از افزودن: «۳ عدد شومیز وارداتی — درست است؟»
   اضافه‌کردن مستقیم بدون تأیید خطرناک است.
۵. اگر مرورگر پشتیبانی نکرد، دکمه اصلاً نشان داده نشود
۶. مجوز میکروفون مثل دوربین مدیریت شود

⚠️ صادق باش: اگر کیفیت تشخیص فارسی Web Speech ضعیف بود، همان را
گزارش کن. یک قابلیت نیمه‌کاره که ۴۰٪ مواقع اشتباه کالا اضافه می‌کند
از نبودش بدتر است.

تست: حداقل ۱۰ عبارت واقعی فارسی از کاتالوگ خودمان، با گزارش نرخ
موفقیت.
```

---

## پرامپت ۷ — پنل سوپرادمین فاز ۱

```
هدف: پنل مدیریت پلتفرم فراتر از «چه کسی عضو شده».

الان چه داریم: /admin/organizations فقط فهرست و تأیید/رد.

فاز ۱ — این‌ها را بساز:

۱. نقش‌های تفکیک‌شده‌ی ادمین
   جدول platform_admins ستون role دارد ('admin' | 'support') ولی
   is_platform_admin() بینشان فرقی نمی‌گذارد. تفکیک کن:
   - super_admin: همه‌چیز
   - support: مشاهده + تمدید تست، بدون حذف و بدون تغییر مالی
   - finance: اشتراک و پرداخت، بدون دسترسی به داده‌ی کسب‌وکار
   - readonly: فقط مشاهده

۲. نمای تک‌سازمان /admin/organizations/[id]
   اشتراک، مصرف، اعضا، آخرین فعالیت، فاکتورها — همه در یک صفحه

۳. مدیریت اشتراک: تغییر پلن، تمدید دستی تست، تعلیق، فعال‌سازی مجدد

۴. مدیریت کاربران در سطح پلتفرم: جستجو بین همه‌ی کاربران،
   بازنشانی رمز، غیرفعال‌سازی

۵. لاگ ممیزی /admin/audit
   جدول‌های audit از migration 0012 و 0016 موجودند — اول بررسی کن
   چه چیزی الان ثبت می‌شود و چه چیزی جا افتاده. هر عمل ادمین باید
   با actor واقعی، زمان، IP و هدف ثبت شود.

۶. تأیید و اصلاح فاکتور در سطح پلتفرم (خواسته‌ی صریح شما):
   سوپرادمین بتواند فاکتور هر سازمان را ببیند و در صورت لزوم
   ابطال/اصلاح کند — با ثبت اجباری دلیل در لاگ.

۷. داشبورد /admin: تعداد سازمان‌ها، تست‌های فعال، نرخ تبدیل،
   ثبت‌نام‌های اخیر، سازمان‌های در آستانه‌ی انقضا

قیدها:
- هر مسیر جدید زیر گارد app/(app)/admin/layout.tsx می‌آید که هست.
- هر عمل حساس: دیالوگ تأیید + ثبت در لاگ.
- الگوی امنیتی migration 0025 را ادامه بده: توابع security definer
  نباشند و EXECUTE از authenticated گرفته شود.
- ⚠️ روی داده‌ی واقعی من هیچ عمل مخربی تست نکن. سازمان یک‌بارمصرف
  بساز، تست کن، پاک کن.
```

---

## پرامپت ۸ — پنل سوپرادمین فاز ۲

```
پیش‌نیاز: پرامپت ۷ تمام شده باشد.

۱. ورود به‌جای کاربر (impersonate)
   ⚠️ حساس‌ترین قابلیت. الزامات:
   - نوار قرمز ثابت بالای صفحه: «شما به‌جای X وارد شده‌اید — خروج»
   - جلسه حداکثر ۳۰ دقیقه، بعد خودکار قطع
   - رویداد شروع و پایان جفت لاگ شود با actor واقعی (ادمین)،
     نه کاربر جعل‌شده
   - ثبت دلیل اجباری قبل از شروع
   - نقش support فقط حالت فقط‌خواندنی

۲. اعلان سراسری: بنر قابل تنظیم برای همه یا یک سازمان
۳. تیکت پشتیبانی: کاربر ثبت کند، ادمین پاسخ دهد
۴. پرچم قابلیت‌ها: روشن/خاموش کردن قابلیت برای سازمان خاص
۵. عملیات دسته‌ای: تمدید تست چند سازمان، خروجی CSV
۶. داشبورد مالی: درآمد ماهانه، نرخ تبدیل تست به پولی، ریزش

همان قیدهای امنیتی فاز ۱.
```

---

## پرامپت ۹ — قابلیت‌های کامیکس که نداریم

```
پیش‌نیاز: پرامپت‌های ۵ و ۶ (اسکن و صدا) جدا هستند و اینجا تکرار نمی‌شوند.

۱. سایت تک‌صفحه‌ای فروشگاه
   هر مشتری یک صفحه‌ی عمومی: tarazoo.ir/s/<slug>
   شامل نام، لوگو، آدرس، تلفن، ساعت کاری، چند کالای منتخب.
   - slug یکتا و قابل ویرایش در تنظیمات
   - قابل خاموش کردن
   - SEO: متادیتا و sitemap
   - ⚠️ امنیت: فقط داده‌ای که مالک صریحاً عمومی کرده. RLS جدا.
     نشت ناخواسته‌ی فهرست کالا یا قیمت خرید فاجعه است.

۲. پنل پیامکی
   - اتصال به یک سرویس ایرانی (کاوه‌نگار یا ملی‌پیامک — هزینه و API
     را مقایسه کن و گزارش بده)
   - قالب پیام: یادآوری بدهی، تشکر بعد از خرید، تولد
   - ارسال گروهی به سگمنت (بخش CRM از قبل سگمنت دارد)
   - ⚠️ قبل از پیاده‌سازی از من بپرس کدام سرویس و آیا اعتبار داری

۳. انتخاب صنف با تحلیل اختصاصی
   در صفحه‌ی معرفی، کاربر صنفش را انتخاب کند و ببیند ترازو دقیقاً
   چه مشکلی از او حل می‌کند. محتوای ثابت، بدون بک‌اند.

۴. PWA به‌جای اپ اندروید
   کامیکس اپ نیتیو دارد. ساخت اپ نیتیو هفته‌ها کار است؛ PWA با
   manifest موجود (که داریم) و service worker، نصب روی صفحه‌ی خانه
   و کار آفلاین محدود می‌دهد. نسبت هزینه به فایده بهتر است.
   - بررسی کن sw.js فعلی چه می‌کند و آیا کافی است

هر کدام را جدا و قابل‌عرضه بساز؛ همه را یک‌جا قاطی نکن.
```

---

## پرامپت ۱۰ — چرخاندن کلید لو رفته 🔴 فوری

```
هدف: کلید service_role که روی گیت‌هاب عمومی بوده باطل شود.

وضعیت: تاریخچه‌ی گیت در ۲ آگوست تمیز شد، ولی خود کلید هنوز معتبر
است. تا وقتی چرخانده نشود، هرکسی که قبلاً مخزن را کلون کرده دسترسی
کامل به کل دیتابیس دارد — دور زدن تمام RLS.

مراحل (SECURITY_RUNBOOK.md را دنبال کن):
۱. کلید جدید در داشبورد سوپابیس بساز
۲. متغیر محیطی Vercel را در هر سه هدف به‌روز کن
۳. دیپلوی و تأیید سلامت اپ
۴. کلید قدیمی را باطل کن
۵. تأیید نهایی: درخواست با کلید قدیمی باید ۴۰۱ بدهد

⚠️ ترتیب مهم است. اول باطل کردن بعد جایگزینی = قطعی سرویس.

بعد از چرخاندن، این را هم بررسی کن:
- شاخه‌ی backup/pre-filter-repo-2026-08-02 روی گیت‌هاب تاریخچه‌ی
  آلوده را دارد. بعد از اطمینان از سلامت همه‌چیز، حذفش کن.
- رمز 123456 روی حساب‌های bahram و test هنوز فعال است.
```

---

# د) ترتیب پیشنهادی اجرا

| # | کار | چرا این ترتیب |
|---|---|---|
| ۱ | چرخاندن کلید (۱۰) | خطر فعال است، به کد ربطی ندارد |
| ۲ | ثبت‌نام فوری (۱) | مستقیم جلوی ریزش کاربر را می‌گیرد |
| ۳ | شمارنده‌ی تست (۲) | مکمل مورد قبلی، کار کمی دارد |
| ۴ | تقویم (۳) | دو صفحه، سریع |
| ۵ | نمودارها (۴) | ظاهری، مستقل |
| ۶ | اسکن بارکد (۵) | بزرگ‌ترین مزیت رقابتی |
| ۷ | ادمین فاز ۱ (۷) | زیربنای مدیریت |
| ۸ | فاکتور صوتی (۶) | بعد از اسکن، چون ریسک فنی بیشتری دارد |
| ۹ | ادمین فاز ۲ (۸) | روی فاز ۱ سوار است |
| ۱۰ | قابلیت‌های کامیکس (۹) | هر کدام جدا، بعد از تثبیت هسته |

---

# ه) پرسش‌های باز

پیش از اجرای پرامپت‌های ۵، ۶ و ۹ به تصمیم شما نیاز است:

1. **سرویس پیامک** — کدام سرویس؟ اعتبار دارید؟
2. **دامنه‌ی tarazoo.ir** — ثبت شده؟ برای سایت فروشگاه لازم است.
3. **قیمت‌گذاری** — کامیکس ۱۵۰ هزار ماهانه با اسکنر و صدا. شما
   ۱۴۹ هزار بدون آن‌ها. بعد از افزودن، قیمت را بازبینی می‌کنید؟
4. **سازمان «Vv»** — تأیید شود، رد شود، یا دست‌نخورده بماند؟


---

# و) درخواست‌های تازه — صف بعدی

این‌ها بعد از پرامپت‌های ۱ تا ۱۰ اجرا می‌شوند. ترتیب پیشنهادی بر پایه‌ی
«چقدر روی کاربر اثر دارد» در برابر «چقدر کار می‌برد».

## و-۱) نسخه‌گذاری خودکار 🔴 اول از همه

الان `BRAND_VERSION` در `lib/brand.ts` دستی روی «۱.۰» ثابت است و هیچ
ارتباطی با کد ندارد.

خواسته:
- نسخه از کامیت واقعی ساخته شود (تعداد کامیت + هش کوتاه)
- در پاورقی سایدبار و صفحه‌ی «درباره» دیده شود
- کاربر بفهمد نسخه‌ی تازه‌ای آمده و بتواند رفرش کند

چرا اول؟ کم‌کارترین است و هر کار بعدی را قابل ردیابی می‌کند.
پیشنهاد فنی: `NEXT_PUBLIC_BUILD_ID` از `VERCEL_GIT_COMMIT_SHA` +
یک endpoint سبک `/api/version` که کلاینت هر چند دقیقه چک کند.

## و-۲) نوار کاشی‌ها — دلار، آب‌وهوا و…

⚠️ نکته‌ی مهم: نرخ ارز و آب‌وهوا هر دو به API خارجی نیاز دارند.
باید تصمیم بگیرید:
- کدام منبع نرخ ارز؟ (منابع ایرانی معمولاً کلید می‌خواهند)
- آیا هزینه‌ی اشتراک قبول است؟
- اگر API قطع شد، کاشی چه نشان بدهد؟

بدون پاسخ این‌ها نمی‌شود شروع کرد. پیشنهاد: کاشی‌ها را طوری بسازیم
که هر منبع داده‌ای قابل وصل باشد و در نبود داده، خودشان را پنهان کنند.

## و-۳) داشبورد بر پایه‌ی بیشترین استفاده

خواسته: چیدمان داشبورد بر اساس رفتار واقعی کاربر، با بررسی الگوی
برندهای مرجع.

پیش‌نیاز: باید بدانیم کاربر واقعاً از چه چیزی بیشتر استفاده می‌کند.
جدول‌های audit از migration 0012 و 0016 موجودند — اول باید تحلیل شوند.
بدون داده، «بیشترین استفاده» حدس است.

## و-۴) اپ اندروید

⚠️ صادقانه: ساخت اپ نیتیو هفته‌ها کار است و به حساب Google Play
(۲۵ دلار یک‌بار) نیاز دارد.

پیشنهاد جایگزین که ۹۰٪ فایده را با ۱۰٪ هزینه می‌دهد: **PWA**.
`manifest.webmanifest` از قبل هست. با یک service worker درست،
کاربر می‌تواند اپ را روی صفحه‌ی خانه نصب کند و آفلاین محدود داشته
باشد. اگر بعداً واقعاً اپ نیتیو خواستید، Capacitor همین PWA را
بسته‌بندی می‌کند.

## و-۵) موارد تکراری که قبلاً حل شده‌اند

این‌ها در پیام شما دوباره آمده بودند ولی از قبل انجام شده‌اند:

| مورد | وضعیت |
|---|---|
| ثبت‌نام فوری + تست ۱۴ روزه | ✅ کامیت `c80d986` |
| شمارنده‌ی گرافیکی تست | ✅ کامیت `1dedd36` |
| یکسان‌سازی فرم فروش جدید | ✅ کامیت `0610952` |
| کلید F2 | ✅ کامیت `49d67a8` — روی سایت زنده تست شد |
| منوی سه‌نقطه خارج از کادر | ✅ کامیت `757ce74` — ۱۰ از ۱۰ آیتم قابل کلیک |
| تقویم فارسی در گزارش‌ها | ✅ کامیت `bb4e449` |
| نمودارهای جذاب‌تر | ✅ کامیت `c9cf9bd` |
| بسته‌شدن پنل هنگام جستجوی کالا | ✅ کامیت `c9cf9bd` |

اگر هنوز خراب می‌بینیدشان، `Ctrl+Shift+R` بزنید — کش مرورگر است.
