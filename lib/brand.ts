/**
 * هویت برند — تک‌منبع حقیقت.
 *
 * هر متنی که نام محصول را به کاربر نشان می‌دهد باید از اینجا بخواند،
 * تا تغییر نام در آینده فقط همین فایل را لمس کند.
 *
 * ⚠️ توجه: دامنه‌ی داخلی احراز هویت (AUTH_EMAIL_DOMAIN) عمداً اینجا نیست.
 * آن مقدار در Supabase Auth ذخیره شده و تغییرش کاربران را قفل می‌کند.
 */

/** نام فارسی محصول — آنچه کاربر می‌بیند. */
export const BRAND_NAME = "ترازو";

/** نام لاتین — برای نام فایل‌ها، آی‌دی‌ها و متادیتای انگلیسی. */
export const BRAND_NAME_EN = "Tarazoo";

/** شعار کوتاه؛ در عنوان صفحه و manifest استفاده می‌شود. */
export const BRAND_TAGLINE = "مدیریت هوشمند فروش و انبارداری";

/** توضیح یک‌خطی برای متادیتا و PWA. */
export const BRAND_DESCRIPTION =
  "ترازو به شما کمک می‌کند فاکتورها را سریع صادر کنید، موجودی انبار را در لحظه چک کنید و با گزارش‌های دقیق سود و زیان تصمیم بگیرید.";

/** عنوان کامل مرورگر. */
export const BRAND_TITLE = `${BRAND_NAME} | ${BRAND_TAGLINE}`;

/** نسخه‌ی نمایشی در پاورقی نوار کناری. */
export const BRAND_VERSION = "۱.۰";

/** ایمیل تماس عمومی روی صفحه‌ی معرفی. */
export const BRAND_CONTACT_EMAIL = "info@tarazoo.ir";
