# ورود اطلاعات از فایل اکسل — گزارش کار

**تاریخ:** ۱۴ مرداد ۱۴۰۵ · **کامیت:** `6e355e8` · **مهاجرت:** `0037`

---

## چه چیزی ساخته شد

کسب‌وکاری که تازه می‌آید، صدها کالا و مشتری در یک فایل اکسل دارد. تا امروز
تنها راه ورودشان، ثبت تک‌تک از طریق فرم بود.

دو مسیر روی یک موتور مشترک:

| مسیر | چه کسی | تفاوت |
|---|---|---|
| `/settings/import` | صاحب کسب‌وکار، برای سازمان خودش | امکان «برگرداندن» دارد |
| `/admin/import` | تیم پشتیبانی، برای هر کسب‌وکاری | **دلیل اجباری** + ثبت در ممیزی |

راهنمای کامل هم در `/settings/import/guide` نوشته شد: چهار گام، جدول تک‌تک
ستون‌ها، هشت پرسش رایج و شش اشتباه رایج.

### جریان سه‌مرحله‌ای

**قالب → بررسی → ثبت.**

مرحله‌ی «بررسی» حذف‌شدنی نبود. کاربر باید *پیش از* نوشتن بداند چند سطر سالم
است؛ بدون آن تنها راه فهمیدن، انجام‌دادن است — و آن وقت باید ۴۰۰ رکورد را
برگرداند. دکمه‌ی «ثبت نهایی» تا وقتی فایل بررسی نشده غیرفعال است.

---

## تصمیم‌ها و دلیلشان

**قالب برای هر کسب‌وکار جدا ساخته می‌شود.** شیت «فهرست» دسته‌بندی‌ها و
برندهای *همان* کسب‌وکار را دارد. قالب عمومی باعث می‌شد کاربر نام دسته را حدس
بزند و بعد ده‌ها هشدار «دسته وجود ندارد» بگیرد.

**موجودی هرگز مستقیم نوشته نمی‌شود.** یک تریگر در دیتابیس جلویش را می‌گیرد
(«`stock_qty` فقط از طریق `stock_movements` قابل تغییر است»)، پس موجودی اولیه
به‌صورت یک سند انبار با دلیل `opening` ثبت می‌شود. نتیجه: کاردکس کالا از روز
اول درست است و ورود هم قابل برگشت می‌ماند.

**هر رکورد به دسته‌ی ورودش وصل است.** ستون `import_job_id` روی سه جدول اضافه
شد. بدون آن، برگرداندن یک ورود اشتباه یعنی حذف تک‌تک.

**رکورد استفاده‌شده حذف نمی‌شود.** اگر کالایی در این فاصله در فاکتوری آمده
باشد، حذفش یعنی فاکتور بی‌قلم می‌شود و گزارش‌های مالی به هم می‌ریزد. چنین
رکوردهایی فقط غیرفعال می‌شوند و شمارششان جدا گزارش می‌گردد.

**حالت پیش‌فرض برخورد با تکراری: «رد کن و دست نزن».** در حالت «به‌روزرسانی»
فقط ستون‌هایی نوشته می‌شوند که در فایل مقدار دارند — وگرنه یک سلول خالی،
آدرس و توضیحی را که مشتری از قبل داشت پاک می‌کرد و «به‌روزرسانی» عملاً به
«حذف اطلاعات» تبدیل می‌شد.

**دسته‌بندی جدید ساخته نمی‌شود، برند بله.** هنگام کار کشف شد که جدول
`categories` **ستون `org_id` ندارد** و تنها policy آن `true` است — یعنی
دسته‌بندی‌ها بین همه‌ی کسب‌وکارها مشترک‌اند (۱۵ ردیف فعلی «پاپ»، «راک»، «جاز»…
از یک پروژه‌ی قدیمی موزیک باقی مانده). اگر ورود اکسل اجازه‌ی ساخت دسته می‌داد،
دسته‌های یک مزون پوشاک در حساب همه‌ی کسب‌وکارهای دیگر ظاهر می‌شد. عمداً در این
مهاجرت اصلاح **نشد** (افزودن `org_id` به جدولی که `products` به آن ارجاع دارد،
ریسک مستقل خودش را دارد) و به‌جایش مسیر ورود فقط با دسته‌های موجود تطبیق
می‌دهد.

**دو مجوز جدا برای ادمین.** `data.import` پرخطر علامت خورده و فقط به مدیر ارشد
داده می‌شود؛ نوشتن انبوه در دیتابیس مشتری از خواندن فاکتورش حساس‌تر است.

---

## 🔴 شش باگ که پیدا و رفع شد

### ۱. کتابخانه‌ی اکسل آسیب‌پذیری وصله‌نشده داشت

بسته‌ی `xlsx` روی npm در نسخه‌ی ۰٫۱۸٫۵ متوقف شده و **CVE-2023-30533**
(Prototype Pollution) دارد: یک فایل دستکاری‌شده می‌تواند `Object.prototype` را
آلوده کند. نسخه‌ی وصله‌شده فقط از CDN خودِ SheetJS پخش می‌شود و روی npm نیست.

`exceljs` هم بررسی و رد شد: نگهداری نمی‌شود و زنجیره‌ی
`archiver → glob 7 → inflight` چند CVE باز دارد.

انتخاب: **`@e965/xlsx`** — بازنشر رسمی نسخه‌ی وصله‌شده (۰٫۲۰٫۳)، **صفر
وابستگی**. با آزمون عملی تأیید شد: شیتی با کلید `__proto__` ساخته شد و
`({}).polluted` تعریف‌نشده ماند.

### ۲. هر فایلی به‌عنوان CSV خوانده می‌شد

SheetJS قالب را حدس می‌زند. متن «این یک فایل اکسل نیست» بدون خطا خوانده شد و
یک سرستون به‌هم‌ریخته داد:

```
headers: ["Ø§ÛÙ ÛÚ© ÙØ§ÛÙ Ø§Ú©Ø³Ù ÙÛØ³Øª"]
```

نتیجه‌ی عملی: کاربری که اشتباهی PDF یا عکس آپلود می‌کرد، به‌جای «این فایل اکسل
نیست»، پیام گیج‌کننده‌ی «ستون اجباری نام کالا در فایل نیست» می‌گرفت و دنبال
ستونی می‌گشت که اصلاً وجود نداشت. حالا امضای فایل (ZIP magic bytes) پیش از
سپردن به کتابخانه بررسی می‌شود.

### ۳. CSV فارسی کاملاً خراب می‌شد

با `type:"buffer"`، SheetJS بایت‌ها را Latin-1 فرض می‌کند:

```
"نام,موبایل"  →  headers: ["ÙØ§Ù", "ÙÙØ¨Ø§ÛÙ"]
```

فایل بدون خطا خوانده می‌شد ولی هیچ ستونی شناخته نمی‌شد. حالا CSV صریحاً
UTF-8 رمزگشایی می‌شود و BOM (که اکسل ویندوز اضافه می‌کند) برداشته می‌شود.

### ۴. تابع برگرداندن با نام ستون اشتباه نوشته شده بود

نام ستون‌ها یکسان نیست و حدس زدنشان اشتباه بود:

```
sales.customer_id  ·  purchases.supplier_id  ·  transactions.contact_id
```

نسخه‌ی اول هر سه را `contact_id` فرض کرد و تابع با
`column s.contact_id does not exist` شکست. **نه `tsc`، نه `next build` و نه
هیچ تست واحدی این را نگرفت** — فقط فراخوانی واقعی روی دیتابیس.

### ۵. ساختار عناوین شکسته بود

پنل‌ها از `h1` مستقیم به `h3` می‌پریدند. axe آن را `heading-order` گزارش کرد؛
صفحه‌خوان سلسله‌مراتب را اشتباه اعلام می‌کند.

### ۶. ارقام انگلیسی وسط پیام فارسی

پیام «تکراری با سطر 2» با رقم انگلیسی، با بقیه‌ی اعداد صفحه که فارسی‌اند
ناهماهنگ بود.

---

## نکته‌های ریز که در ورودی رعایت شد

- **ارقام فارسی و عربی** تبدیل می‌شوند. بدون آن «۱۲۵۰۰۰» که کاربر ایرانی
  طبیعی می‌نویسد `NaN` می‌شد و کل سطر رد.
- **تومان → ریال.** دیتابیس ریال ذخیره می‌کند، ستون اکسل «(تومان)» برچسب دارد.
  جاافتادن ×۱۰ یعنی همه‌ی قیمت‌ها یک‌دهم ثبت شوند.
- **موبایل یکدست می‌شود:** `۰۹۱۲…`، `+98…`، `0098…`، `9121234567` و
  `0912-123-4567` همه به یک شکل. وگرنه دو شکل از یک شماره، دو مشتری جدا
  می‌شدند.
- **نیم‌فاصله حفظ می‌شود** (بخشی از املای درست فارسی) ولی نویسه‌های جهت‌دهی
  نامرئی که با کپی از وب می‌آیند حذف می‌شوند.
- **«ي» و «ك» عربی** در سرستون‌ها تحمل می‌شود.
- **قیمت نامفهوم رد می‌شود، نه اینکه صفر ثبت شود.** اگر «۲۵۰ هزار» بی‌صدا صفر
  می‌شد، کاربر ماه‌ها بعد در گزارش سود متوجه می‌شد.
- **یک پیراهن در سه رنگ = سه واریانت از یک کالا**، نه سه کالا و نه یک تکراری.
- **مخاطب بدون شماره پذیرفته می‌شود.** رد کردنش یعنی کاربر شماره‌ی الکی بسازد.
- **سطر خالی نادیده گرفته می‌شود** — فایل اکسل معمولاً چند سطر خالی ته دارد.
- **شماره‌ی سطر در گزارش خطا** دقیقاً همان است که کاربر در اکسل می‌بیند.

---

## تست واقعی روی production build

| مورد | نتیجه |
|---|---|
| قالب دانلودشده با پارسر خودمان سازگار است | ✅ سه شیت: داده، راهنما، فهرست |
| **پیش‌نمایش هیچ چیزی ننوشت** | ✅ `import_jobs = 0` |
| ۶ سطر ورودی → ۳ ثبت، ۳ رد با دلیل | ✅ |
| تبدیل ۳۹۰٬۰۰۰ تومان → ۳٬۹۰۰٬۰۰۰ ریال | ✅ |
| پیراهن دو رنگ → **۱ کالا با ۲ واریانت** | ✅ |
| موجودی اولیه از سند `opening` | ✅ ۲ سند، مجموع ۸ عدد |
| برند تازه خودکار ساخته شد | ✅ |
| دسته‌ی نامعتبر → هشدار، کالا ثبت شد | ✅ |
| ثبت در گزارش ممیزی با دلیل | ✅ |
| برگرداندن کامل، شامل اسناد انبار | ✅ |
| موبایل `۰۹۱۲…` و `+98…` یکدست شد | ✅ |
| تکراری با شکل متفاوت شماره تشخیص داده شد | ✅ |
| مخاطب بدون شماره پذیرفته شد | ✅ |
| نوع نامعتبر («فروشگاه») رد شد | ✅ |
| برگرداندن دوباره | ✅ `HTTP 409` |
| axe دسکتاپ / تبلت / موبایل | ✅ صفر ایراد |
| سرریز افقی | ✅ صفر |

### وضعیت داده‌ی شما پس از تست

همه‌ی داده‌های آزمایشی حذف شدند و شمارش‌ها **دقیقاً** با پیش از شروع یکی است:

```
کالاها ۳۸۲ · واریانت‌ها ۳۸۵ · اشخاص ۵۴۸ · گردش انبار ۷۳۱
فاکتورها ۲۱ · کاربران ۵ · سازمان‌ها ۳ · برندها ۰ · ورودها ۰
```

تست‌ها روی «سازمان تست پنل» انجام شد که داده‌ی واقعی ندارد.

---

## وضعیت نهایی

```
npx tsc --noEmit   → پاک
npx vitest run     → ۶۳۰/۶۳۰ (۲۸ فایل، ۹۴ تست جدید)
npm run build      → Compiled successfully
axe-core           → صفر ایراد
npm audit          → بسته‌ی جدید صفر آسیب‌پذیری اضافه نکرد
```

تست‌ها با برگرداندن عمدی باگ راستی‌آزمایی شدند: با تغییر `customer_id` به
`contact_id` تست شکست و پس از بازگردانی سبز شد.

---

## آنچه هنوز نیست

- **خروجی اکسل (پشتیبان‌گیری)** — در راهنما به کاربر گفته شده پیش از ورود
  پشتیبان بگیرد، ولی دکمه‌ی «دانلود همه‌ی داده‌ها» هنوز ساخته نشده. قدم منطقی
  بعدی.
- **ورود فاکتورها و تراکنش‌های گذشته** — پیچیده‌تر است چون به کالا و مشتریِ
  از قبل موجود ارجاع می‌دهد.
- **`categories` بدون `org_id`** — یک بدهی فنی که مستقل باید اصلاح شود.
- **گزارش خطا به‌صورت فایل اکسل** — تابعش نوشته و تست شده
  (`buildErrorReport`) ولی هنوز به رابط کاربری وصل نشده؛ فعلاً فهرست خطاها
  روی صفحه نمایش داده می‌شود.
