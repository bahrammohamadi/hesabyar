# واحد پول، شخصی‌سازی صنفی، منوهای کشویی و کدهای پشتیبان

نسخه‌ی زنده: `26.0822` · `5c15c6b` · ۳۱ مرداد ۱۴۰۵

---

## اول تحقیق، بعد طراحی

«اصولی» یعنی اول ببینیم استاندارد چیست. سه تحقیق انجام شد:

### ۱) واحد پول — نرم‌افزارهای ایرانی چه می‌کنند؟

**چندارزی واقعی را محصول جداگانه می‌فروشند.** هلو و سپیدار «نسخه‌ی
صرافی» دارند با نرخ تبدیل، سند تسعیر و حساب ارزی — قیمتش چند برابر
نسخه‌ی عادی است. در نسخه‌ی معمولی فقط **واحد نمایش** عوض می‌شود؛
تدبیر صراحتاً «قابلیت تغییر واحد پول (تومان به جای ریال) در گزارشات»
را تبلیغ می‌کند.

**تصمیم:** دیتابیس **همیشه ریال** می‌ماند، فقط نمایش و ورودی عوض
می‌شوند.

🔴 **چرا این تصمیم درست است:** اگر واحد ذخیره‌سازی قابل تغییر بود،
عوض‌کردنش یعنی بازنویسی هر مبلغ در `sales`، `purchases`،
`transactions`، `sale_items`… روی دفتر مالی واقعی. یک خطا در آن
مهاجرت یعنی داده‌ی مالی نابود.

### ۲) کدهای پشتیبان — NIST SP 800-63B §4.2.1.1

استاندارد صریح است:
- `SHALL` هش‌شده با تابع یک‌طرفه ذخیره شوند
- `SHALL` یک‌بارمصرف باشند
- `SHALL` مشمول محدودیت نرخ باشند
- کدهای با آنتروپی کمتر از ۱۱۲ بیت باید **نمک‌دار** باشند

هر چهار مورد رعایت شد؛ نمک `per-user` است.

### ۳) منوهای کشویی — الگوی سپیدار

«واحدهای سنجش» فهرستی است که کاربر خودش تعریف می‌کند، نه فهرست
قفل‌شده. سامانه‌ی مؤدیان هم کد استاندارد واحد می‌خواهد — به همین
دلیل ستون `code` از الان در جدول هست، حتی اگر فعلاً استفاده نشود.

---

## آنچه ساخته شد

### واحد پول

انتخاب تومان یا ریال از «تنظیمات ← نمایش و شخصی‌سازی»، با
**پیش‌نمایش زنده** که همان مبلغ را در هر دو واحد نشان می‌دهد.

⚠️ متن صریح به کاربر می‌گوید مبالغ ثبت‌شده دست نمی‌خورند. بدون آن،
کاربری که واحد را عوض می‌کند و می‌بیند همه‌ی اعداد ده برابر شده‌اند
فکر می‌کند داده‌اش نابود شده — و آن ترس کاملاً منطقی است.

**۱۲۷ نقطه‌ی `formatToman`** مهاجرت شد. تابع قدیمی **حذف نشد**؛
ماژول تازه کنارش نشست با رفتار پیش‌فرض **بایت‌به‌بایت یکسان** (تستش
هر دو را مقایسه می‌کند). حذف یک‌باره یعنی ۱۲۷ نقطه‌ی شکست بالقوه در
یک کامیت.

### شخصی‌سازی صنفی

| صنف | واژه‌ی کالا | واحد پیش‌فرض | فیلدهای پنهان |
|---|---|---|---|
| کافه | آیتم منو | شمارشی | رنگ، سایز، فصل، جنس |
| سوپرمارکت | کالا | **وزنی** | رنگ، سایز، فصل، جنس |
| طلا | مصنوع | **وزنی** | فصل |
| پوشاک | کالا | شمارشی | — |

⚠️ فیلد پنهان‌شده مقدارش را **از دست نمی‌دهد** — فقط نمایش داده
نمی‌شود.

⚠️ فقط چیزهایی در جدول است که **واقعاً** اثر دارد. وسوسه‌ی این کار
آن است که برای هر صنف وعده‌ی چیزی بدهیم که نداریم.

### منوهای کشویی

رنگ، سایز، فصل، جنس، واحد — با ترکیب دو منبع: گزینه‌های خود کاربر
**اول**، بعد پیشنهادهای صنف.

🔴 **`datalist` نه `select`.** داده‌ی موجود این فیلدها متن آزاد است.
`select` سفت یعنی هر مقداری که در فهرست نیست از فرم ناپدید می‌شود و
با ذخیره‌ی ساده **پاک** می‌شود.

### کدهای پشتیبان

ده کد یک‌بارمصرف، با دانلود و چاپ. الفبا عمداً `0/O`، `1/I/l`،
`5/S`، `2/Z`، `8/B` ندارد — کاربر این را از **کاغذ** می‌خواند و یک
اشتباه خواندن یعنی سوختن یکی از پنج تلاش.

🔴 مصرف کد، فاکتور دومرحله‌ای را **حذف می‌کند** و کاربر با رمز وارد
می‌شود. چون سطح تضمین در توکن Supabase امضا شده و از بیرون قابل
ارتقا نیست. همان رفتاری که GitHub و Google دارند: کد پشتیبان یک در
اضطراری است، نه روش ورود دائمی.

---

## سه باگ که هرکدام را فقط یک ابزار گرفت

### ۱) 🔴 نقض قواعد hook — فقط بازرسی خودکار

مهاجرت ۳۰ فایل با اسکریپت انجام شد و **دو بار hook را جای اشتباه
گذاشت**:

- `formatMoney` در `src/shared/format` — تابع خالص است و از داخل شرط
  و حلقه صدا زده می‌شود
- `describe` در صفحه‌ی فعالیت‌ها — داخل `map` صدا زده می‌شود

**نه `tsc` گرفتشان** (از نظر نوع درست‌اند) و **نه `next build`**.

راه‌حل: `formatMoney` خالص ماند و کامپوننت `<Money>` hook گرفت — که
باعث شد هر جای برنامه که از `<Money>` استفاده می‌کند **خودکار** واحد
سازمان را بگیرد. `describe` پارامتر می‌گیرد.

آن بازرسی حالا یک **تست دائمی** است که همه‌ی فایل‌های `.tsx` را
می‌پیماید.

### ۲) 🔴 «تومان» سخت‌کد — فقط تست مرورگر

بعد از مهاجرت، واحد را روی ریال گذاشتم و سبد همچنان «تومان» گفت.
`formatToman` عوض شده بود ولی **خود واژه** در کلید تعویض
واحد/درصد و سرستون جدول مانده بود.

تست‌های رشته‌ای قبلی نگرفتند چون فقط دنبال `formatToman(` بودند.

سپس آزمون فراگیر روی ۸ صفحه، سه صفحه‌ی دیگر را هم لو داد: سرستون
جدول مخاطبین، برچسب کارت‌های KPI، و tooltip نمودارها.

### ۳) تست موجود یک اشتباه واقعی من را گرفت

پنج نسخه‌ی پیاپی را `important` علامت زده بودم — نسبت رسید به ۱۲ از
۳۵. تست `notifications` دقیقاً برای جلوگیری از همین انباشت نوشته شده
بود. دو مورد غیرمهم شد.

**بررسی کردم تست درست است یا کد — تست درست بود.**

---

## اندازه‌گیری‌ها

```
تست‌ها          ۱۵۵۷ / ۱۵۵۷ (۵۶ فایل) — ۶۹ تای آن این نوبت
tsc --noEmit    پاک
build           موفق (محلی + Vercel)
axe-core        صفر ایراد — دسکتاپ، تبلت، موبایل — محلی و زنده
خطای JS         صفر
راستی‌آزمایی    ۷ باگ عمدی برگردانده شد، هر ۷ تا توسط تست گرفته شد
```

**آزمون فراگیر روی سرور واقعی، واحد ریال:**

| صفحه | نتیجه |
|---|---|
| داشبورد · فروش · کالاها · مخاطبین · گزارش‌ها · چک‌ها · خرید · مالی | **هیچ «تومان» باقی نماند** ✅ |

**رفتار امنیتی روی دیتابیس واقعی** (تراکنش rollback شده):
استفاده‌ی دوباره رد ✅ · ساخت تازه قبلی‌ها را باطل کرد ✅ · ترجیحات
ادغام شد نه جایگزین ✅ · گزینه‌ی تکراری رد ✅

**داده‌ی واقعی دست‌نخورده:** `users 6 · sales 30 · products 383 ·
contacts 552 · movements 759` · واحد به `toman` برگشت

---

## صادقانه: چه چیزی نیست

| مورد | وضعیت |
|---|---|
| **چندارزی واقعی** (دلار، درهم) | ساخته نشد و عمدی است. نیاز به ستون ارز و نرخ روی هر سند، سند تسعیر، و حساب ارزی دارد. رقبا آن را محصول جدا می‌فروشند. |
| **ستون‌های CSV گزارش‌ها** | همچنان «(تومان)» ثابت است. فایل خروجی یک سند با شمای ثابت است، نه نمای زنده — تغییرش یعنی فایل‌های قدیمی و جدید ستون متفاوت دارند. |
| **`/shop/[slug]`** | کامپوننت سروری است و hook نمی‌پذیرد. صفحه‌ی عمومی فروشگاه هنوز تومانی است. |
| **بازنشانی 2FA توسط مدیر** | اگر کاربر گوشی **و** کدهای پشتیبان را با هم از دست بدهد، هنوز راه خودکاری نیست. |
| **اجبار 2FA برای مالک** | زیرساخت هست، سیاستش پیاده نشده. |
| **خروج خودکار پس از بی‌کاری** | طراحی شده، ساخته نشده. |
| **نگاشت واحد به کد مؤدیان** | ستون `code` آماده است ولی پر نمی‌شود. |

⚠️ **درباره‌ی صفحه‌ی فروشگاه عمومی:** چون کامپوننت سروری است، برای
احترام به واحد سازمان باید ترجیحات را در خود صفحه بخواند و به
کامپوننت‌ها پاس بدهد. کار کوچکی است ولی الگوی متفاوتی می‌خواهد.

---

## گام بعد پیشنهادی

۱. صفحه‌ی فروشگاه عمومی (تنها جای باقی‌مانده‌ی واحد پول)
۲. بازنشانی 2FA توسط مدیر — تنها حفره‌ی بازمانده‌ی بازیابی
۳. ادامه‌ی نقشه‌ی راه صنفی: تاریخ انقضا و سری ساخت
