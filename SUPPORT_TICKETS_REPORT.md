# تیکت پشتیبانی — گزارش کار

**تاریخ:** ۱۴ مرداد ۱۴۰۵ · **کامیت:** `63609e4` · **مهاجرت:** `0036`

---

## چه چیزی ساخته شد

جدول‌های `support_tickets` و `support_messages` در مهاجرت ۰۰۲۸ ساخته شده بودند
ولی هیچ‌وقت رابط کاربری نگرفتند — یعنی روی سایت زنده وجود داشتند و صفر رکورد
داشتند. حالا هر دو سمتش کامل شد.

### سمت مشتری — `/support`

- ثبت تیکت با **دسته** (مشکل فنی، صورتحساب، درخواست قابلیت، داده و گزارش، سایر)
  و **فوریت**
- گفتگوی دوطرفه با حباب پیام؛ پیام تیم پشتیبانی رنگ متمایز دارد
- نشان **«پاسخ جدید»** روی تیکتی که جواب خوانده‌نشده دارد
- بستن تیکت («مشکلم حل شد») و بازکردن دوباره
- هر ۲۰ ثانیه به‌روزرسانی خودکار وقتی گفتگو باز است

لینک «پشتیبانی» **در فهرست اصلی** است، نه داخل تنظیمات. کاربری که مشکل دارد
آن را در منوی اصلی می‌گردد؛ پنهان‌کردنش پشت یک آکاردئون یعنی به‌جای تیکت،
تماس تلفنی می‌گیرد. همچنین **هیچ مجوز سازمانی نمی‌خواهد** — صندوق‌دار هم باید
بتواند بگوید «صفحه‌ی فروش باز نمی‌شود».

### سمت پشتیبانی — `/admin/tickets`

- صف تیکت‌ها با فیلتر وضعیت؛ پیش‌فرض **«در جریان»** است نه «همه»، چون صفحه‌ای
  که با تیکت‌های بسته‌ی سه ماه پیش شروع شود کار روزمره را کند می‌کند
- شمارنده‌ها: کل، نیازمند پاسخ، منتظر مشتری، فوریِ باز، و **میانگین زمان اولین پاسخ**
- زمینه‌ی کسب‌وکار در بالای گفتگو (نام کسب‌وکار، فرستنده، وضعیت اولین پاسخ)
- پاسخ، تغییر وضعیت و اولویت، و **«من پیگیری می‌کنم»**

### چند تصمیم و دلیلشان

**چرا «میانگین زمان اولین پاسخ» و نه «میانگین زمان بسته‌شدن»؟**
دومی عدد قشنگ‌تری است ولی کسی که ۶ ساعت منتظر *اولین* جواب مانده، با تیکتی که
روز سوم بسته شد راضی نمی‌شود. ضمناً فقط تیکت‌هایی که پاسخ گرفته‌اند در میانگین
می‌آیند؛ اگر تیکت‌های بی‌پاسخ را با صفر وارد می‌کردیم، میانگین **بهتر از واقعیت**
نشان داده می‌شد — برعکس چیزی که این عدد باید هشدار بدهد.

**چرا برچسب وضعیت برای مشتری و ادمین فرق دارد؟**
برای ادمین `pending` یعنی «جواب دادم، منتظر مشتری‌ام». اگر همین کلمه را به
مشتری نشان دهیم، «در انتظار» را «هنوز کسی نگاه نکرده» می‌فهمد و تیکت تکراری
می‌زند. پس مشتری «پاسخ داده شد» می‌بیند و ادمین «منتظر مشتری».

**چرا دو مجوز جدا (`tickets.view` و `tickets.reply`)؟**
یک ادمین مالی ممکن است لازم باشد تیکت را بخواند تا زمینه‌ی یک اختلاف صورتحساب
را بفهمد، بدون اینکه از طرف تیم پشتیبانی حرف بزند. پاسخ به نام پلتفرم ثبت
می‌شود و مشتری آن را رسمی تلقی می‌کند.

**چرا دو ستون تاریخ برای «خوانده‌نشده» و نه یک boolean؟**
boolean باید در دو جا (موقع ارسال پیام و موقع باز کردن) هماهنگ بماند و هر
ناهماهنگی یعنی نشانگر دروغین. با مقایسه‌ی دو زمان حقیقت همیشه یکتاست:
`unread ⇔ last_message_at > read_at`.

---

## 🔴 شش باگ که پیدا و رفع شد

چهارتای اینها **سراسری** بودند، نه مخصوص صفحه‌ی تیکت.

### ۱. مجوز `tickets.reply` از ماتریس افتاده بود

مهاجرت ۰۰۲۸ این مجوز را تعریف کرد. مهاجرت‌های ۰۰۳۱ و ۰۰۳۳ کل تابع
`platform_admin_can` را بازنویسی کردند و این سطر در بازنویسی جا ماند، پس به
`else false` می‌رسید.

اندازه‌گیری روی دیتابیس زنده، پیش از اصلاح:

```
platform_admin_can('tickets.reply', <super_admin>)  →  false
platform_admin_can('orgs.view',     <super_admin>)  →  true
```

یعنی اگر رابط تیکت را بدون این اصلاح می‌ساختیم، **حتی مدیر ارشد هم ۴۰۳
می‌گرفت** — دقیقاً همان اتفاقی که در ۰۰۳۱ برای `users.view` و `impersonate` و
`announcements.manage` افتاد و آن سه قابلیت روی سایت زنده مرده بودند.

**علت ساختاری:** تستی که باید این را می‌گرفت، فایل `0033` را با نام ثابت
می‌خواند. مهاجرت جدید همان تابع را عوض می‌کرد و تست همچنان سبز می‌ماند.
حالا تست همیشه **آخرین** مهاجرتی را پیدا می‌کند که ماتریس را بازنویسی می‌کند،
پس هر بازنویسی تازه خودبه‌خود زیر آزمون می‌رود.

### ۲. `is_staff` قابل جعل بود

policy موجود فقط عضویت در سازمان را چک می‌کرد و مقدار `is_staff` مستقیم از
بدنه‌ی درخواست می‌آمد. یک مشتری معمولی می‌توانست با یک درخواست ساده به
PostgREST پیامی با `is_staff=true` بسازد و در نخ گفتگو **به‌جای تیم پشتیبانی**
حرف بزند — با همان حباب رنگی و همان اعتبار.

حالا مقدار این ستون هرگز از ورودی خوانده نمی‌شود؛ یک تریگر آن را از روی
`author_id` محاسبه می‌کند. همچنین `author_id` و `created_by` به کاربر واردشده
قفل شدند تا کسی نتواند به نام دیگری بنویسد.

### ۳. هیچ‌کدام از ۷۷ فیلد پروژه برچسب متصل نداشتند

کامپوننت `Field` عنوان را در `<span>` می‌گذاشت، نه `<label>`. صفحه‌خوان برای
هر ورودی فقط «edit text» می‌گفت. این روی **۷۷ فیلد در ۹ فایل** بود — از فرم
کالا و شخص تا پرداخت فاکتور.

```
axe: label        (critical) → textarea
axe: select-name  (critical) × 2
```

حالا `<label htmlFor>` با شناسه‌ی `useId` استفاده می‌شود و متن راهنما و خطا هم
با `aria-describedby` به ورودی وصل می‌شوند — بدون آن، صفحه‌خوان متن خطا را
هرگز اعلام نمی‌کرد و کاربر نمی‌فهمید چرا فرم ثبت نشد.

`htmlFor` انتخاب شد و نه `<label>` دورِ ورودی، چون دکمه‌ی راهنما («؟») داخل
همین برچسب است و با پیچیدن label، کلیک روی آن به ورودی منتقل می‌شد.

### ۴. دو سایدبار هم‌نام روی همه‌ی صفحه‌ها

`Sidebar` دو بار در DOM رندر می‌شود (یکی دسکتاپ، یکی موبایل) و هر دو یک
`<nav>` بی‌نام داشتند. نتیجه `landmark-unique` روی **هر صفحه‌ای** بود:
کاربر صفحه‌خوان دو «ناوبری اصلی» می‌دید و نمی‌دانست کدام واقعی است.

تأیید شد که سراسری است — روی `/dashboard`، `/products`، `/contacts`،
`/settings/general`، `/sales` و `/support` همگی همین یک ایراد گزارش شد.

### ۵. کنتراست و اعلام‌نشدن پیام‌های سیستم (Toast)

سه ایراد در یک کامپوننت، هر سه سراسری:

```
Element has insufficient color contrast of 2.46
(foreground #10b77f, background #edfdf5) — expected 4.5:1
```

`text-success` رنگِ **پُرکردن** است، نه رنگ متن. توکن `--success-on-soft`
دقیقاً برای متن روی پس‌زمینه‌ی ملایم تعریف شده بود ولی استفاده نشده بود.
(همین اشتباه برای `warning` هم بود.) ضمناً یک `opacity-85` روی توضیح، توکن را
باز هم پایین‌تر می‌برد.

مهم‌تر: Toast هیچ `aria-live` یا `role` نداشت. یعنی پیام «تیکت ثبت شد» فقط
*دیده* می‌شد؛ کاربر نابینا هیچ بازخوردی از موفقیت عملیات نمی‌گرفت. حالا خطا
با `role="alert"` فوراً و بقیه با `role="status"` مؤدبانه اعلام می‌شوند.

### ۶. `void` روی کوئری Supabase — کوئری اصلاً اجرا نمی‌شد

برای علامت‌زدن «خوانده‌شده» نوشته بودم:

```ts
void svc.from("support_tickets").update({ customer_read_at: ... })
```

با این تصور که «عملیات جانبی نباید پاسخ را کند کند». ولی سازنده‌ی کوئری
Supabase یک **thenable تنبل** است: تا `await` نشود هیچ درخواستی به شبکه
نمی‌رود. `void` فقط مقدار را دور می‌اندازد و اجرا هرگز شروع نمی‌شود.

اندازه‌گیری روی سرور واقعی:

```
قبل از باز کردن گفتگو:  customer_read_at = null
GET /api/support/tickets/{id} → 200
بعد از GET:              customer_read_at = null   ← تغییر نکرد
```

یعنی نشان «پاسخ جدید» **برای همیشه** روی تیکت می‌ماند. نه `tsc`، نه
`next build`، نه هیچ تست واحدی این را نگرفت — فقط خواندن دیتابیس بعد از یک
درخواست واقعی.

---

## چیزی که فقط `next build` گرفت

```
Type error: Route "app/api/admin/tickets/route.ts" does not match the
required types of a Next.js Route.
  "mapTicket" is not a valid Route export field.
```

`npx tsc --noEmit` تمیز رد شده بود. فایل `route.ts` فقط اجازه‌ی export نام‌های
شناخته‌شده (GET/POST/dynamic/…) را دارد. تابع مشترک به `lib/support/tickets.ts`
منتقل شد. یادآوری اینکه type-check تنها، اثبات درستی نیست.

---

## تست واقعی روی production build

سناریوی کامل با Playwright روی سرور واقعی و داده‌ی واقعی:

| مرحله | نتیجه |
|---|---|
| ساخت تیکت توسط مشتری (`test`) | ✅ دسته، فوریت و حفظ خط جدید درست |
| **IDOR — خواندن تیکت سازمان دیگر** | ✅ `HTTP 404` |
| **IDOR — نوشتن در تیکت سازمان دیگر** | ✅ `HTTP 404` |
| دیدن تیکت در صف ادمین + نشان خوانده‌نشده | ✅ |
| پاسخ ادمین → وضعیت «منتظر مشتری» | ✅ |
| واگذاری «من پیگیری می‌کنم» | ✅ |
| مشتری نشان «پاسخ جدید» می‌بیند | ✅ |
| بستن تیکت توسط مشتری | ✅ |
| پاک‌شدن نشانگر پس از خواندن | ✅ (بعد از رفع باگ ۶) |
| axe روی دسکتاپ / تبلت / موبایل | ✅ صفر ایراد |
| سرریز افقی در سه اندازه | ✅ صفر |

تیکت ناموجود و تیکت متعلق به سازمان دیگر **پاسخ یکسان** می‌گیرند؛ تفاوت پاسخ
وجود تیکت را لو می‌دهد.

### وضعیت داده‌ی شما پس از تست

همه‌ی تیکت‌های آزمایشی حذف شدند و شمارش‌ها با قبل از شروع کار یکی است:

```
تیکت‌ها ۰ · پیام‌ها ۰ · گزارش ممیزی ۴ (بدون تغییر)
کاربران ۵ · سازمان‌ها ۳ · ادمین پلتفرم ۱ · عضویت‌ها ۷
اشخاص ۵۴۸ · کالاها ۳۸۲ · فاکتورها ۲۱ · گردش انبار ۷۳۱
```

---

## وضعیت نهایی

```
npx tsc --noEmit   → پاک
npx vitest run     → ۵۳۴/۵۳۴ (۲۷ فایل، ۵۹ تست جدید)
npm run build      → Compiled successfully
axe-core           → صفر ایراد در همه‌ی صفحات آزموده‌شده
```

تست‌های جدید **واقعاً** باگ را می‌گیرند — با برگرداندن عمدی `await` به `void`،
دو تست شکستند و پس از بازگردانی دوباره سبز شدند. (چند ادعای اولیه روی
*توضیح فارسی* گیر می‌کردند نه روی کد؛ حالا کد پیش از بررسی از توضیحات پاک
می‌شود، چون تست غلط بدتر از نبود تست است.)

---

## آنچه هنوز نیست

- **اعلان ایمیلی تیکت** — نیاز به SMTP اختصاصی دارد که به دامنه‌ی `tarazoo.ir`
  وابسته است (هنوز گرفته نشده)
- **پیوست فایل** — اسکرین‌شات در پشتیبانی خیلی کمک می‌کند؛ نیاز به
  Supabase Storage و سیاست حجم دارد
- **پاسخ آماده** — وقتی معنا پیدا می‌کند که چند تیکت مشابه دیده باشیم
