# گزارش — نقش سفارشی ادمین

نسخه `26.0804` · کامیت `e77aa1e` · ۱۳ مرداد ۱۴۰۵

---

## مسئله

چهار نقش ثابت (`super_admin` / `support` / `finance` / `readonly`) در خودِ کد
SQL هاردکد شده بودند. برای دادن **یک مجوز اضافه** به کسی، تنها راه ارتقای او
به «مدیر ارشد» بود — یعنی دادن **همه‌ی اختیارات**، از جمله بازنشانی رمز
کاربران و کنترل بقیه‌ی ادمین‌ها.

این دقیقاً نقض اصل کمترین امتیاز است.

---

## چه ساختم

نقش پنجمی به نام **«سفارشی»** که مجوزهایش را تک‌تک انتخاب می‌کنید.

### ۱۳ مجوز، دسته‌بندی‌شده با سطح خطر

| دسته | مجوزها |
|---|---|
| **کسب‌وکارها** | مشاهده · تأیید و رد · تعلیق |
| **اشتراک** | تمدید دوره آزمایشی · تغییر پلن |
| **کاربران** | مشاهده · بازنشانی رمز 🔴 · ورود به حساب 🔴 |
| **داده مشتری** | مشاهده فاکتور 🔴 · ویرایش فاکتور 🔴 |
| **نظارت** | گزارش ممیزی · مدیریت اعلان‌ها · مدیریت ادمین‌ها 🔴 |

هر مجوز **توضیح فارسی** و **سطح خطر** دارد. اگر مجوز پرخطری انتخاب کنید،
پیش از ذخیره هشدار می‌گیرید — چون «بازنشانی رمز» با «مشاهده» زمین تا آسمان
فرق دارد.

---

## 🔴 قید اصلی: حساب شما نباید تغییر کند

تنها ادمین فعلی شما هستید و باید بعد از این مهاجرت **دقیقاً همان دسترسی‌ها**
را داشته باشید. این را اندازه گرفتم، نه فرض:

```
قبل از مهاجرت : ۱۳ از ۱۳ مجوز
بعد از مهاجرت : ۱۳ از ۱۳ مجوز
تفاوت         : هیچ
```

ماتریس ثابت کاملاً دست‌نخورده ماند و شاخه‌ی سفارشی فقط **اضافه** شد.

---

## تصمیم‌های طراحی

**نقش سفارشی هیچ مجوز پیش‌فرضی ندارد** — حتی «مشاهده کسب‌وکارها» که در
ماتریس ثابت برای همه‌ی نقش‌ها `true` است. اگر مشاهده را رایگان می‌دادم،
ساختن نقشی که *نتواند* کسب‌وکارها را ببیند ممکن نبود.

تأیید شد: نقش سفارشی با دو مجوز، برای `audit.view` مقدار `false` داد — با
اینکه آن مجوز در ماتریس ثابت برای همه باز است.

**اعتبارسنجی در دیتابیس است، نه در API.** یک غلط املایی مثل `users.veiw`
وگرنه بی‌صدا ذخیره می‌شد و ادمین بدون هیچ پیام خطایی دسترسی نمی‌گرفت —
دقیقاً همان نوع خرابی که در مهاجرت ۰۰۳۱ سه روت را از کار انداخته بود.

**تغییر نقش، آرایه را پاک می‌کند.** اگر کسی از «سفارشی» به «پشتیبانی» برود،
فهرست قدیمی نباید بماند و بعداً دوباره فعال شود.

---

## دو گارد قفل‌شدگی

**۱. ادمین نمی‌تواند نقش خودش را عوض کند.** دو خطر: تنزل تصادفی که خودش را
بیرون می‌اندازد، و ارتقای بی‌نظارت. تغییر نقش یک ادمین باید همیشه کار
*شخص دیگری* باشد.

**۲. آخرین مدیر ارشد حذف نمی‌شود.** بدون این، حذفش یعنی هیچ‌کس دیگر نمی‌تواند
ادمین اضافه کند و پلتفرم برای همیشه بدون مدیر می‌ماند — تنها راه بازگشت،
دست‌کاری مستقیم دیتابیس.

---

## تستی که جلوی تکرار باگ ۰۰۳۱ را می‌گیرد

یادتان هست سه مجوز که روت‌ها می‌خواستند اصلاً تعریف نشده بودند و کل صفحه‌ی
کاربران را ۴۰۳ کرده بودند؟

حالا یک تست خودکار **همه‌ی روت‌های `/api/admin` را می‌گردد**، هر مجوزی که
می‌خواهند استخراج می‌کند، و بررسی می‌کند که در **هر دو** جا تعریف شده باشد:
ماتریس و کاتالوگ. آن دسته باگ دیگر نمی‌تواند بی‌صدا منتشر شود.

---

## تست واقعی

```
npx tsc --noEmit  → تمیز
npx vitest run    → ۴۴۴/۴۴۴ (۲۴ فایل)
npm run build     → Compiled successfully
```

| بررسی | نتیجه |
|---|---|
| مجوزهای super_admin قبل و بعد یکسان | ✅ |
| ساخت نقش سفارشی از رابط کاربری | ✅ |
| **کاربر سفارشی: `users.view` → ۲۰۰ ولی `audit.view` → ۴۰۳** | ✅ |
| تغییر نقش خود ادمین رد شد | ✅ |
| حذف خود ادمین رد شد | ✅ |
| مجوز با غلط املایی رد شد | ✅ |
| نقش سفارشی بدون مجوز رد شد | ✅ |
| ثبت `admin.grant` / `role_change` / `revoke` در ممیزی | ✅ |
| axe در ۳۹۰ و ۱۴۴۰، فهرست و ویرایشگر | صفر تخلف |

مهم‌ترین تست بخش سوم بود: یک کاربر واقعی با نقش سفارشی ساختم و **از حساب
خودش** بررسی کردم که فقط به مجوزهای داده‌شده دسترسی دارد.

بعد از تست، ادمین آزمایشی حذف شد و تعداد به ۱ برگشت.

```
users 5 · orgs 3 · admins 1 · contacts 548 · products 382 · sales 21
```

---

## وضعیت فهرست تحقیق پنل ادمین

| مورد | وضعیت |
|---|---|
| تعلیق حساب | ✅ از قبل |
| تمدید تریال | ✅ از قبل |
| جعل هویت با لاگ | ✅ از قبل |
| گزارش ممیزی | ✅ از قبل |
| بازنشانی رمز کاربر | ✅ انجام شد |
| تغییر رمز توسط کاربر | ✅ انجام شد |
| اعلان زنگوله | ✅ انجام شد |
| قفل/کندسازی ورود | ✅ انجام شد |
| مدیریت نشست‌ها | ✅ انجام شد |
| **نقش سفارشی** | ✅ **انجام شد** |
| اشتراک و پلن | ⏸ منتظر تصمیم شما |
| تایمر اشتراک | ⏸ وابسته به بالا |
| صادرات داده کسب‌وکار | ⬜ باقی |
| حذف نرم و بازیابی سازمان | ⬜ باقی |
| آمار مصرف هر کسب‌وکار | ⬜ باقی |
| رابط تیکت پشتیبانی | ⬜ باقی |
| پرچم قابلیت | ⬜ باقی |
| داشبورد درآمد | ⏸ وابسته به اشتراک |

---

## مرحله‌ی بعد

از موارد باقی‌مانده، پیشنهاد من به ترتیب:

1. **آمار مصرف هر کسب‌وکار** — تعداد فاکتور، کالا، کاربر. هم برای شناختن
   کسب‌وکارهای فعال از رهاشده، هم پیش‌نیاز سقف پلن.
2. **صادرات داده** — برای پشتیبانی («فاکتورهایم را می‌خواهم»).
3. **رابط تیکت پشتیبانی** — جدول‌ها و RLS از قبل ساخته شده‌اند.

هر وقت درباره‌ی پلن‌های اشتراک تصمیم گرفتید، آن بخش هم آماده‌ی کار است.
