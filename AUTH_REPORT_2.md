# سیستم ورود — گام دوم و سوم

نسخه‌ی زنده: `26.0819` · `1b94947` · ۲۸ مرداد ۱۴۰۵

ادامه‌ی `AUTH_ARCHITECTURE.md`. گام اول (بازیابی رمز) در
`AUTH_REPORT.md` است.

---

## گام ۲ — صفحه‌ی سابقه‌ی ورود

### مشکل

نسخه‌ی قبل رویدادهای ورود را در `login_events` ثبت می‌کرد ولی
**هیچ جایی برای دیدنشان نبود**. داده بود، رابط نبود — یعنی عملاً
بی‌فایده. بدون آن، کاربر هرگز نمی‌فهمد کسی شب گذشته با رمزش تلاش
کرده وارد شود.

### تصمیم‌ها

**خواندن با کلاینت کاربر، نه `service_role`.**
تابع `my_login_history` با `auth.uid()` فیلتر می‌کند؛ با کلید سرویس
آن تهی است و تابع چیزی برنمی‌گرداند. این خودش یک لایه‌ی محافظت است:
حتی اگر در این روت اشتباه کنیم، کاربر نمی‌تواند سابقه‌ی دیگری را
ببیند.

**فقط شکست‌ها هشدار می‌گیرند.**
🔴 «ورود موفق از دستگاه جدید» عمداً هشدار **نیست**. کاربر روی گوشی و
لپ‌تاپ و تبلت وارد می‌شود؛ اگر هر بار علامت قرمز ببیند، خیلی زود یاد
می‌گیرد نادیده‌اش بگیرد — و آن‌وقت هشدار واقعی هم گم می‌شود.

**شمارش فقط ۲۴ ساعت اخیر.**
کاربری که سه ماه پیش رمزش را اشتباه زده مهم نیست. با شمردن کل
تاریخچه، عدد همیشه بزرگ می‌ماند و بی‌معنا می‌شود.

### 🔴 باگی که فقط اسکرین‌شات گرفت

فهرست «دستگاه‌های واردشده» نشانی را **کامل** چاپ می‌کرد:
`31.171.101.138`. کارت سابقه از همان اول می‌پوشاند و آن کارتِ
قدیمی‌تر جا مانده بود. همچنین الگوی ` · ${توکن}` داخل یک رشته داشت —
همان خانواده‌باگ RTL. حالا هر دو از یک تابع پوشاندن استفاده می‌کنند.

⚠️ **در DOM هم خام بود، ولی هیچ تستی این را نمی‌پرسید.**

---

## گام ۳ — ورود دومرحله‌ای (TOTP)

### اول سنجیدم، بعد ساختم

پلن رایگان قبلاً دو قابلیت را بسته بود (`sessions_timebox` و هوک
تأیید رمز)، پس به تنظیمات اعتماد نکردم و **روی پروژه‌ی زنده آزمایش
کردم**:

```
POST /auth/v1/factors → QR + secret برگشت  ✅ کار می‌کند
```

### 🔴 کشف تعیین‌کننده

سه اندازه‌گیری پشت سر هم:

```
۱. ورود بدون MFA                     → aal1
۲. فعال‌سازی و تأیید TOTP            → aal2
۳. ورود تازه فقط با رمز، با MFA فعال → موفق می‌شود! aal1
۴. خواندن جدول products با همان aal1 → مجاز است!
```

**یعنی Supabase سطح را فقط گزارش می‌کند، اعمال نمی‌کند.** بدون گارد
صریح، فعال‌کردن ورود دومرحله‌ای صرفاً تزئین بود و کاربری که آن را
روشن می‌کرد دقیقاً به اندازه‌ی قبل محافظت داشت — یعنی هیچ.

### گارد کجا نشست و چرا

**در middleware.** تنها نقطه‌ای که همه‌ی مسیرها از آن عبور می‌کنند.
هر راه‌حل صفحه‌به‌صفحه یعنی اولین صفحه‌ای که یادمان برود، یک در باز
است.

سه استثنا لازم بود:
- `/mfa` — وگرنه حلقه‌ی بی‌نهایت
- `/api/auth/mfa-event` — کاربر `aal1` باید بتواند شکست را ثبت کند
- مسیرهای عمومی

مقصد اصلی در پارامتر `next` نگه داشته می‌شود تا کاربر پس از تأیید به
همان صفحه‌ای برگردد که می‌خواست.

### تصمیم‌های ریز که مهم بودند

| تصمیم | چرا |
|---|---|
| **راه خروج در صفحه‌ی تأیید** | کسی که گوشی‌اش را گم کرده وگرنه در صفحه‌ای گیر می‌کند که نه می‌تواند ردش کند و نه ترکش — حتی نمی‌تواند با حساب دیگری وارد شود |
| **پاک‌کردن فاکتور نیمه‌کاره** | بستن پنجره وسط کار، فاکتوری می‌گذارد که در رابط دیده نمی‌شود ولی خطای «already exists» می‌دهد — بن‌بست بدون خروج |
| **نرمال‌سازی ارقام فارسی** | کد تایپ‌شده با کیبورد فارسی **درست** است؛ ردش یعنی کاربر فکر می‌کند ساعت گوشی‌اش خراب است و آخرش از حسابش قفل می‌شود |
| **راهنمای اختلاف ساعت** | شایع‌ترین علت «کد کار نمی‌کند» ساعت اشتباه است نه کد اشتباه |
| **ثبت شکست مرحله‌ی دوم** | تأیید سمت کلاینت است، پس سرور از حمله خبردار نمی‌شود مگر خودمان بگوییم |
| **فاکتور `unverified` = غیرفعال** | وگرنه کاربری که وسط راه بست، فکر می‌کند محافظت دارد در حالی که ندارد |
| **TOTP نه پیامک** | سرویس پیامکی وصل نیست؛ SIM swap حمله‌ی واقعی است؛ TOTP آفلاین کار می‌کند |

---

## اندازه‌گیری‌ها

```
تست‌ها          ۱۴۸۸ / ۱۴۸۸ (۵۴ فایل) — ۵۰ تای آن این دو گام
tsc --noEmit    پاک
build           موفق (محلی + Vercel)
axe-core        صفر ایراد — دسکتاپ، تبلت، موبایل — محلی و زنده
خطای JS         صفر
راستی‌آزمایی    ۱۰ باگ عمدی برگردانده شد، هر ۱۰ تا توسط تست گرفته شد
```

### آزمون انتها به انتها با TOTP واقعی

روی حساب تست، با کد TOTP محاسبه‌شده از secret واقعی — **هم محلی هم
روی سایت زنده**، دسکتاپ و موبایل:

| مرحله | نتیجه |
|---|---|
| ورود با رمز درست | → `/mfa?next=/dashboard` ✅ **نه داشبورد** |
| کد اشتباه | رد شد + پیام فارسی ✅ |
| کد درست | → `/dashboard` ✅ |
| راه خروج | موجود ✅ |

**پاک‌سازی تأیید شد:** `auth.mfa_factors` صفر ردیف.
**داده‌ی واقعی دست‌نخورده:** `users 6 · sales 30 · products 383 ·
contacts 552 · movements 759`

---

## صادقانه: چه چیزی هنوز نیست

| مورد | وضعیت |
|---|---|
| **کدهای پشتیبان** | 🔴 **ساخته نشد.** کاربری که گوشی‌اش را گم کند، فقط می‌تواند از مدیر کد بازیابی رمز بگیرد — که 2FA را غیرفعال نمی‌کند. **مهم‌ترین شکاف باقی‌مانده.** |
| **اجبار 2FA برای مالک** | زیرساختش هست، سیاستش پیاده نشده. الان کاملاً اختیاری است. |
| **بازنشانی 2FA توسط مدیر** | اگر کاربر گوشی و دسترسی را با هم از دست بدهد، الان راه خودکاری نیست. |
| **خروج خودکار پس از بی‌کاری** | طراحی شده، ساخته نشده. |
| **بازیابی با پیامک** | سرویس وصل نیست. |
| **SMTP اختصاصی** | سقف ۲ ایمیل در ساعت باقی است. |

⚠️ **هشدار عملیاتی:** تا وقتی کدهای پشتیبان ساخته نشده، به کاربران
بگویید secret را هنگام فعال‌سازی جایی امن نگه دارند. اگر گوشی گم شود
و secret هم نباشد، بازکردن حساب نیاز به دخالت دستی در دیتابیس دارد.

---

## گام بعد پیشنهادی

۱. **کدهای پشتیبان** — تنها چیزی که 2FA را از یک ریسک به یک قابلیت
   کامل تبدیل می‌کند
۲. بازنشانی 2FA توسط مدیر (با همان الگوی کد یک‌بارمصرف)
۳. خروج خودکار پس از بی‌کاری
