# گزارش: بازچینش تنظیمات و کاربران

**تاریخ:** ۱۹ مرداد ۱۴۰۵ · **مهاجرت:** `0040_categories_per_org.sql` · **نسخه:** ۲۶.۰۸۱۰

---

## درخواست شما

> «بخش تنظیمات و کاربران و دسترسی ها یکم شلوغ و پلوغه میخوام مرتب تر باشه»

رفتم سراغ مرتب‌سازی، ولی وسط کار **یک باگ کاملاً خاموش** پیدا شد که مهم‌تر از
شلوغی بود. اول آن را می‌گویم.

---

## 🔴 باگ اول: «دسته‌بندی کالا» کاملاً از کار افتاده بود

کارت «دسته‌بندی کالا» در `/settings/catalog` می‌گفت **«موردی ثبت نشده.»** در
حالی که ۱۵ ردیف در دیتابیس بود.

### علت

جدول `categories` برخلاف `brands` و `expense_categories` **سه ستون کم داشت**:

| جدول | `org_id` | `is_active` | `branch_id` |
|---|---|---|---|
| `brands` | ✅ | ✅ | ✅ |
| `expense_categories` | ✅ | ✅ | ✅ |
| `categories` | ❌ | ❌ | ❌ |

ولی کد برای هر سه، دقیقاً یک کوئری می‌زد. اندازه‌گیری مستقیم روی دیتابیس زنده:

```
GET  categories?is_active=eq.true
   → 42703: column categories.is_active does not exist

POST categories {org_id, branch_id, name}
   → PGRST204: Could not find the 'branch_id' column
```

یعنی **خواندن، افزودن، ویرایش و حذف — هر چهار عمل خراب بودند.**

### چرا هیچ‌وقت متوجه نشدید

کد نتیجه‌ی عملیات را **اصلاً بررسی نمی‌کرد**:

```ts
await supabase.from(table).insert({ ... });   // ← خطا خوانده نمی‌شود
```

کلاینت Supabase برای خطای دیتابیس استثنا پرتاب نمی‌کند؛ خطا را در فیلد `error`
برمی‌گرداند. پس دکمه زده می‌شد، هیچ اتفاقی نمی‌افتاد، و **هیچ پیامی هم نمایش
داده نمی‌شد.** تنها نشانه، یک خطای ۴۰۰ در کنسول مرورگر بود.

این الگو **پنج بار** در همان فایل تکرار شده بود.

---

## 🔴 باگ دوم: نشت داده بین کسب‌وکارها

policy جدول `categories` این بود:

```sql
create policy "Public read categories" for select using (true);
```

`true` یعنی **هر کاربری دسته‌بندی‌های همه‌ی کسب‌وکارها را می‌دید.** مزون پوشاک
دسته‌های رستوران را می‌دید و برعکس. ضمناً هیچ policy‌ای برای نوشتن نبود.

اثبات پس از اصلاح:

```
کاربر ناشناس (anon) پیش از مهاجرت → ۱۵ ردیف
کاربر ناشناس پس از مهاجرت        → ۰ ردیف   ✅
bahram (عضو مزون پوشاک)          → فقط ۳ دسته‌ی مزون، نه بقیه  ✅
```

### درباره‌ی ۱۵ دسته‌ی قدیمی

همه از پروژه‌ی قدیمی موزیک بودند: «جاز»، «راک»، «پاپ»، «بی‌کلام»… و
**هیچ کالایی به آن‌ها وصل نبود** (صفر محصول با `category_id` غیرتهی).

**هیچ‌کدام را حذف نکردم.** با `org_id = NULL` سر جایشان ماندند و policy جدید
از دید همه پنهانشان می‌کند. اگر روزی معلوم شد جایی لازم بوده‌اند، داده هست.
حذف تصمیمی است که پس گرفته نمی‌شود.

### دسته‌های پیش‌فرض

چون پس از اصلاح policy هر کسب‌وکار صفر دسته داشت، بر اساس صنف چند دسته ساخته
می‌شود — و برای ثبت‌نام‌های آینده هم داخل `bootstrap_org` وصل شد:

```
پوشاک   → پیراهن، شومیز، شلوار، مانتو، کیف و کفش، اکسسوری
کافه    → پیش‌غذا، غذای اصلی، نوشیدنی، دسر
موبایل  → گوشی موبایل، لوازم جانبی، قاب و گلس، شارژر و کابل
…
```

⚠️ نسخه‌ی اول این بخش شناسه‌های حدسی (`clothing`, `restaurant`) داشت که در
`lib/business-types.ts` اصلاً وجود ندارند — همه‌ی سازمان‌ها به شاخه‌ی پیش‌فرض
می‌افتادند. حالا یک تست، این فهرست را با فهرست واقعی مقایسه می‌کند.

---

## دو باگ کوچک‌تر

**حذف بدون هیچ تأییدی.** یک کلیک روی سطل زباله و دسته/برند/حساب می‌رفت. حالا
پنجره‌ی تأیید می‌آید و متنش **صادق** است: می‌گوید «پنهان می‌شود» نه «حذف
می‌شود»، چون عملیات واقعی `is_active=false` است و کالاهای وابسته دست‌نخورده
می‌مانند.

**دکمه‌های ویرایش و حذف روی موبایل نامرئی بودند.**
`opacity-0 group-hover:opacity-100` — روی لمسی hover وجود ندارد.

---

## مرتب‌سازی

### پیش از این

`app/(app)/settings/page.tsx` = **۶۴۳ خط** که همه‌چیز در آن بود: داشبورد، تم
رنگی، سه فهرست، حساب‌های بانکی، درخت ۵۱ گزینه‌ای مجوز و دو مودال.
`/settings/users`، `/settings/catalog` و `/settings/accounts` هرکدام **فقط دو
خط** بودند که همین فایل را با یک prop صدا می‌زدند.

### حالا

```
settings/page.tsx        ۶۴۳ → ۱۶۳ خط  (فقط کارت‌های ورود، در ۴ گروه)
settings/users/page.tsx    ۲ → صفحه‌ی واقعی
settings/catalog/page.tsx  ۲ → صفحه‌ی واقعی
settings/accounts/page.tsx ۲ → صفحه‌ی واقعی

components/shared/managed-list.tsx        فهرست قابل مدیریت (مشترک)
components/shared/accounts-manager.tsx    صندوق و بانک
components/shared/users-access.tsx        کاربران
components/shared/permission-tree-editor.tsx
+ دو فایل .helpers.ts برای منطق خالصِ قابل تست
```

### «کاربران و دسترسی‌ها» — ریشه‌ی شلوغی

درخت مجوز با **۵۱ گزینه در ۹ گروه** برای *هر کاربر* کاملاً باز رندر می‌شد.
با ۴ کاربر یعنی حدود **۲۰۰ چک‌باکس روی یک صفحه**.

اندازه‌گیری واقعی در مرورگر:

| | چک‌باکس دیده‌شده |
|---|---|
| پیش از این | ~۲۰۰ |
| حالا (حالت عادی) | **۰** |
| حالا (یک کاربر باز) | **۹** (فقط سرگروه‌ها) |

هر کاربر یک ردیف جمع‌شده است؛ درخت تک‌بازشو است و کنار هر گروه می‌نویسد چند
مورد از چند مورد انتخاب شده.

### جابه‌جایی‌های منطقی

- **تم رنگی** در *دو* صفحه تکرار شده بود (`general` و `catalog`) — دو نسخه‌ی
  جدا از یک کامپوننت. حالا فقط در «کسب‌وکار و ظاهر».
- **دسته‌بندی هزینه** از «کاتالوگ» به «مالی و حساب‌ها» رفت؛ هزینه مفهومی مالی
  است نه کالایی.
- **حساب کاربری** به نوار تب اضافه شد — تغییر رمز پرکاربرد است ولی از نوار
  غایب بود.
- `alert()` مرورگر حذف شد (تنها جای برنامه که این کار را می‌کرد).
- پنج مورد `bg-white/90 shadow-slate-900` که در حالت تیره می‌شکستند، با توکن
  معنایی جایگزین شدند. **در حالت تیره صفر عنصر سفید باقی مانده** (اندازه‌گیری
  با `getComputedStyle`).

---

## راستی‌آزمایی تست‌ها

**هر پنج باگ عمداً برگردانده شد و تست شکست:**

| باگ برگردانده‌شده | تستی که گرفت |
|---|---|
| `branch_id` برای categories | `categories هرگز branch_id نمی‌گیرد` |
| policy نشتی `true` | `🔴 policy نشتی «true» حذف می‌شود` |
| حذفِ پنجره‌ی تأیید | `managed-list پیش از حذف تأیید می‌گیرد` |
| `await supabase` بدون بررسی خطا | `همه‌ی نوشتن‌ها نتیجه را بررسی می‌کنند` |
| تم رنگی در دو صفحه | `🔴 انتخابگر تم فقط در یک صفحه است` |

دو تستِ خودم هم اول اشتباه بودند و اصلاح شدند — یکی روی *کامنت فارسی* گیر کرده
بود (همان تله‌ی همیشگی) و دیگری regex بیش‌ازحد سخت‌گیر داشت.

---

## آزمون واقعی در مرورگر

روی دسته‌ی یکبارمصرف «ZZ تست موقت» با داده‌ی زنده:

```
افزودن            → در فهرست ظاهر شد            ✅
افزودن تکراری     → «این نام قبلاً ثبت شده است»  ✅ (پیش از این: سکوت)
حذف               → پنجره‌ی تأیید آمد            ✅
تأیید حذف         → از فهرست رفت                 ✅
```

سپس پاک شد.

---

## وضعیت نهایی

```
tsc --noEmit   پاک
vitest         ۸۴۸ / ۸۴۸  (۳۴ فایل) — از ۸۰۱
next build     Compiled successfully
axe-core       صفر ایراد در ۵ صفحه × ۳ اندازه = ۱۵ ترکیب
سرریز افقی     هیچ‌کدام
حالت تیره      صفر عنصر سفیدِ جامانده
خطای کنسول     صفر
```

**داده‌ی شما دست‌نخورده:** ۴ کسب‌وکار · ۲۶ فاکتور · ۳۸۳ کالا · ۵۴۹ شخص ·
۷۵۲ گردش انبار · ۶ حساب · ۱۲ دسته هزینه. نام مالکان سر جایشان.
دسته‌ها ۱۵ → ۲۸ (۱۵ یتیم دست‌نخورده + ۱۳ تازه برای چهار کسب‌وکار).

---

## صادقانه: چه چیزی انجام نشد

- **برندها برای مزون پوشاک خالی است** — جدول `brands` صفر ردیف دارد. این باگ
  نیست، فقط هنوز برندی ثبت نکرده‌اید. حالا که کارت درست کار می‌کند می‌توانید
  اضافه کنید.
- **`/settings/general` هنوز فقط نام فروشگاه را دارد.** لوگو، آدرس، تلفن و
  تنظیمات فاکتور (که در همان صفحه وعده داده شده بود) هنوز نیست.
- **صفحه‌ی `/settings/price-lists` دست نخورد** — خارج از دامنه‌ی این مرحله بود.

## باقی‌مانده‌ی کلی

⬜ خروجی/پشتیبان کامل · ⬜ نمودارهای جذاب‌تر · ⬜ داشبورد بر اساس بیشترین استفاده
⬜ صفحه‌ی عمومی فروشگاه · ⬜ تحلیل صنفی · ⬜ اپ اندروید (PWA اول)
⬜ ویرایش قلم‌به‌قلم فاکتور توسط پشتیبانی · ⬜ پرچم قابلیت
