# کسانی که سایت فروشگاهی دارند، چطور به نرم‌افزار مالی وصلش می‌کنند؟

تحقیق مرداد ۱۴۰۵ · بر پایه‌ی **مستندات رسمی خود شرکت‌ها**، نه تبلیغات

---

## خلاصه در یک بند

فروشنده‌ای که سایت دارد، سایتش را با **وب‌سرویس (REST API)** نرم‌افزار
حسابداری وصل می‌کند. یک افزونه‌ی واسط (معمولاً روی ووکامرس) هر چند
دقیقه یک‌بار کالا و موجودی و قیمت را از حسابداری می‌کشد و سفارش‌های
سایت را به‌صورت فاکتور به حسابداری برمی‌گرداند. نقطه‌ی چسبِ دو سیستم،
**کد کالا (SKU)** است.

ولی وقتی مستندات را باز کردم، یک چیز مهم فهمیدم که کل تصویر را عوض
می‌کند. اول بگذارید ببینیم دقیقاً چه می‌کنند.

---

## ۱. چطور کار می‌کند — از زبان خود سپیدار

سپیدار مستندات عمومی دارد و صریح‌ترین منبع است.

**راه‌اندازی:** کاربر باید در نرم‌افزار سپیدار گزینه‌ی «اجرای برنامه در
حالت سرویس‌دهنده» را بزند (فقط برای کاربر `admin` فعال است). سرویس روی
**پورت ۷۳۷۳** بالا می‌آید:

```
http://localhost:7373
```

و بعد این جمله را عیناً در مستندات رسمی‌شان نوشته‌اند:

> «آدرس ذکرشده در بالا **فقط از داخل همان سیستم** قابل دسترسی است و
> برای دسترسی به سرویس‌دهنده از طریق اینترنت، این موضوع باید توسط خود
> مشتری و با تهیه **IP static** از شرکت ارائه سرویس اینترنت پیگیری
> شود.»

**احراز هویت:** JWT، به‌علاوه‌ی سه هدر دیگر روی هر درخواست:
`GenerationVersion` (نسخه‌ی API)، `IntegrationID` (کد دستگاه)،
`ArbitraryCode` (یک GUID یکتا) و `EncArbitraryCode` (همان GUID
رمزشده با کلید عمومی).

**سرفصل‌های API** (از فهرست رسمی): مشتری‌ها، گروه مشتری، واحد سنجش،
مشخصات کالا، انبارها، کالاها، تصویر کالا، **موجودی کالاها**، نوع فروش،
اعلامیه قیمت، ارزها، **پیش‌فاکتورها**، **فاکتورها**، بانک‌ها،
حساب‌های بانکی، **رسید دریافت**.

**نمونه‌ی ثبت فاکتور** — `POST /api/invoices/`:

```json
{
  "GUID": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "CurrencyRef": 1,
  "CustomerRef": 1,
  "SaleTypeRef": 1,
  "Price": 0, "Discount": 0, "Tax": 0, "Duty": 0,
  "Items": [
    { "RowID": 1, "ItemRef": 1, "StockRef": 1,
      "Quantity": 0, "Fee": 0, "Price": 0, "Discount": 0 }
  ]
}
```

نکته‌ی طراحی که ارزش یادگیری دارد: فیلد `GUID` با توضیح **«شناسه‌ی
یکتا برای جلوگیری از ثبت تکراری»**. یعنی اگر افزونه به‌خاطر قطعی شبکه
دوباره تلاش کند، فاکتور دوم ثبت نمی‌شود. این همان الگوی *idempotency
key* است و اگر ما هم API بدهیم، حتماً باید داشته باشیم.

خطاهایی که برمی‌گرداند هم گویا است: «شناسه سند نباید خالی باشد»،
«کالای نامعتبر»، «شناسه کالای مشمول تخفیف پیدا نشد».

---

## ۲. هلو — همان الگو، با اعتراف صریح‌تر

افزونه‌ی ووکامرسِ هلو دوطرفه است: کالا و مشتری و موجودی و قیمت از هلو
به سایت، سفارش سایت به‌صورت فاکتور فروش به هلو.

در پرسش‌های متداول فروشنده‌ی افزونه:

> «ارتباط آنلاین و دائمی با هلو، **در صورتی که سیستمِ دارای نرم‌افزار
> هلو روشن و به اینترنت متصل باشد.**»

و در پاسخ به «شب‌ها سیستم را خاموش می‌کنم اشکالی دارد؟» می‌گویند نه —
یعنی می‌پذیرند که در آن ساعت‌ها **همگام‌سازی متوقف است**.

فهرست تغییرات نسخه‌های افزونه، خودش یک سند صادقانه از دردسرهاست:

- «رفع مشکل **موجودی‌های منفی** هلو در ووکامرس» (سه بار در نسخه‌های مختلف!)
- «هماهنگی کلیدهای موجودی با نسخه‌ی جدید وب‌سرویس هلو»
- «ایجاد جداول اختصاصی دیتابیس و بهینه‌سازی سرعت وقتی تعداد کالاها زیاد است»
- «نمایش خطاهای اتصال در تنظیمات افزونه»

یعنی: موجودی منفی، ناسازگاری نسخه، کندی روی کاتالوگ بزرگ، و کور بودن
کاربر نسبت به قطعی اتصال. اینها مسائل ذاتی این معماری‌اند.

---

## ۳. الگوی دوم: کرون‌جاب به‌جای درخواست زنده

بعضی واسط‌ها (مثل حساب‌یار برای پرستاشاپ) روش دوم دارند: به‌جای اینکه
هنگام بازدید کاربر درخواست بزنند، یک **کرون‌جاب** هر دقیقه تعداد
مشخصی محصول و سفارش را منتقل می‌کند.

دلیلش را خودشان می‌گویند: «عدم امکان ثبت دسته‌جمعی — نیاز به ارسال
درخواست جداگانه برای هر محصول» و «محدودیت هاست و وب‌سرویس».

مزیت: «نیازی به باز بودن مرورگر نیست.» عیب: کندتر است.

---

## ۴. الگوی سوم: فروشگاه‌ساز میزبان

سپهر فروشگاه‌ساز ابری است که خودش اتصال به سپیدار، راهکاران، باران و
حسابفا را برقرار می‌کند. مشتری فرم پر می‌کند و بقیه‌اش با پشتیبانی
است. اینجا فروشگاه‌ساز میزبان است و حسابداری مهمان.

## ۵. الگوی چهارم: مارکت‌پلیس

مارکیتوهاب برای دیجی‌کالا: همگام‌سازی موجودی بین سایت و پنل فروشنده،
قیمت‌گذاری خودکار، صدور خودکار فاکتور فروش و برگشت. افزونه‌ی
ووکامرس↔باسلام هم وجود دارد و لاگ تغییراتش پر است از «رفع مشکل سینک
موجودی».

---

## 🔴 چیزی که کل این تصویر را برای ما عوض می‌کند

بعد از خواندن مستندات، الگو روشن است:

**هلو، سپیدار و محک روی ویندوزِ کامپیوتر مغازه نصب‌اند و از اینترنت
در دسترس نیستند. تمام این صنعتِ افزونه و وب‌سرویس و کرون‌جاب و
IP استاتیک، فقط برای عبور از همین یک دیوار ساخته شده.**

هزینه‌ای که مشتری می‌پردازد:

| مورد | هزینه |
|---|---|
| اتصال یک‌طرفه‌ی موجودی | ۷۵۰ هزار تومان |
| اتصال یک‌طرفه‌ی قیمت | ۹۵۰ هزار تومان |
| اتصال قیمت و موجودی محک | ۵ میلیون تومان |
| اتصال سفارش‌های ووکامرس به هلو | ۲۵ میلیون تومان |
| IP استاتیک | ماهانه، از ISP |
| کامپیوتر روشن ۲۴ ساعته | برق + ریسک |

و در ازایش چه می‌گیرد؟ سینکی که شب‌ها قطع است، موجودی که گاهی منفی
می‌شود، و وابستگی به روشن‌ماندن یک کامپیوتر.

**ترازو این دیوار را ندارد.** دیتابیس ما همین حالا روی اینترنت است و
۲۴ ساعته بالاست. کاری که آن‌ها با ۲۵ میلیون تومان و IP استاتیک انجام
می‌دهند، برای ما یک `route.ts` است.

---

## وضعیت واقعی ما — سنجیده‌شده، نه حدس

```
تنوع کالا           ۳۸۶
دارای SKU           ۳۷۵  (۹۷٪) ← نقطه‌ی چسب آماده است
دارای بارکد          ۴   (۱٪)  ← تقریباً هیچ
sales_orders        ۰ ردیف     ← جدول هست، خالی
sales_order_items   ۰ ردیف
storefronts         ۰ ردیف     ← هنوز هیچ فروشگاهی منتشر نشده
```

`sales_orders` ستون‌های لازم را از قبل دارد: `status` با مقادیر
`pending/confirmed/converted/cancelled/expired` و مهم‌تر
`converted_to_id` که به `sales.id` اشاره می‌کند — یعنی «این سفارش به
کدام فاکتور تبدیل شد». دقیقاً همان چیزی که برای جریان سفارش آنلاین
لازم است.

`/shop/<slug>` فقط ویترین است: سبد خرید ندارد.

---

## پیشنهاد من

### گام ۱ — سفارش مستقیم روی `/shop` (بیشترین ارزش، کمترین کار)

مشتری از بیو اینستاگرام می‌آید، سبد می‌بندد، نام و شماره می‌دهد، سفارش
در `sales_orders` می‌نشیند و شما با یک کلیک به فاکتور تبدیلش می‌کنید.

چرا اول این: جدولش هست، موجودی خودبه‌خود یکی است (**صفر همگام‌سازی، صفر
موجودی منفی**)، و مشتری مزون از اینستاگرام می‌آید نه از گوگل.

### گام ۲ — API عمومی با کلید

`/api/public/v1/` برای کسی که سایت جدا دارد. با درس‌هایی که از سپیدار
گرفتیم:

- **کلید idempotency روی ثبت سفارش** (همان `GUID` سپیدار)
- **کلید API با دامنه‌ی محدود** — فقط خواندن کالا، فقط ثبت سفارش
- **صفحه‌بندی** روی فهرست کالا (PostgREST پیش‌فرض ۱۰۰۰ ردیف می‌دهد)
- **`updated_since`** تا افزونه فقط تغییرات را بکشد، نه کل کاتالوگ
- **SKU به‌عنوان کلید تطبیق** — ۹۷٪ کالاهایمان دارند
- **لاگ قابل مشاهده** تا کاربر بفهمد اتصال قطع شده (چیزی که افزونه‌ی
  هلو بعد از چند نسخه اضافه کرد)

### گام ۳ — افزونه‌ی ووکامرس

فقط اگر مشتری‌ای پیدا شد که سایت وردپرسی دارد. روی گام ۲ سوار می‌شود.

---

## دو تصمیمی که باید بگیرید

**۱. سفارش موجودی را رزرو کند یا نه؟** اگر دو نفر هم‌زمان آخرین کالا را
سفارش دهند چه می‌شود؟ رقبا با «کسر پیش‌فاکتور» حلش می‌کنند. سه گزینه:
رزرو کامل، رزرو با انقضا (مثلاً ۳۰ دقیقه)، یا بدون رزرو با هشدار.

**۲. پرداخت آنلاین یا تماس تلفنی؟** درگاه پرداخت نیاز به نماد اعتماد و
دامنه دارد. «سفارش بدون پرداخت، بعد تماس می‌گیرم» برای مزون کاملاً
رایج است و همین حالا شدنی.

---

## صادقانه: محدودیت این تحقیق

- مستندات سپیدار را مستقیم از سایت خودشان خواندم و نقل‌قول‌ها دقیق‌اند.
  برای هلو به توضیحات **فروشنده‌ی افزونه** تکیه کردم نه مستند رسمی هلو،
  چون هلو مستندات عمومی ندارد.
- با هیچ‌کدام از این API‌ها واقعاً کار نکرده‌ام. اگر روزی افزونه
  بسازیم، باید روی نصب واقعی آزمایش شود.
- قیمت‌ها از صفحه‌ی فروش شرکت‌هاست، نه فاکتور واقعی؛ معمولاً قابل مذاکره‌اند.
- **دامنه‌ی اختصاصی ندارید.** برای گام ۱ مانع نیست ولی
  `tarazooapp.vercel.app/shop/...` در بیو اینستاگرام حرفه‌ای نیست.
