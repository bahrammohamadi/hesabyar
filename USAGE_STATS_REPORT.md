# گزارش — آمار مصرف و رفع سه باگ

نسخه `26.0805` · کامیت `ee7d2b1` · ۱۴ مرداد ۱۴۰۵

---

## بازرسی خودکار ۳۴ صفحه

پیش از شروع کار جدید، یک بازرسی خودکار روی **۳۴ صفحه** اجرا کردم که همزمان
خطای کنسول، درخواست‌های ناموفق، تخلفات دسترس‌پذیری و اسکرول افقی را رصد
می‌کرد.

**سه باگ واقعی پیدا شد — هیچ‌کدام هرگز گزارش نشده بودند**، چون همگی بی‌صدا در
کنسول شکست می‌خوردند در حالی که ظاهر صفحه سالم به نظر می‌رسید.

### 🔴 ۱. صفحه‌ی «خروج کالا» وجود نداشت

سایدبار به `/inventory/out` لینک می‌داد ولی **فایل صفحه‌اش ساخته نشده بود**.
کامپوننت از `mode="out"` پشتیبانی می‌کرد و فقط دو خط کم بود.

چون Next.js لینک‌های سایدبار را پیش‌واکشی می‌کند، این خطا **در هر صفحه‌ای که
سایدبار باز بود** یک ۴۰۴ در کنسول ثبت می‌کرد.

> تستی نوشتم که همه‌ی لینک‌های سایدبار را می‌گردد و اگر صفحه‌ای نداشته باشند
> می‌شکند. این دسته باگ دیگر تکرار نمی‌شود.

### 🔴 ۲. فیلتر دسته‌بندی همیشه خالی بود

انتخابگر کالا با `.eq("is_active", true)` دسته‌بندی‌ها را می‌خواند، ولی جدول
`categories` **اصلاً چنین ستونی ندارد** (برخلاف `brands` که دارد).

```
column categories.is_active does not exist   → HTTP 400
```

نتیجه: کاربر پنجره‌ی انتخاب کالا را باز می‌کرد و فکر می‌کرد هیچ دسته‌بندی
تعریف نشده.

### 🔴 ۳. گزارش «پرفروش‌ترین کالاها» کاملاً مرده بود

صفحه‌ی `/reports/profit` دو بار به نمای `top_selling_products` درخواست می‌زد و
هر بار **۴۰۴** می‌گرفت.

علت: مهاجرت ۰۰۰۸ اول پنج نما را `drop` می‌کند و بعد می‌سازد. در دیتابیس زنده
**فقط دو تای آخر وجود داشتند** — یعنی آن مهاجرت نیمه‌کاره اجرا شده بود:

```
✅ sales_by_color, sales_by_size
🔴 top_selling_products, sales_by_category, low_selling_products
```

**ضمن بازسازی، یک اشکال محاسباتی هم درست شد:** تعریف قدیمی فقط فاکتورهای
`confirmed` را می‌شمرد. از ۲۱ فاکتور واقعی شما، **۲ تا `settled` هستند**
(۲٬۳۷۵٬۰۰۰ تومان) و از گزارش حذف می‌شدند — یعنی هرچه کسب‌وکار منظم‌تر تسویه
کند، فروشش کمتر نشان داده می‌شود.

حالا به‌جای فهرست سفید، وضعیت‌های **باطل** حذف می‌شوند. اگر روزی وضعیت تازه‌ای
اضافه شود، به‌طور پیش‌فرض در گزارش می‌آید و دوباره بی‌صدا از قلم نمی‌افتد.

---

## بازرسی دیتابیس

مستقل از رابط کاربری، سلامت داده را هم بررسی کردم:

| بررسی | نتیجه |
|---|---|
| ردیف‌های یتیم (اقلام بدون فاکتور، گردش بدون کالا و…) | ✅ صفر |
| مبالغ منفی | ✅ صفر |
| **موجودی انبار در برابر تاریخچه‌ی گردش‌ها** | ✅ هر ۳۸۵ تنوع دقیقاً می‌خوانند |
| توابع `definer` بدون `search_path` | ✅ صفر |
| جداول بدون RLS | ✅ صفر |

**یک نکته:** دو فاکتور اول (`F-00001` و `F-00002` از ۶ و ۷ تیر) **صفر قلم**
دارند ولی مبلغ دارند. اینها داده‌ی آزمایشی روزهای راه‌اندازی خودتان هستند، نه
باگ کد فعلی — دست نزدم.

---

## آمار مصرف کسب‌وکارها

صفحه‌ی تازه در «مدیریت پلتفرم ← آمار مصرف»:

**برای هر کسب‌وکار:** تعداد کاربر، کالا، تنوع، مخاطب، فروش، خرید، تراکنش و
گردش انبار — به‌علاوه‌ی فعالیت ۳۰ روز اخیر به‌صورت **جدا**.

> چرا جدا؟ سازمانی با ۵۰۰ فاکتور قدیمی و صفر فاکتور ماه اخیر «بزرگ» به نظر
> می‌رسد ولی در واقع رهاشده است.

**وضعیت سلامت** که خودکار محاسبه می‌شود:

| وضعیت | معنی |
|---|---|
| **فعال** | در ۳۰ روز اخیر فروش داشته |
| **تازه** | کمتر از یک هفته از ثبت‌نام گذشته |
| **کم‌تحرک** | فعالیت دارد ولی ۳۰ روز فروشی ثبت نکرده |
| **رهاشده** | بیش از ۳۰ روز هیچ عملیاتی نکرده |
| **شروع نکرده** | از ثبت‌نام تاکنون هیچ داده‌ای وارد نکرده |

«شروع نکرده» عمداً از «رهاشده» جداست: اولی مشکل onboarding است، دومی مشکل
نگهداشت — و پاسخ پشتیبانی به هرکدام فرق دارد. سازمان‌های زیر یک هفته هم هرگز
«رهاشده» علامت نمی‌خورند.

### وضعیت فعلی شما

```
مزون پوشاک         فعال        ۳ کاربر · ۳۸۱ کالا · ۵۴۸ مخاطب · ۲۱ فروش (۱۶ اخیر) · ۲۰٬۲۰۵٬۰۰۰ تومان
حسابداری بازرگانی  تازه        ۱ کاربر · بدون داده
سازمان تست پنل     شروع نکرده  ۳۱ روز بی‌فعالیتی
```

---

## تست

```
npx tsc --noEmit  → تمیز
npx vitest run    → ۴۷۱/۴۷۱ (۲۶ فایل)
npm run build     → Compiled successfully
```

| بررسی | نتیجه |
|---|---|
| هر سه باگ روی برنامه‌ی در حال اجرا تأیید شد | ✅ |
| شمارش‌ها با دیتابیس می‌خوانند (۳۸۱ کالا، ۵۴۸ مخاطب، ۲۱ فروش) | ✅ |
| تشخیص وضعیت برای هر سه سازمان درست بود | ✅ |
| فیلتر و جستجو | ✅ |
| axe در ۳۹۰ و ۱۴۴۰ | صفر تخلف |

همین مجموعه **روی سایت زنده هم اجرا شد** و پاس شد.

---

## وضعیت فهرست تحقیق

| مورد | وضعیت |
|---|---|
| بازنشانی رمز · تغییر رمز · اعلان زنگوله | ✅ |
| کندسازی ورود · مدیریت نشست‌ها · نقش سفارشی | ✅ |
| **آمار مصرف** | ✅ **انجام شد** |
| صادرات داده کسب‌وکار | ⬜ باقی |
| رابط تیکت پشتیبانی | ⬜ باقی |
| حذف نرم و بازیابی سازمان | ⬜ باقی |
| پرچم قابلیت | ⬜ باقی |
| اشتراک و پلن · تایمر · داشبورد درآمد | ⏸ منتظر تصمیم شما |

**پیشنهاد بعدی:** رابط تیکت پشتیبانی — جدول‌ها و RLS از قبل ساخته شده‌اند و
فقط صفحه ندارند، پس کار کمی دارد و ارزش زیادی.
