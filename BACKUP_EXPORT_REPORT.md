# گزارش: پشتیبان کامل و رفع نقص امنیتی خروجی‌ها

**تاریخ:** ۱۹ مرداد ۱۴۰۵ · **نسخه:** ۲۶.۰۸۱۰b · بدون مهاجرت دیتابیس

---

## چرا این مرحله

راهنمای «ورود اطلاعات از اکسل» به کاربر می‌گفت:

> پیش از هر کاری از اطلاعات خود پشتیبان بگیرید… **از پشتیبانی بخواهید یک نسخه‌ی
> کامل برایتان بفرستد.**

یعنی توصیه‌ای می‌کردیم که راهش را نداشتیم. حالا دکمه‌اش هست.

---

## ۱) پشتیبان کامل — `/settings/backup`

یک فایل اکسل با **۱۲ شیت**؛ هر جدول جدا، به‌علاوه‌ی یک شیت «فهرست» که می‌گوید
فایل مال کیست، از چه تاریخی است و هر شیت چند رکورد دارد.

### آزمون واقعی روی داده‌ی زنده

فایل دانلود شد (۱٫۰۹ مگابایت) و محتوایش **سطر‌به‌سطر با دیتابیس مقایسه شد**:

| شیت | فایل | دیتابیس |
|---|---|---|
| دسته‌بندی | ۴ | ۴ ✅ |
| برندها | ۰ | ۰ ✅ |
| کالاها | ۳۸۱ | ۳۸۱ ✅ |
| تنوع کالا | ۳۸۴ | ۳۸۴ ✅ |
| اشخاص | ۵۴۹ | ۵۴۹ ✅ |
| حساب‌ها | ۲ | ۲ ✅ |
| دسته هزینه | ۶ | ۶ ✅ |
| فاکتور فروش | ۲۶ | ۲۶ ✅ |
| اقلام فروش | ۳۳ | ۳۳ ✅ |
| تراکنش مالی | ۲۴ | ۲۴ ✅ |
| گردش انبار | **۷۵۰** | **۷۵۰** ✅ |

### 🔴 چرا «گردش انبار» مهم‌ترین سطر این جدول است

PostgREST به‌طور پیش‌فرض **فقط ۱۰۰۰ ردیف** برمی‌گرداند. بدون صفحه‌بندی صریح،
پشتیبانِ کسب‌وکاری با ۵۰۰۰ حرکت انبار **بی‌صدا ناقص** می‌شد — و کاربر تا روزی
که واقعاً به آن نیاز پیدا کند نمی‌فهمید. برای یک ابزار پشتیبان این بدترین نوع
خرابی است.

پس صفحه‌بندی پیاده شد و اگر روزی به سقف سخت (۵۰٬۰۰۰ ردیف) بخوریم، **در خود
فایل با هشدار قرمز نوشته می‌شود** که کدام شیت‌ها ناقص‌اند. پشتیبانی که ناقص
است ولی خودش را کامل نشان بدهد، از نبودِ پشتیبان خطرناک‌تر است.

### تصمیم‌های دیگر

- **مجوز `settings.manage`** — این فایل همه‌چیز را یک‌جا جمع می‌کند: تلفن تمام
  مشتریان، قیمت خرید هر کالا، کل گردش مالی. هرکدام جداگانه شاید در دسترس
  باشند، ولی یک‌جا و قابل‌حمل بودنشان ریسک دیگری است.
- **سقف ۵ درخواست در ۵ دقیقه** — آزموده شد: درخواست چهارم `429` گرفت.
- **جدول‌ها پشت‌سرهم خوانده می‌شوند، نه موازی** — `Promise.all` روی ۱۱ کوئریِ
  صفحه‌بندی‌شده استخر اتصال Supabase را اشباع می‌کند.
- **`select` صریح، نه `*`** — تا ستون حساسی که فردا اضافه شود، خودبه‌خود وارد
  فایل کاربر نشود.
- **جداسازی مستأجر آزموده شد**: کاربر `test` (مالک سازمان دیگر) فقط ۴۲ کیلوبایت
  داده‌ی خودش را گرفت، نه داده‌ی مزون پوشاک.

---

## ۲) 🔴 نقص امنیتی: CSV Injection

### مسئله

تابع `csvEscape` **در ۹ فایل کپی شده بود** و هر ۹ نسخه فقط گیومه را دو برابر
می‌کرد:

```ts
`"${text.replace(/"/g, '""')}"`
```

اکسل و Google Sheets هر سلولی را که با `=`، `+`، `-` یا `@` شروع شود **فرمول**
تلقی می‌کنند — حتی داخل گیومه.

**سناریوی واقعی:** کاربر کالایی به نام `=cmd|' /C calc'!A1` می‌سازد (یا ما از
یک فایل اکسل واردش می‌کنیم). بعد صاحب فروشگاه از فهرست کالاها خروجی می‌گیرد و
فایل را باز می‌کند. آن فرمول روی **کامپیوتر او** اجرا می‌شود.

بازتولید شد:
```
csvEscape("=cmd|@ calc!A1")  →  "=cmd|@ calc!A1"     ← اکسل اجرا می‌کند
```

### راه‌حل

یک آپاستروف پیش از کاراکتر خطرناک (توصیه‌ی OWASP). اکسل آپاستروف را نمایش
نمی‌دهد ولی سلول را «متن» می‌کند.

**⚠️ ظریف‌ترین بخش:** اگر همه‌ی مقادیرِ شروع‌شده با `-` را خنثی می‌کردیم،
**تمام مبالغ منفی در اکسل به متن تبدیل می‌شدند** و جمع‌بستن ستون کار نمی‌کرد —
یعنی رفع یک باگ با ساختن باگ بدتر. پس شرط فقط وقتی برقرار است که کاراکتر بعدی
رقم نباشد:

```
"-15000"     → دست‌نخورده  ✅ (عدد می‌ماند)
"-cmd|calc"  → خنثی        ✅
```

همین خنثی‌سازی در فایل اکسل پشتیبان هم جداگانه پیاده شد (آنجا `csvEscape` کمکی
نمی‌کند چون سلول باینری است). در فایل واقعی دانلودشده: **صفر سلول فرمولِ
خنثی‌نشده.**

### دو باگ کوچک‌تر در همان کد

- **`URL.revokeObjectURL` بلافاصله بعد از کلیک** صدا زده می‌شد. در فایرفاکس و
  سافاری دانلود هنوز شروع نشده که آدرس باطل می‌شود و **فایل ناقص** ذخیره
  می‌گردد. در کروم معمولاً کار می‌کند، برای همین کسی متوجه نشده بود.
- **`alert()` مرورگر** برای «داده‌ای برای خروجی وجود ندارد» — پنجره‌ی سیستمی
  انگلیسی وسط رابط فارسی.

هر سه در یک فایل مشترک (`lib/export/`) حل شدند و **هر ۹ کپی حذف شد**.

---

## 🔴 باگی که فقط با اجرای واقعی پیدا شد

`tsc --noEmit` تمیز، `next build` موفق — ولی درخواست واقعی **۵۰۰** داد:

```
TypeError: Cannot convert argument to a ByteString because the character
at index 37 has a value of 1778 which is greater than 255
```

علت: `todayJalali()` تاریخ را با **ارقام فارسی** برمی‌گرداند («۱۴۰۵/۰۵/۱۹») و
آن رشته مستقیم در هدر `Content-Disposition` می‌رفت. هدرهای HTTP فقط بایت
می‌پذیرند.

کامنت خودم بالای همان تابع نوشته بود «نام فایل ASCII است» — ولی **هیچ کدی آن را
تضمین نمی‌کرد**. دقیقاً همان فاصله‌ی «ظاهری» با «واقعاً پیاده‌شده».

اصلاح در **دو لایه**: هم تولیدکننده‌ی نام، و هم `xlsxResponse` که روت‌های قالب
اکسل هم از آن استفاده می‌کنند و نباید به هر فراخوان اعتماد کند.

---

## راستی‌آزمایی تست‌ها

**هشت باگ عمداً برگردانده شد و تست شکست:**

| باگ برگردانده‌شده | نتیجه |
|---|---|
| `csvEscape` آسیب‌پذیر | **۶ تست** شکست |
| خنثی‌کردن عدد منفی (باگ بدتر) | `🔴 عدد منفی دست‌نخورده می‌ماند` |
| حذف BOM | `🔴 BOM دارد وگرنه فارسی خراب می‌شود` |
| `revokeObjectURL` فوری | `🔴 بلافاصله صدا زده نمی‌شود` |
| حذف صفحه‌بندی | `🔴 صفحه‌بندی دارد — سقف ۱۰۰۰ ردیف` |
| حذف هشدار ناقص‌بودن | `🔴 ناقص‌بودن در فایل اعلام می‌شود` |
| برگرداندن یک کپی محلی | `هیچ فایلی csvEscape خودش را ندارد` |
| نام فایل فارسی | **۳ تست** شکست |

دو تست خودم هم اول اشتباه بودند: اندیس ستون را ثابت فرض کرده بودم در حالی که
`total` ستون هشتم است. اول فکر کردم باگ در کد است — نبود.

---

## وضعیت نهایی

```
tsc --noEmit   پاک
vitest         ۸۹۷ / ۸۹۷  (۳۵ فایل) — از ۸۴۸
next build     Compiled successfully
axe-core       صفر ایراد در ۳ صفحه × ۳ اندازه
سرریز افقی     هیچ‌کدام
دانلود واقعی   ۱٫۰۹ مگابایت · ۱۲ شیت · همه‌ی شمارش‌ها با دیتابیس یکی
سقف نرخ        درخواست چهارم ۴۲۹ گرفت ✅
```

**داده‌ی شما دست‌نخورده** — این مرحله فقط خواندنی بود؛ هیچ مهاجرتی و هیچ
نوشتنی در دیتابیس نداشت.

---

## صادقانه: چه چیزی انجام نشد

- **بازگردانی از روی فایل پشتیبان وجود ندارد.** فایل برای نگهداری و مرور است.
  این را هم در خود صفحه و هم در شیت «فهرست» صریح نوشته‌ام تا کسی روی چیزی که
  نداریم حساب باز نکند. بازگردانی خودکار کار بزرگ و پرریسکی است (تشخیص تعارض،
  ترتیب وابستگی‌ها، رکوردهای نیمه‌وارد) و باید مرحله‌ی جداگانه‌ای باشد.
- **`purchases` در پشتیبان نیست** — جدولش روی داده‌ی زنده خالی است.
- **`alert()` در جاهای دیگر هنوز هست** (`sales/orders`، `sales/returns`). آن‌ها
  خارج از مسیر خروجی بودند و عمداً دست نخوردند تا این تغییر متمرکز بماند.
- **پشتیبان زمان‌بندی‌شده‌ی خودکار** نداریم؛ `pg_cron` روی پلن رایگان نیست.

## باقی‌مانده‌ی کلی

⬜ نمودارهای جذاب‌تر · ⬜ داشبورد بر اساس بیشترین استفاده
⬜ صفحه‌ی عمومی فروشگاه · ⬜ تحلیل صنفی · ⬜ اپ اندروید (PWA اول)
⬜ ویرایش قلم‌به‌قلم فاکتور توسط پشتیبانی · ⬜ پرچم قابلیت
⬜ ورود فاکتورها و تراکنش‌های گذشته از اکسل
