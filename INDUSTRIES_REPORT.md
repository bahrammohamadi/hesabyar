# گزارش: صفحات صنفی در سایت معرفی

**تاریخ:** ۱۹ مرداد ۱۴۰۵ · **نسخه:** ۲۶.۰۸۱۰e · بدون مهاجرت دیتابیس

---

## درخواست

مورد دوم از بررسی kamixapp که تأیید کرده بودید:

> تحلیل صنفی در سایت معرفی — کاربر صنفش را انتخاب می‌کند و می‌بیند برنامه چه
> مشکلی از او حل می‌کند

نشانی: **`/industries`** و برای هر صنف **`/industries/<شناسه>`**

---

## چرا صفحه‌ی جدا و نه بخشی از «امکانات»

صفحه‌ی امکانات فهرست قابلیت‌هاست — از زبان *محصول*. کاربر باید خودش ترجمه کند
که «تنوع کالا» یعنی «سایز و رنگ شومیز». این صفحات همان ترجمه را انجام می‌دهند:
از زبان *مشکل کاربر* شروع می‌کنند.

ساختار هر صفحه:

```
«نمی‌دانم از هر سایز و رنگ چند تا مانده»
    ↓
هر کالا می‌تواند چند «تنوع» داشته باشد؛ هر رنگ و سایز موجودی
و قیمت جداگانه دارد.                        → دیدن در برنامه ←
```

۹ صنف: پوشاک، کافه و رستوران، سوپرمارکت، آرایشی بهداشتی، موبایل، طلا و جواهر،
قنادی، ابزار و لوازم خانگی، لوازم‌التحریر.

---

## 🔴 قاعده‌ی سختی که گذاشتم: فقط قابلیت موجود

وسوسه‌ی صفحات صنفی این است که برای هر صنف چیز جذابی وعده بدهیم — «مدیریت
آشپزخانه» برای کافه، «نسخه‌پیچی» برای داروخانه، «محاسبه‌ی عیار» برای طلافروشی.
هیچ‌کدام را نداریم.

سه تست این قاعده را نگه می‌دارند:

**۱. هر لینک به مسیر واقعیِ موجود اشاره می‌کند.** تست، درخت `app/(app)` را
پیمایش می‌کند و مسیرها را با فایل‌سیستم مقایسه می‌کند — نه با فهرست دستی.
لینک به صفحه‌ای که وجود ندارد یعنی وعده‌ی قابلیتی که نداریم.

**۲. کلمات ممنوع در پاسخ‌ها.** «سامانه‌ی مودیان»، «درگاه پرداخت»، «اپلیکیشن
اندروید» و «دوربین موبایل» در بخش *پاسخ* ممنوع‌اند (ولی در بخش «نداریم» مجازند).

**۳. هر صنف باید بخش «هنوز نداریم» داشته باشد** — با حداقل یک مورد.

### آنچه صریحاً اعلام شده که نداریم

| صنف | اعلام‌شده |
|---|---|
| کافه | مدیریت میز، رسپی و کسر خودکار مواد، سفارش آنلاین |
| سوپرمارکت | ترازو و کالای وزنی، سامانه‌ی مودیان، تاریخ انقضا |
| آرایشی بهداشتی | نسخه‌پیچی، تی‌تک، کنترل انقضا |
| موبایل | شماره سریال و IMEI، گارانتی، سامانه‌ی همتا |
| طلا | وزن و عیار و اجرت، قیمت لحظه‌ای طلا، انگ |

این بخش **پیش از دکمه‌ی ثبت‌نام** می‌آید، نه بعد از آن — یک تست ترتیبش را
تضمین می‌کند. بهتر است کاربر قبل از تصمیم بداند تا بعد ناامید شود.

---

## تأیید عملی

**هر ۱۹ مسیر لینک‌شده در مرورگر واقعی باز شد** (با ورود، تا صفحات پنل واقعاً
رندر شوند):

```
/products  /reports/profitability  /purchases  /contacts/debtors
/sales  /inventory  /finance  /settings/users  /settings/import
/inventory/adjust  /settings/catalog  /loyalty  /inventory/stock-card
/checks  /dashboard  /sales/orders  /contacts/suppliers
/settings/price-lists  /reports

مسیرهای خراب: هیچ ✅
```

**بازدیدکننده‌ی خارج‌شده:** `/industries` و `/industries/apparel` هر دو
`200` دادند و به `/login` منتقل نشدند. صنف ناموجود `404` گرفت.

### درسی که از نوبت قبل به کار آمد

در نوبت قبل، صفحه‌ی فروشگاه ساخته شده بود ولی `middleware` بازدیدکننده‌ی
ناشناس را به `/login` می‌فرستاد — چون فهرست مسیرهای عمومی **تطبیق دقیق** دارد
و `/shop/<slug>` در آن نمی‌گنجید.

اینجا همان الگو تکرار می‌شد (`/industries/<id>` هم متغیر است)، پس از همان اول
`isPublicIndustry` با `startsWith` اضافه شد و دو تست نگهش می‌دارند.

---

## چند تصمیم ریز

- **برچسب و ایموجی از `lib/business-types.ts` می‌آید، نه تکرار محلی.** اگر در
  دو جا بود، روزی یکی عوض می‌شد و فرم معارفه با سایت معرفی فرق می‌کرد. یک تست
  وجودِ هر شناسه در فهرست اصلی را بررسی می‌کند — همان اشتباهی که در مهاجرت
  ۰۰۴۰ با شناسه‌های حدسی (`clothing`, `restaurant`) کردم.
- **صنف «سایر» صفحه ندارد** — محتوای صنفی معناداری نمی‌شد نوشت.
- **«صنف من نیست» بن‌بست نیست** — پایین فهرست به صفحه‌ی امکانات ارجاع می‌دهد.
- **همه‌ی ۹ صفحه در زمان بیلد ساخته می‌شوند** (`generateStaticParams`)؛ محتوا
  ثابت است و رندر در زمان درخواست بی‌دلیل بود.
- **در نقشه‌ی سایت آمده‌اند** — این‌ها دقیقاً صفحاتی‌اند که کسی «نرم‌افزار
  حسابداری پوشاک» را جستجو می‌کند و باید پیدا کند.

---

## راستی‌آزمایی تست‌ها

**پنج مورد عمداً برگردانده شد و تست شکست:**

| باگ برگردانده‌شده | تستی که گرفت |
|---|---|
| لینک به مسیر ناموجود | `🔴 هر پاسخ به مسیر واقعی اشاره می‌کند` |
| وعده‌ی «سامانه‌ی مودیان» | `هیچ قابلیت ناموجودی وعده داده نشده` |
| حذف بخش «هنوز نداریم» | `هر صنف بخش «هنوز نداریم» دارد` |
| حذف `/industries` از middleware | `در محاسبه‌ی isPublicSite لحاظ شده` |
| شناسه‌ی حدسی `clothing` | `🔴 هر صنف در business-types.ts وجود دارد` |

---

## وضعیت نهایی

```
tsc --noEmit   پاک
vitest         ۹۹۲ / ۹۹۲  (۳۸ فایل) — از ۹۶۸
next build     Compiled successfully · ۹ صفحه‌ی صنفی پیش‌ساخته
axe-core       صفر ایراد در ۴ صفحه × ۳ اندازه = ۱۲ ترکیب
سرریز افقی     هیچ‌کدام
خطای کنسول     صفر
لینک‌های شکسته  هیچ (۱۹ مسیر آزموده شد)
```

**داده‌ی شما لمس نشد** — این مرحله فقط محتوای ایستا بود؛ نه مهاجرتی، نه
نوشتنی در دیتابیس.

---

## صادقانه: محدودیت‌ها

- **محتوا دستی است، نه تحلیل خودکار.** «تحلیل صنفی» یعنی متنی که من بر اساس
  قابلیت‌های واقعی نوشته‌ام، نه سیستمی که داده‌ی صنف را تحلیل کند.
- **آمار بازدید ندارد** — نمی‌دانیم کدام صنف بیشتر دیده می‌شود.
- **بدون عکس و اسکرین‌شات.** افزودن تصویر واقعی از هر بخش، صفحات را ملموس‌تر
  می‌کند ولی نیازمند تصاویر آماده است.
- **متن‌ها بازبینی بازاریابی نشده‌اند** — از دید توسعه‌دهنده نوشته شده‌اند و
  اگر جمله‌ای با لحن کسب‌وکار شما نمی‌خواند، بگویید اصلاح کنم.

## باقی‌مانده

⬜ نمودارهای جذاب‌تر · ⬜ داشبورد بر اساس بیشترین استفاده
⬜ اپ اندروید (PWA اول) · ⬜ ویرایش قلم‌به‌قلم فاکتور توسط پشتیبانی
⬜ پرچم قابلیت · ⬜ ورود فاکتورها و تراکنش‌های گذشته از اکسل
