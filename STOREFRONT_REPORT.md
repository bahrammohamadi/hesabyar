# گزارش: صفحه‌ی عمومی فروشگاه

**تاریخ:** ۱۹ مرداد ۱۴۰۵ · **مهاجرت:** `0041_public_storefront.sql` · **نسخه:** ۲۶.۰۸۱۰d

---

## درخواست

از بررسی kamixapp، خودتان تأیید کردید:

> صفحه‌ی عمومی فروشگاه — آدرس، تلفن، ساعت کاری، چند کالا؛ لینکش را در اینستاگرام
> می‌گذارند

نشانی: **`/shop/<نشانی-شما>`**

---

## 🔴 مهم‌ترین تصمیم: چطور داده عمومی شود بدون اینکه چیزی لو برود

راه ساده این بود که به جدول‌های `products` و `organizations` یک policy عمومی
اضافه کنیم. **این کار فاجعه می‌بود**: همان جدول‌ها قیمت خرید، حاشیه‌ی سود،
موجودی واقعی و شماره تلفن مالک را دارند. یک policy اشتباه یا یک `select *` در
آینده، همه را لو می‌داد.

به‌جایش سه لایه:

1. **هیچ policy عمومی روی جدول‌های موجود اضافه نشد.** یک جدول جدا `storefronts`
   فقط برای تنظیمات عمومی.
2. **دو تابع `security definer` با ستون‌های صریح.** نه نما — چون نما با
   `grant select` یعنی هر ستونی که فردا اضافه شود خودکار عمومی می‌شود.
   با `returns table (...)` ستون تازه تا وقتی کسی عمداً اضافه‌اش نکند بیرون
   نمی‌رود.
3. **صفحه کاملاً سمت سرور رندر می‌شود.** مرورگر هیچ کلید Supabase نمی‌بیند.

### اندازه‌گیری واقعی با کلید ناشناس (anon)

```
تابع فروشگاه            → داده‌ی عمومی برگشت ✅
جدول storefronts مستقیم → []  ✅
جدول products مستقیم    → []  ✅

کلیدهای برگشتی کالاها:
  product_id, name, category, image_url, price, in_stock
کلیدهای حساس نشتی: هیچ ✅
```

`purchase_price`، `cost_price` و `stock_qty` عددی **در خروجی وجود ندارند**.
موجودی فقط بولین است: مشتری باید بداند «هست یا نیست»، نه اینکه «۳ تا مانده» —
آن اطلاعات تجاری فروشگاه است.

### گاردهایی که با اجرای واقعی آزموده شدند

| سناریو | نتیجه |
|---|---|
| فروشگاه منتشرنشده | صفر ردیف ✅ |
| `show_prices = false` | هر سه قیمت `null` ✅ |
| کسب‌وکار **معلق** | صفحه ناپدید شد ✅ |
| نشانی ناموجود | ۴۰۴ ✅ |
| کلید Supabase در HTML | هیچ ✅ |

گارد تعلیق مهم است: بدون آن، تعلیق یک حساب فقط دسترسی *خودش* را می‌بست و
ویترین عمومی‌اش بالا می‌ماند.

**پیش‌فرض همه‌چیز خاموش است** — هم انتشار، هم نمایش قیمت. عمومی‌شدن ناخواسته‌ی
فهرست کالا و نشانی، خطایی است که پس گرفته نمی‌شود.

---

## 🔴 باگی که فقط با بازدید واقعی پیدا شد

صفحه ساخته شده بود، تابع دیتابیس درست جواب می‌داد، و **۴۱ تست سبز بودند**. ولی
وقتی به‌عنوان بازدیدکننده‌ی خارج‌شده باز کردم:

```
URL: /shop/zz-test-shop  →  منتقل شد به  /login
```

`middleware` فهرست ثابتی از مسیرهای عمومی دارد با **تطبیق دقیق**، و
`/shop/<slug>` هرگز در آن نمی‌گنجید. یعنی صفحه‌ی «عمومی» عملاً خصوصی بود.

نه `tsc` و نه `next build` چنین چیزی را نمی‌گیرند — هیچ‌کدام مسیر واقعی درخواست
را اجرا نمی‌کنند. سه تست تازه اضافه شد تا تکرار نشود.

---

## چه چیزی ساخته شد

**صفحه‌ی عمومی** — سربرگ با نام و شعار، دکمه‌های تماس (تلفن، واتس‌اپ،
اینستاگرام، تلگرام)، کارت نشانی و ساعت کاری، و ویترین کالا با **موجودها اول**.

**تنظیمات** در `تنظیمات ← صفحه‌ی فروشگاه`: کلید انتشار، نشانی با اعتبارسنجی
زنده، دکمه‌ی کپی لینک، و کلید جداگانه‌ی نمایش قیمت.

### چند تصمیم ریز

- **نشانی فقط لاتین.** حروف فارسی در URL به percent-encoding تبدیل می‌شوند و
  لینکی که در بیو اینستاگرام گذاشته می‌شود زشت و شکننده می‌شود.
- **الگوی نشانی در TypeScript و قید دیتابیس یکی است** — یک تست این را تضمین
  می‌کند. اگر جدا بیفتند، فرم چیزی را می‌پذیرد که دیتابیس رد می‌کند.
- **نشانی‌های رزروشده** (`admin`, `api`, `settings`…) گرفته نمی‌شوند.
- **واتس‌اپ خودکار کد کشور می‌گیرد**: `09121234567` → `989121234567`، چون
  `wa.me` بدون آن برای کاربر خارج از ایران کار نمی‌کند.
- **اینستاگرام از هر قالبی** (`@name`، آدرس کامل، با پارامتر) به نام خالص
  نرمال می‌شود.
- **۴۰۴ برای منتشرنشده** — نباید فرق «وجود ندارد» با «هست ولی خصوصی است» را لو
  بدهیم، وگرنه با حدس‌زدن نشانی می‌شود فهمید چه کسی مشتری ماست.
- **`/shop` در robots باز است** (کل هدفش دیده‌شدن است) ولی `/settings` مسدود.

---

## راستی‌آزمایی تست‌ها

**پنج مورد عمداً برگردانده شد و تست شکست:**

| باگ برگردانده‌شده | تستی که گرفت |
|---|---|
| افزودن `purchase_price` به خروجی | `🔴 تابع کالاها هیچ ستون حساسی برنمی‌گرداند` |
| حذف گارد کسب‌وکار معلق | `🔴 کسب‌وکار معلق صفحه‌ی زنده ندارد` |
| تبدیل صفحه به کامپوننت کلاینت | `کامپوننت کلاینت نیست` |
| روشن‌کردن پیش‌فرض انتشار | `پیش‌فرض انتشار خاموش است` |
| جداکردن الگوی slug از دیتابیس | `🔴 الگوی TypeScript با قید دیتابیس یکی است` |

یک تست خودم هم اشتباه بود: انتظار داشتم `UPPER` رد شود، ولی رفتار درست
پذیرفتن و کوچک‌کردن است — کاربری که ناخواسته Caps Lock داشته نباید خطا بگیرد.
تست را به سنجش رفتار درست تبدیل کردم، نه اینکه کد را به تست تحمیل کنم.

---

## وضعیت نهایی

```
tsc --noEmit   پاک
vitest         ۹۶۸ / ۹۶۸  (۳۷ فایل) — از ۹۲۴
next build     Compiled successfully
axe-core       صفر ایراد در صفحه‌ی عمومی و تنظیمات × ۳ اندازه = ۶ ترکیب
سرریز افقی     هیچ‌کدام
خطای کنسول     صفر
کلید نشتی      هیچ
```

**داده‌ی شما دست‌نخورده:** ۴ کسب‌وکار · ۲۶ فاکتور · ۳۸۳ کالا · ۵۴۹ شخص ·
۷۵۲ گردش انبار. فروشگاه آزمایشی ساخته و پس از تست حذف شد؛ وضعیت همه‌ی
سازمان‌ها `approved` است (در تست تعلیق موقت انجام و بلافاصله بازگردانده شد).

---

## صادقانه: چه چیزی نیست

- **سبد خرید و فروش آنلاین نیست.** این یک ویترین است، نه فروشگاه اینترنتی.
  مشتری کالا را می‌بیند و تماس می‌گیرد.
- **تصویر کالا نمایش داده نمی‌شود** چون `image_url` تقریباً همه‌ی کالاهای شما
  خالی است. ستونش آماده است؛ به‌محض آپلود عکس کار می‌کند.
- **دامنه‌ی اختصاصی نیست** — نشانی فعلاً `tarazooapp.vercel.app/shop/...` است.
  گفتید «دامنه ندارم هنوز».
- **آمار بازدید ندارد.**

## باقی‌مانده

⬜ تحلیل صنفی در سایت معرفی (مورد دوم کامیکس) · ⬜ نمودارهای جذاب‌تر
⬜ داشبورد بر اساس بیشترین استفاده · ⬜ اپ اندروید (PWA اول)
⬜ ویرایش قلم‌به‌قلم فاکتور توسط پشتیبانی · ⬜ پرچم قابلیت
⬜ ورود فاکتورها و تراکنش‌های گذشته از اکسل
