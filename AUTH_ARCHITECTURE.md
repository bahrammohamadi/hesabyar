# معماری سیستم ورود و احراز هویت

نسخه‌ی سند: ۱ · ۲۵ مرداد ۱۴۰۵

این سند **پیش از** پیاده‌سازی نوشته شد تا تصمیم‌ها و دلیلشان ثبت
بماند. هر بخش «چرا این و نه آن» دارد.

---

## ۱) وضعیت موجود — آنچه واقعاً هست

اندازه‌گیری‌شده، نه حدس:

| لایه | وضعیت | جای پیاده‌سازی |
|---|---|---|
| ورود با رمز | ✅ از روت سرور | `app/api/auth/login/route.ts` |
| کندسازی نمایی per-account | ✅ ۵ تلاش آزاد، بعد ۲→۴→۸… سقف ۱۵ دقیقه | `record_login_failure` (۰۰۳۲) |
| محدودیت per-IP | ✅ دو لایه (Edge + روت) | `middleware.ts` + `rate-limit.ts` |
| جلوگیری از user enumeration | ✅ پیام و کد یکسان | روت ورود |
| محافظت مسیرها | ✅ allowlist صریح | `lib/supabase/middleware.ts` |
| مدیریت نشست‌های فعال | ✅ فهرست + بستن | `v_user_sessions` (۰۰۳۲) |
| تغییر رمز | ✅ | `app/api/account/password` |
| نقش‌ها و دسترسی | ✅ RLS + `has_permission` | ۰۰۰۶ |

**این پایه سالم است و بازنویسی نمی‌شود.**

## ۲) شکاف‌های واقعی

| # | شکاف | شدت | شاهد |
|---|---|---|---|
| ۱ | **بازیابی رمز وجود ندارد** | 🔴 بحرانی | FAQ: «با پشتیبانی تماس بگیرید» · صفحه‌ی ورود لینک «فراموشی» ندارد |
| ۲ | **ورود دومرحله‌ای نیست** | 🔴 بالا | صفر ارجاع به mfa/totp در کل کد |
| ۳ | **سابقه‌ی ورود ثبت نمی‌شود** | 🟠 متوسط | فقط شکست‌ها ثبت می‌شوند؛ ورود موفق هیچ ردی ندارد |
| ۴ | **رمز ضعیف مجاز است** | 🟠 متوسط | `password_min_length = 6`، بدون الزام نویسه |
| ۵ | **نشست بی‌پایان** | 🟡 پایین | خروج خودکار پس از بی‌کاری وجود ندارد |

### محدودیت‌های سکو — اندازه‌گیری‌شده

```
mfa_totp_enroll_enabled  = True    ✅ TOTP رایگان در دسترس است
smtp_host                = None    🔴 SMTP اختصاصی تنظیم نشده
rate_limit_email_sent    = 2       🔴 دو ایمیل در ساعت برای کل پروژه
password_min_length      = 6
jwt_exp                  = 3600    (توکن دسترسی یک ساعته)
refresh_token_rotation   = True    ✅
```

🔴 **`rate_limit_email_sent = 2` معماری را تعیین می‌کند.**
اگر بازیابی فقط با ایمیل بود، سومین کاربری که در یک ساعت رمزش را
فراموش می‌کرد هیچ ایمیلی نمی‌گرفت — و **دلیلش را هم نمی‌فهمید**.

🔴 **۴ از ۶ کاربر فعلی ایمیل ساختگی `@hesabyar.app` دارند.**
اینها با شماره‌ی موبایل ثبت‌نام کرده‌اند و آدرسشان صندوق واقعی نیست.
بازیابی با ایمیل برایشان **از اساس ناممکن** است.

**نتیجه: مسیر «کد بازیابی از مدیر» یک ضرورت است نه یک تجمل.**

---

## ۳) معماری هدف

### ۳-۱) سه مسیر بازیابی رمز

```
┌─ کاربر روی «رمزم را فراموش کرده‌ام» می‌زند
│
├─ الف) ایمیل واقعی دارد؟
│     └→ لینک بازیابی Supabase           [ساخته می‌شود]
│        ⚠️ سقف ۲ ایمیل در ساعت
│
├─ ب) ایمیل ساختگی @hesabyar.app دارد؟
│     └→ پیام صریح: «حسابت با شماره ساخته شده؛
│        از مدیر مجموعه کد بگیر»          [ساخته می‌شود]
│
└─ ج) مدیر از پنل کد یک‌بارمصرف می‌سازد     [ساخته می‌شود]
      └→ کاربر کد ۸ رقمی + رمز جدید وارد می‌کند
         اعتبار ۳۰ دقیقه · یک‌بار مصرف · هش‌شده ذخیره می‌شود

پیامک → مرحله‌ی بعد، وقتی سرویس وصل شد. جای اتصالش
        از الان در کد باز گذاشته می‌شود.
```

**چرا کد ۸ رقمی و نه ۶؟**
کد ۶ رقمی ۱۰⁶ حالت دارد. با اعتبار ۳۰ دقیقه و بدون محدودیت نرخ،
حدس‌زدنش شدنی است. ۸ رقم یعنی ۱۰⁸ حالت، به‌علاوه‌ی حداکثر ۵ تلاش.

**چرا هش‌شده ذخیره می‌شود؟**
اگر کسی به دیتابیس دسترسی خواندنی پیدا کند (نشت پشتیبان، اشتباه در
RLS)، کد خام یعنی تصاحب فوری حساب. هش یعنی کد فقط در همان لحظه که
مدیر می‌بیند ارزش دارد.

**چرا خودِ مدیر رمز را عوض نکند؟**
می‌تواند (`/api/admin/users/password` هست). ولی آن‌وقت مدیر **رمز
کاربر را می‌داند** — و همان مدیر بعداً می‌تواند به‌جای او سند مالی
ثبت کند و انکارناپذیری از بین می‌رود. با کد یک‌بارمصرف، رمز نهایی را
فقط خود کاربر می‌داند.

### ۳-۲) ورود دومرحله‌ای (TOTP)

اختیاری، به انتخاب کاربر. استاندارد TOTP یعنی با Google
Authenticator، Authy، و ۱Password کار می‌کند.

**چرا TOTP و نه پیامک؟**
- پیامک هزینه‌ی جاری دارد و سرویسش وصل نیست
- SIM swap یک حمله‌ی واقعی است؛ NIST از ۲۰۱۶ پیامک را توصیه نمی‌کند
- TOTP آفلاین کار می‌کند — مهم برای فروشنده‌ای که اینترنت ضعیف دارد

**کدهای پشتیبان:** ۱۰ کد یک‌بارمصرف هنگام فعال‌سازی. بدون آن‌ها،
گم‌شدن گوشی یعنی از دست رفتن کامل حساب.

⚠️ `mfa_allow_low_aal = False` یعنی وقتی کاربر TOTP دارد، نشست بدون
تأیید عامل دوم سطح دسترسی کامل نمی‌گیرد. این پیش‌فرض درست است.

### ۳-۳) سابقه‌ی ورود

جدول `login_events`: هر ورود موفق و ناموفق با زمان، IP، دستگاه.

**چرا جدا از `login_attempts`؟**
آن جدول یک **شمارنده‌ی حالت** است — با ورود موفق پاک می‌شود. سابقه
باید بماند. دو مسئولیت متفاوت، دو جدول.

**چرا مهم است:** کاربر باید بتواند ببیند «دیروز ساعت ۳ شب از یک IP
ناشناس وارد شدند». بدون سابقه، نفوذ هرگز کشف نمی‌شود.

### ۳-۴) سیاست رمز عبور

حداقل ۸ نویسه + بررسی رمزهای رایج. سنجش قدرت **در سمت کلاینت** نشان
داده می‌شود ولی **الزام در سمت سرور** است.

**چرا الزام سمت سرور؟** هر چیزی که فقط در مرورگر چک شود، با یک
درخواست مستقیم به API دور زده می‌شود.

**چرا «رمزهای رایج» و نه قواعد پیچیده؟**
NIST 800-63B صراحتاً الزام «حتماً حرف بزرگ و علامت» را **توصیه
نمی‌کند** چون کاربر را به `Password1!` می‌رساند که ضعیف است. به‌جایش
فهرست رمزهای لو‌رفته چک می‌شود.

### ۳-۵) خروج خودکار پس از بی‌کاری

پیش‌فرض خاموش. قابل تنظیم توسط مالک سازمان.

**چرا خاموش به‌صورت پیش‌فرض؟** صندوق‌دار مغازه کل روز پای سیستم است؛
خروج خودکار برایش آزار است نه امنیت. ولی حسابدار که روی لپ‌تاپ
مشترک کار می‌کند لازمش دارد.

---

## ۴) تصمیم‌های معماری

### چه چیزی را **نمی‌سازیم** و چرا

| گزینه | چرا نه |
|---|---|
| جایگزینی Supabase Auth با سیستم خودمان | مدیریت رمز و نشست جایی است که خطاهای امنیتی کشنده‌اند. Supabase این را درست پیاده کرده و آزموده شده. |
| قفل کامل حساب پس از N تلاش | OWASP توصیه نمی‌کند: خودش ابزار حمله می‌شود. مهاجم با ۵ رمز غلط حساب رقیب را قفل می‌کند. تأخیر نمایی از قبل هست. |
| ورود با گوگل | کاربر هدف (مغازه‌دار ایرانی) اغلب حساب گوگل کاری ندارد؛ و دسترسی به گوگل پایدار نیست. |
| Magic link به‌عنوان روش اصلی | همان سقف ۲ ایمیل در ساعت. |
| WebAuthn / کلید سخت‌افزاری | `mfa_web_authn_enroll_enabled = False` و مخاطب ما کلید سخت‌افزاری ندارد. |

### قواعد سختی که رعایت می‌شوند

۱. **هیچ پاسخی نگوید حساب وجود دارد یا نه.** «اگر این حساب وجود
   داشته باشد، ایمیل فرستاده شد» — چه وجود داشته باشد چه نه.

۲. **هر عملیات حساس محدودیت نرخ دارد**، حتی وقتی به‌نظر بی‌خطر است.
   درخواست بازیابی بدون سقف = ابزار اسپم رایگان.

۳. **کد و توکن هرگز خام ذخیره نمی‌شوند** — فقط هش.

۴. **تغییر رمز همه‌ی نشست‌های دیگر را می‌بندد.** اگر کسی نفوذ کرده،
   عوض‌کردن رمز باید بیرونش کند؛ وگرنه چه فایده‌ای دارد.

۵. **هر رویداد امنیتی ثبت می‌شود** — حتی ناموفق‌ها.

---

## ۵) مدل داده

```sql
-- کد بازیابی صادرشده توسط مدیر
password_reset_codes(
  id, org_id, user_id,
  code_hash      text     -- 🔴 هش، نه خام
  expires_at     timestamptz,
  used_at        timestamptz,
  attempt_count  int,     -- سقف ۵
  created_by     uuid     -- کدام مدیر صادر کرد
)

-- سابقه‌ی ورود (جدا از شمارنده)
login_events(
  id, user_id, login_id,
  event          text,    -- success | failure | locked | reset | mfa_fail
  ip, user_agent,
  created_at
)

-- تنظیمات امنیتی هر سازمان
settings.key = 'security'
  { idle_timeout_minutes, require_mfa_for_owner }
```

---

## ۶) ترتیب اجرا

| گام | چه چیزی | چرا این ترتیب |
|---|---|---|
| ۱ | سیاست رمز + سابقه‌ی ورود | پایه‌ای؛ بقیه به سابقه نیاز دارند |
| ۲ | بازیابی: ایمیل + مسیر مدیر | بحرانی‌ترین شکاف |
| ۳ | ورود دومرحله‌ای TOTP | مستقل |
| ۴ | خروج خودکار + صفحه‌ی امنیت حساب | جمع‌بندی |
| ۵ | پیامک | وقتی سرویس وصل شد |

---

## ۷) آنچه پس از این هم نخواهیم داشت

صادقانه، تا وقتی موارد زیر حل نشود:

- **SMTP اختصاصی** → سقف ۲ ایمیل در ساعت باقی می‌ماند. برای رفعش
  باید یک سرویس مثل Resend یا SMTP خودمان وصل شود.
- **دامنه‌ی اختصاصی** → ایمیل از `@supabase.co` می‌رود که در
  صندوق اسپم می‌افتد.
- **Redis مشترک** → محدودیت نرخ درون‌حافظه‌ای روی چند instance
  دقیق نیست (در `SECURITY_RUNBOOK.md` هم آمده).
