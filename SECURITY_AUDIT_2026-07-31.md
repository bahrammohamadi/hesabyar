# گزارش ممیزی امنیتی و آمادگی تولید — ترازو

**تاریخ:** ۱۴۰۵/۰۵/۰۹ (2026-07-31)
**دامنه‌ی بررسی:** ۲۱٬۹۵۳ خط کد · ۴ روت API · ۲۳ مایگریشن · ۴۴ جدول
**روش:** تست واقعی روی محیط production و دیتابیس زنده — نه بازبینی صرف کد

> هر یافته با یک دستور اجراشده اثبات شده است. یافته‌های «تئوریک» جدا علامت خورده‌اند.

---

## خلاصه‌ی مدیریتی

| شدت | یافته | وضعیت |
|---|---|---|
| 🔴 بحرانی | کلید `service_role` نشتی در تاریخچه‌ی گیت — **زنده** | ❌ باز (نیاز به تصمیم شما) |
| 🔴 بحرانی | نشت ۳۲۶ ردیف داده‌ی انبار به اینترنت بدون احراز هویت | ✅ رفع و تأیید شد |
| 🟠 بالا | شماره‌ی فاکتور تکراری (race condition + نبود قید یکتایی) | ✅ رفع و تأیید شد |
| 🟠 بالا | ارتقای سطح دسترسی از طریق `permissions[]` دلخواه | ✅ رفع شد |
| 🟠 بالا | IDOR در تغییر مجوز کاربرِ سازمان دیگر | ✅ رفع شد |
| 🟠 بالا | نبود کامل rate limiting (brute-force آزاد) | ✅ رفع و تأیید شد |
| 🟡 متوسط | نبود کامل هدرهای امنیتی | ✅ رفع و تأیید شد |
| 🟡 متوسط | نشت پیام خطای داخلی دیتابیس به کلاینت | ✅ رفع شد |
| 🟡 متوسط | انتخاب غیرقطعی سازمان برای کاربران چندسازمانی | ✅ رفع شد |
| 🟡 متوسط | رمز عبور ضعیف `123456` روی دو حساب واقعی | ❌ باز (تصمیم شما) |
| 🟡 متوسط | ۱٬۰۷۲ ردیف PII در تاریخچه‌ی گیت | ❌ باز (نیاز به بازنویسی تاریخچه) |

---

## ۱. 🔴 بحرانی — کلید `service_role` نشتی هنوز زنده است

**وضعیت: رفع نشده. مهم‌ترین ریسک باقی‌مانده.**

### شرح
کلید `service_role` در کامیت `cf4b443` (۱ جولای) وارد تاریخچه‌ی گیت شده و هنوز معتبر است. این کلید **تمام RLS را دور می‌زند**.

### اثبات (اجراشده در همین ممیزی)
```bash
K=$(git show 4763219:health-check.js | grep -oE "eyJ[A-Za-z0-9_.-]{40,}" | head -1)
curl "https://niuajsppobtgcefapsaw.supabase.co/rest/v1/contacts?select=id,name,phone&limit=3" \
  -H "apikey: $K" -H "Authorization: Bearer $K"
```
**نتیجه:** `HTTP 200` — سه رکورد واقعی برگشت، شامل «مائده فلاح» و شماره‌ی موبایل `091185***`.
**انقضا:** سال ۲۰۳۶ · **نقش:** `service_role`

### سناریوی حمله
هر کسی که مخزن را clone کند (یا اگر مخزن روزی public شود)، با یک دستور به **همه‌ی ۵۴۳ مخاطب با شماره موبایل، ۳۸۲ محصول و کل سوابق مالی** دسترسی خواندن و **نوشتن و حذف** دارد. RLS هیچ محافظتی نمی‌کند.

### تأثیر کسب‌وکاری
افشای کامل پایگاه مشتریان، امکان دستکاری یا حذف کل داده‌های مالی، نقض حریم خصوصی ۵۴۳ نفر.

### راه‌حل
مسیر جایگزینی بدون قطعی در `SECURITY_RUNBOOK.md` مستند شده. کلیدهای جدید (`sb_secret_…` و `sb_publishable_…`) از قبل ساخته و تست شده‌اند.

**چرا انجام ندادم:** قبلاً گفتید «فعلاً هیچ تغییری نده». چون این کار روی سرویس زنده اثر می‌گذارد، بدون تأیید صریح شما اجرا نمی‌کنم.

---

## ۲. 🔴 بحرانی — نشت ۳۲۶ ردیف داده به اینترنت ✅ رفع شد

### شرح
جدول `stock_reset_snapshots` بدون RLS بود و با کلید عمومی `anon` از اینترنت قابل خواندن.

### اثبات قبل از رفع
```bash
curl "$SUPABASE_URL/rest/v1/stock_reset_snapshots?select=*" -H "apikey: $ANON_KEY"
# HTTP 200 · content-range: 0-325/326
```
داده‌ی برگشتی شامل: `product_name`، `stock_before`، `org_id`، یادداشت فارسی مالک.

دو جدول دیگر هم بدون RLS بودند: `avahang_genres` و `audit_logs` (RLS روشن ولی صفر policy).

### سناریوی حمله
کلید `anon` در سورس فرانت‌اند هر بازدیدکننده‌ای قابل مشاهده است. مهاجم بدون هیچ حسابی، فهرست کالاها و سطح موجودی رقیب را استخراج می‌کند.

### رفع (مایگریشن `0023`)
```sql
alter table public.stock_reset_snapshots enable row level security;
create policy stock_reset_snapshots_org_isolation
  on public.stock_reset_snapshots for select to authenticated
  using (org_id in (select m.org_id from public.memberships m
                    where m.user_id = auth.uid() and m.is_active));
```

### تأیید پس از رفع
```
stock_reset_snapshots → 0 ردیف   (قبلاً ۳۲۶)
avahang_genres        → 0 ردیف
document_counters     → 0 ردیف
```
داده‌ها حذف نشدند — `select count(*)` با دسترسی مدیر همچنان ۳۲۶ برمی‌گرداند. فقط دسترسی ناشناس بسته شد.

---

## ۳. 🟠 بالا — شماره‌ی فاکتور تکراری ✅ رفع شد

### شرح
```sql
-- پیاده‌سازی قبلی
select count(*) into v_count from public.sales where org_id = p_org;
return p_prefix || '-' || lpad((v_count + 1)::text, 5, '0');
```
دو نقص:
1. **رقابت همزمانی:** دو صندوق‌دار همزمان → هر دو `count` یکسان می‌خوانند → یک شماره.
2. **حذف فاکتور:** `count` کم می‌شود → شماره‌ی قبلی دوباره تولید می‌شود.

`invoice_no` هیچ `UNIQUE` نداشت (تأیید شده با `pg_constraint`)، پس دیتابیس هم جلویش را نمی‌گرفت.

### سناریوی حمله / خطا
لازم نیست مهاجم باشد — دو فروش همزمان کافی است. دو فاکتور با شماره‌ی `F-00021` صادر می‌شود؛ در گزارش مالیاتی و دفاتر، یکی از فروش‌ها ناپدید یا دوبار شمرده می‌شود.

### تأثیر
مغایرت دفاتر، مشکل ممیزی مالیاتی، امکان انکار فروش توسط مشتری.

### رفع
جدول شمارنده‌ی مستقل با قفل سطری اتمیک:
```sql
insert into public.document_counters (org_id, doc_type, last_value)
values (p_org, v_type, 1)
on conflict (org_id, doc_type) do update
  set last_value = public.document_counters.last_value + 1
returning last_value into v_next;
```
به‌علاوه ایندکس یکتای جزئی روی `(org_id, invoice_no)` به‌عنوان خط دفاع آخر.

### چرا این پیاده‌سازی بهتر است
- `UPDATE ... RETURNING` قفل سطری می‌گیرد؛ تراکنش دوم منتظر می‌ماند و عدد بعدی را می‌گیرد.
- شمارنده از `max(شماره‌های موجود)` مقداردهی شد، نه `count` — پس حذف فاکتور دیگر شماره را برنمی‌گرداند.
- قید یکتایی مشروط ساخته شد تا اگر داده‌ی تکراری وجود داشت، مایگریشن خراب نشود.

### تأیید
```
next_invoice_no(سازمان تست) → F-00001, F-00002, F-00003   ✅ بدون تکرار
شمارنده سازمان اصلی = 20  → فاکتور بعدی F-00021           ✅ ادامه‌ی درست
```

---

## ۴. 🟠 بالا — ارتقای سطح دسترسی و IDOR در `/api/admin/users` ✅ رفع شد

### الف) ارتقای عمودی از طریق `permissions[]`
```ts
// قبل
const permissions = Array.isArray(body.permissions) ? body.permissions.map(String) : [];
await svc.auth.admin.createUser({ app_metadata: { permissions } });
```
هیچ اعتبارسنجی‌ای نبود. یک `manager` می‌توانست `["*"]` بفرستد و مجوز کامل بگیرد.

**رفع:** فهرست سفید `ASSIGNABLE_PERMISSIONS` (۱۶ مجوز مجاز، `*` عمداً نیست) + سقف ۳۲ آیتم.

### ب) IDOR افقی در `PATCH`
```ts
// قبل — user_id مستقیم از بدنه، بدون بررسی سازمان
if (permissions && userId) {
  await svc.auth.admin.updateUserById(userId, { app_metadata: { permissions } });
}
```
مهاجم `user_id` کاربری از سازمان دیگر را می‌فرستاد و مجوزهایش را بازنویسی می‌کرد.

**رفع:** ابتدا ردیف عضویت با `.eq("org_id", ...)` واکشی می‌شود؛ سپس مجوز روی `target.user_id` اعمال می‌شود، نه ورودی کاربر.

### ج) سه محافظ اضافه
- فقط `owner` می‌تواند نقش `owner` بدهد یا بسازد.
- مالک نمی‌تواند حساب خودش را غیرفعال کند (جلوگیری از قفل‌شدن سازمان).
- اگر ثبت عضویت شکست بخورد، کاربر ساخته‌شده حذف می‌شود (حساب یتیم باقی نمی‌ماند).

---

## ۵. 🟠 بالا — نبود rate limiting ✅ رفع و تأیید شد

### اثبات قبل از رفع
```bash
# ۱۲ تلاش ورود ناموفق پشت سر هم
400 400 400 400 400 400 400 400 400 400 400 400
```
هیچ قفل‌شدن، هیچ تأخیر، هیچ `429`.

### نکته‌ی مهم — اولین تلاش شکست خورد
ابتدا rate limit را داخل خود روت‌ها گذاشتم. تست واقعی نشان داد **اصلاً کار نمی‌کند**:
```bash
# ۸۰ درخواست موازی به /api/activity
80 × 401   ← هیچ 429 نبود
```
دلیل: روی Vercel هر فراخوانی serverless ماژول تازه بارگذاری می‌کند، پس شمارنده‌ی درون‌حافظه‌ای همیشه از صفر شروع می‌شد.

**راه‌حل:** انتقال به `middleware.ts` که روی Edge اجرا می‌شود و instanceهایش بازاستفاده می‌شوند.

### تأیید پس از رفع
```bash
# ۲۰ درخواست POST به /login
405 405 405 405 405 405 405 405 405 405 429 429 429 429 429 429 429 429 429 429
```
دقیقاً پس از ۱۰ درخواست، مسدود شد. ✅

**سقف‌های اعمال‌شده:** ورود/ثبت‌نام `POST` → ۱۰ در ۵ دقیقه (مسدودی ۱۵ دقیقه) · سایر APIها → ۱۲۰ در دقیقه · ساخت کاربر → ۱۰ در ۵ دقیقه · گزارش فروشندگان → ۲۰ در دقیقه.

### ⚠️ محدودیت صادقانه
این راهکار درون‌حافظه‌ای است و در مقیاس چند-instance «کامل» نیست. برای محافظت قطعی باید Vercel Firewall یا Upstash Redis اضافه شود. rate limit سمت Supabase Auth هم باید جداگانه در داشبورد فعال شود.

---

## ۶. 🟡 متوسط — نبود کامل هدرهای امنیتی ✅ رفع و تأیید شد

### قبل
```
content-security-policy   ❌ غایب
x-frame-options           ❌ غایب
x-content-type-options    ❌ غایب
referrer-policy           ❌ غایب
permissions-policy        ❌ غایب
x-powered-by: Next.js     ← نسخه فریم‌ورک لو می‌رفت
```

بدون `X-Frame-Options`، هر سایتی می‌توانست پنل شما را در `iframe` نامرئی بگذارد و کاربر واردشده را فریب دهد تا ناخواسته «حذف مشتری» را کلیک کند (clickjacking).

### بعد (تأیید شده روی سایت زنده)
```
content-security-policy   default-src 'self'; frame-ancestors 'none'; …
x-frame-options           DENY
x-content-type-options    nosniff
referrer-policy           strict-origin-when-cross-origin
permissions-policy        camera=(), microphone=(), geolocation=(), …
x-powered-by              ❌ حذف شد
```
به‌علاوه `Cache-Control: no-store` روی `/api/*` تا پاسخ‌های حاوی داده‌ی مالی در CDN کش نشوند.

**نکته:** CSP فعلاً `'unsafe-inline'` و `'unsafe-eval'` برای اسکریپت دارد، چون Next.js اسکریپت bootstrap درون‌خطی تزریق می‌کند. حذف کامل آن نیازمند پیاده‌سازی nonce است — گام بعدی پیشنهادی.

---

## ۷. 🟡 متوسط — نشت پیام خطای داخلی ✅ رفع شد

```ts
// قبل — در هر ۴ روت
catch (error) {
  return NextResponse.json({ error: (error as Error).message }, { status: 500 });
}
```
پیام خطای Postgres مستقیم به کلاینت می‌رفت و نام جدول/ستون را لو می‌داد. در همین ممیزی از این نشت برای کشف نام ستون‌ها استفاده کردم (`column contacts.mobile does not exist`).

**رفع:** تابع `safeError()` — خطای کامل در لاگ سرور با شناسه‌ی ۸ رقمی، پیام عمومی برای کلاینت به همراه همان شناسه برای پیگیری.

---

## ۸. 🟡 متوسط — انتخاب غیرقطعی سازمان ✅ رفع شد

```ts
// قبل
.eq("is_active", true).limit(1).single()   // بدون order!
```
برای کاربری که در دو سازمان عضو است، Postgres تضمینی برای ترتیب نمی‌دهد. ممکن بود داده‌ی سازمان اشتباه نمایش داده شود.

**رفع:** `order("created_at", ascending)` + پارامتر اختیاری `org_id` که اعتبارسنجی می‌شود عضو بودن کاربر را (جلوگیری از IDOR).

**وضعیت فعلی داده‌ی شما:** هر ۴ کاربر فقط یک عضویت دارند، پس این باگ هنوز بروز نکرده بود.

---

## ۹. مواردی که بررسی شد و **مشکلی نداشت** ✅

| مورد | نتیجه |
|---|---|
| SQL Injection | تمام کوئری‌ها از طریق Supabase client پارامتری‌اند. صفر رشته‌ی SQL پویا. |
| XSS | صفر مورد `dangerouslySetInnerHTML`، `innerHTML`، `eval`، `new Function`. React به‌صورت پیش‌فرض escape می‌کند. |
| توکن در localStorage | فقط تم و حالت شب ذخیره می‌شود. توکن نشست در کوکی `httpOnly` است. ✅ |
| CORS | هیچ هدر `Access-Control-Allow-Origin` بازی وجود ندارد. |
| RLS | ۴۲ جدول از ۴۴ جدول RLS فعال داشتند (۲ مورد رفع شد). |
| ایندکس‌گذاری | ۲۰ ایندکس روی `org_id`؛ همه‌ی جدول‌های پرترافیک پوشش دارند. |
| توابع مالی | `create_sale`، `cancel_sale` و بقیه `SECURITY DEFINER` با `search_path` ثابت‌اند. |
| آپلود فایل | هیچ مسیر آپلودی در پروژه وجود ندارد — سطح حمله صفر. |
| فاکتور تکراری موجود | `group by invoice_no having count(*)>1` → صفر ردیف. |

---

## ۱۰. وابستگی‌ها

```
npm audit --omit=dev → 2 high (next, postcss)
npm install          → 33 total (1 critical, 20 high) — همه dev-only از طریق vercel CLI
```
`next@14.2.35` آخرین پچ شاخه‌ی ۱۴ است. دو مورد باقی‌مانده فقط با ارتقا به Next 16 (تغییر شکننده) رفع می‌شوند و هر دو DoS هستند، نه اجرای کد.

---

## کارهای باقی‌مانده (به ترتیب اولویت)

| # | کار | چرا | نیاز به تأیید شما |
|---|---|---|---|
| ۱ | **چرخش کلید `service_role`** | کلید نشتی زنده است و کل RLS را دور می‌زند | ✅ بله |
| ۲ | **تغییر رمز `123456`** | صفحه‌ی ورود عمومی است، ۵۴۳ شماره موبایل در خطر | ✅ بله |
| ۳ | حذف PII از تاریخچه‌ی گیت | ۱٬۰۷۲ ردیف نام و موبایل؛ نیازمند `git filter-repo` | ✅ بله |
| ۴ | Redis/WAF برای rate limit | راهکار فعلی در مقیاس چند-instance کامل نیست | خیر |
| ۵ | CSP با nonce | حذف `unsafe-inline` و `unsafe-eval` | خیر |
| ۶ | لاگ ممیزی ورود ناموفق | تشخیص نفوذ | خیر |
| ۷ | پشتیبان‌گیری خودکار + تست بازیابی | هیچ پشتیبان تأییدشده‌ای وجود ندارد | خیر |

---

## ظرفیت و مقیاس‌پذیری

بر پایه‌ی ساختار فعلی (Supabase Free/Pro + Vercel Hobby):

| بار همزمان | ارزیابی |
|---|---|
| ۱۰۰ کاربر | ✅ بدون مشکل |
| ۵۰۰ کاربر | ✅ قابل قبول؛ گزارش فروشندگان کند می‌شود |
| ۱۰۰۰ کاربر | ⚠️ سقف اتصال Postgres تنگ می‌شود → نیاز به connection pooling |
| ۵۰۰۰+ کاربر | ❌ نیاز به: Redis برای کش و rate limit، صفحه‌بندی سمت سرور در گزارش‌ها، ارتقای پلن Vercel |

**گلوگاه اصلی شناسایی‌شده:** `/api/reports/sellers` تا ۵۰۰۰ ردیف فروش را می‌خواند و برای هر کاربر یکتا یک فراخوانی `getUserById` جدا می‌زند (الگوی N+1). با ۵۰ فروشنده یعنی ۵۰ رفت‌وبرگشت شبکه در یک درخواست. راه‌حل: کش کردن نگاشت کاربران یا نگهداری نام در جدول `memberships`.

---

*تهیه‌شده با اجرای واقعی دستورات روی محیط production و دیتابیس زنده. هر ادعا با خروجی دستور پشتیبانی می‌شود.*
