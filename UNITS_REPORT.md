# مرحله ۱ — واحد شمارش و کالای وزنی

نسخه‌ی زنده: `26.0816` · `18782ca` · ۲۵ مرداد ۱۴۰۵

---

## چرا این مرحله اول شد

فهرست کامل شکاف‌ها (۶ سراسری + ۲۳ صنفی = ۲۵ قابلیت مجزا) در
`GAPS_ROADMAP.md` نوشته شد. این مورد اول اجرا شد چون:

**ستون `qty` در ۹ جدول از نوع `integer` بود.** یعنی «۱٫۵ کیلو گوشت»
یا «۲۵۰ گرم آرد» اصلاً قابل ثبت نبود. این تغییر نوع ستون روی
جدول‌هایی با **۷۵۹ حرکت انبار واقعی** است — هرچه دیرتر، پرریسک‌تر.

**سه شکاف صنفی را هم‌زمان بست:**

| صنف | آنچه سایت می‌گفت نداریم | وضعیت |
|---|---|---|
| سوپرمارکت | ترازو و کالای وزنی | ✅ حذف شد |
| ابزار | قیمت‌گذاری بر اساس واحد فرعی (بسته/کارتن) | ✅ حذف شد |
| لوازم‌التحریر | فروش عمده با واحد بسته | ✅ حذف شد |

---

## چه ساخته شد

### واحد شمارش روی کالا

چهار نوع: **شمارشی** (عدد) · **وزنی** (کیلوگرم) · **حجمی** (لیتر) ·
**طولی** (متر). برچسب دلخواه هم می‌شود گذاشت («گرم»، «تخته»).

کالای شمارشی فقط عدد صحیح می‌پذیرد؛ بقیه تا **سه رقم اعشار** یعنی
دقت گرم.

### واحد فرعی (بسته/کارتن)

«کارتن ۱۲ عددی» تعریف می‌کنید. انبار همیشه واحد اصلی را می‌شمارد و
قیمت هر واحد از قیمت کارتن حساب می‌شود. راهنمای زنده زیر فیلد:

> هر کارتن برابر ۱۲ کیلوگرم

🔴 **چرا مهم است:** تأمین‌کننده «کارتن ۱۲تایی، ۲۴۰ هزار تومان» می‌دهد.
اگر همان ۲۴۰ هزار قیمت واحد گرفته شود، بهای تمام‌شده **۱۲ برابر**
می‌شود و گزارش سود کاملاً غلط از آب درمی‌آید.

### کادر تعداد قابل تایپ

🔴 پیش از این `QtyStepper` فقط دو دکمه‌ی + و − داشت و **هیچ راهی برای
تایپ نبود**:
- «۱٫۵ کیلو» غیرقابل ثبت
- «۲۴ عدد» یعنی بیست‌وچهار بار کلیک

---

## باگ‌هایی که حین کار پیدا شد

### 🔴 ۱) مهاجرت ۰۰۴۸ نیمه‌کاره بود — گرد شدن بی‌صدای مقدار

ستون‌ها `numeric` شدند ولی **چهار تابع مالی** هنوز مقدار را با
`(it->>'qty')::int` از JSON می‌خواندند. اندازه‌گیری روی دیتابیس زنده:

| ورودی کاربر | آنچه ثبت می‌شد | پس از رفع |
|---|---|---|
| ۱٫۵ کیلو × ۱۰۰٬۰۰۰ | **۲ کیلو** | ۱٫۵ → ۱۵۰٬۰۰۰ ✅ |
| ۰٫۲۵ کیلو × ۱۰۰٬۰۰۰ | **صفر** (کالا رایگان!) | ۰٫۲۵ → ۲۵٬۰۰۰ ✅ |

**بدترین بخش: بی‌صدا بود.** مبلغ از همان مقدار گردشده حساب می‌شد، پس
هیچ ناهماهنگی دیده نمی‌شد؛ فقط موجودی انبار با واقعیت فرق می‌کرد.

⚠️ تعاریف توابع از `pg_get_functiondef` گرفته شد نه از فایل مهاجرت.
این توابع در پنج مهاجرت پیاپی بازنویسی شده‌اند و آخرین نسخه فقط در
دیتابیس است؛ برداشتن نسخه‌ی قدیمی، تخفیف سطری و سرشکن هزینه‌ی حمل را
از بین می‌برد.

### 🔴 ۲) تریگر محافظ موجودی

Postgres اجازه‌ی تغییر نوع ستونی که در تعریف تریگر آمده نمی‌دهد:

```
cannot alter type of a column used in a trigger definition
DETAIL: trigger trg_guard_stock_qty depends on column "stock_qty"
```

این همان محافظی است که نمی‌گذارد کسی موجودی را مستقیم دستکاری کند.
اگر حذف می‌شد و برنمی‌گشت، موجودی برای همیشه از `stock_movements` جدا
می‌افتاد — **بی‌صدا**. حذف و بازسازی عیناً از `pg_get_triggerdef`.

### 🔴 ۳) یازده نما و چهار پرچم امنیتی

۱۱ نما به این ستون‌ها وابسته بودند و باید drop و بازساخته می‌شدند.
**چهارتاشان `security_invoker = true` دارند** — اگر آن گزینه از قلم
می‌افتاد، RLS دور زده می‌شد و هر کاربر داده‌ی سازمان‌های دیگر را
می‌دید. تعاریف از `pg_get_viewdef` گرفته شد نه دستی.

### ۴) موجودی اولیه‌ی کالای وزنی رد می‌شد

`normalizeStock` **همیشه** عدد صحیح می‌خواست و فقط می‌گفت «باید عدد
صحیح باشد» بدون آنکه بگوید چرا.

---

## اندازه‌گیری‌ها

```
تست‌ها          ۱۳۸۶ / ۱۳۸۶ (۵۱ فایل) — ۴۴ تای آن units
tsc --noEmit    پاک
build           موفق (محلی + Vercel)
axe-core        صفر ایراد — دسکتاپ، تبلت، موبایل — محلی و زنده
خطای JS         صفر
راستی‌آزمایی    ۹ باگ عمدی برگردانده شد، هر ۹ تا توسط تست گرفته شد
```

**آزمایش انتها به انتها روی مرورگر واقعی:**
`۱٫۵ کیلوگرم × ۱٬۵۹۰٬۰۰۰ = ۲٬۳۸۵٬۰۰۰ تومان` ✅ (دسکتاپ و موبایل)

**داده‌ی زنده دست‌نخورده — پیش و پس از مهاجرت:**
```
stock_movements 759 · مجموع مقدار 265.000
sale_items 40 · مجموع مقدار 45.000
موجودی کل 265.000 · products 383 · sales 30
هر ۱۱ نما برگشت · هر ۴ security_invoker حفظ شد
```

آزمایش‌های SQL داخل تراکنشی با `raise exception` پایانی انجام شد تا
هیچ رکوردی نماند.

---

## یک راستی‌آزمایی که خودش باگ داشت

یکی از ۹ باگ آزمایشی **گرفته نشد**. بررسی نشان داد سه کامپوننت در
`PosPieces.tsx` متغیر `draft` دارند و تست به `draft` دو کامپوننت دیگر
می‌خورد. تست به بدنه‌ی `QtyStepper` محدود شد و بعد باگ را گرفت.

اگر راستی‌آزمایی نمی‌کردم، یک تست سبزِ بی‌خاصیت داشتم.

---

## صادقانه: چه چیزی نیست

- **ترازوی متصل به رایانه** خوانده نمی‌شود. کاربر وزن را **دستی**
  وارد می‌کند. اتصال سریال/USB به ترازو نیاز به درایور بومی دارد که
  در مرورگر ممکن نیست.
- **پیش‌فاکتور** (`/sales/orders`) هنوز واحد را نشان نمی‌دهد — آن فرم
  پیاده‌سازی جدایی دارد و از `PosCartList` مشترک استفاده نمی‌کند.
- **سرشکن هزینه‌ی حمل «به نسبت تعداد»** برای کالای وزنی تقریبی است
  (`ceil` می‌شود). حالت پیش‌فرض «به نسبت ارزش» دقیق است.
- **ورود اکسل** ستون واحد ندارد؛ کالاهای واردشده شمارشی می‌مانند.
- **گزارش‌ها** مقدار را با واحد نشان نمی‌دهند، فقط عدد.

---

## مرحله‌ی بعد

طبق `GAPS_ROADMAP.md`: **تاریخ انقضا و سری ساخت** (سوپرمارکت +
داروخانه). به همین زیرساخت وابسته است چون حرکت انبار باید بداند از
کدام بچ خارج می‌شود.
