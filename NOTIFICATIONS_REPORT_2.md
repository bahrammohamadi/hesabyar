# گزارش: اعلان‌های واقعی و پوش روی گوشی

مهاجرت `0045_notifications.sql` · مستقر روی `08a219e`

---

## ۱. زنگوله پر از یادداشت انتشار بود

شمارش پیش از اصلاح:

```
۲۷ نسخه · ۱۰۹ تغییر
از این تعداد: ۳۴ رفع اشکال · ۴۲ قابلیت · ۲۱ بهبود · ۱۲ امنیتی
```

کاربر برای دیدن یک خبر مهم باید از ده تا «چسبیدن تاریخ در فهرست
پرداخت» رد می‌شد. نتیجه: زنگوله را باز نمی‌کرد.

**راه‌حل:** فیلد `important` روی یادداشت‌ها. پیش‌فرضش `false` است —
عمدی، چون اگر پیش‌فرض `true` بود، فراموش‌کاری دوباره همان انباشت را
می‌ساخت. الان **۴ نسخه از ۲۷** مهم علامت خورده‌اند.

بقیه گم نمی‌شوند؛ در تاریخچه می‌مانند و `unseenAll` همه را می‌دهد.

---

## ۲. اعلان از داده‌ی خودِ مجموعه

جدول `notifications` با چهار منبع:

| منبع | اولویت | قاعده |
|---|---|---|
| چک سررسیدگذشته | 🔴 بالا | عواقب قانونی دارد |
| چک تا ۳ روز آینده | 🔴 بالا | یک بار در روز |
| پیگیری CRM | 🔵 عادی | `next_followup` که خود کاربر گذاشته |
| بدهی معوق | 🔵 عادی | **فقط بیش از ۳۰ روز**، هفتگی نه روزانه |

دو تصمیم که از اشتباه قبلی آمدند:

- **مرز ۳۰ روز برای بدهی** — درس «موجودی کم» که ۳۶۱ از ۳۸۶ کالا را
  قرمز می‌کرد. نسیه‌ی ۱۰ روزه عادی است نه مشکل.
- **کلید هفتگی (`IYYY-IW`) برای بدهی** — بدهی ۶۰ روزه هر روز یادآوری
  لازم ندارد.

**ضد تکرار:** هر اعلان `dedupe_key` دارد و ایندکس یکتا ردیف دوم را رد
می‌کند. کاربر می‌تواند ده بار در روز داشبورد را باز کند بدون اینکه
اعلان تکراری بسازد.

**امنیت:** هیچ سیاست `insert` برای کاربر عادی نیست و
`push_notification` از `authenticated` هم revoke شده — وگرنه هر کسی
می‌توانست اعلان جعلی به همکارانش بفرستد.

روی داده‌ی زنده: **۵ اعلان واقعی** ساخته شد (بدهی معوق).

---

## ۳. پوش روی دستگاه

Web Push استاندارد با VAPID. سرویس‌ورکر `push` و `notificationclick`
می‌گیرد؛ کلیک، پنجره‌ی باز را جلو می‌آورد نه تب جدید.

### 🔴 محدودیت iOS که صادقانه گفته می‌شود

تحقیق تأیید کرد پوش وب روی آیفون فقط از **iOS 16.4** و **فقط وقتی
برنامه از صفحه‌ی اصلی باز شده باشد** کار می‌کند. در تب سافاری
`PushManager` اصلاً وجود ندارد.

پس روی آیفونِ نصب‌نشده **دکمه‌ی فعال‌سازی نشان نمی‌دهیم** — راهنمای
نصب می‌دهیم. دکمه‌ای که بزنند و کار نکند بدتر از نبودنش است.

آزمون روی سایت زنده:

| محیط | دکمه | راهنما |
|---|---|---|
| دسکتاپ کروم | ✅ هست | — |
| آیفون بدون نصب | ❌ نیست (درست) | ✅ هست |

---

## سه باگ واقعی که موقع راستی‌آزمایی پیدا شدند

### ۱. کلید VAPID رمزشده در باندل

کلید عمومی را در Vercel به‌صورت `encrypted` ثبت کردم. `NEXT_PUBLIC_*`
در باندل کلاینت می‌نشیند و Vercel **مقدار رمزشده** (`eyJ2IjoidjIi…`)
را جای کلید گذاشت.

`subscribe` بی‌صدا شکست می‌خورد — نه خطا، نه پیام. اگر به build موفق
اکتفا کرده بودم، تا اولین کاربر واقعی معلوم نمی‌شد.

→ به `plain` تغییر کرد + تابع `looksLikeVapidKey` که شکل کلید را
می‌سنجد و مقدار `eyJ` را رد می‌کند.

### ۲. `void supabase.rpc(...)` هیچ درخواستی نمی‌فرستاد

تله‌ی شناخته‌شده که **دوباره خوردم**: سازنده‌ی کوئری Supabase یک
thenable **تنبل** است. تا `then` صدا زده نشود، درخواست HTTP نمی‌رود.
`void` فقط مقدار را دور می‌ریزد.

تشخیصش سخت بود چون نه خطایی بود نه چیزی در تب شبکه — جدول فقط خالی
می‌ماند.

### ۳. ارقام لاتین در متن اعلان

`to_char` در Postgres «500,000» می‌سازد. متن اعلان **مستقیم** به کاربر
می‌رسد — در زنگوله و در پوش دستگاه که هیچ لایه‌ی نمایشی برای تبدیل
ندارد.

**از روی اسکرین‌شات پیدا شد، نه تست.** → تابع `fa_amount` در SQL.

بعلاوه دو دور اصلاح کنتراست: `opacity-60` روی ردیف خوانده‌شده کنتراست
را به ۲.۴۶ می‌رساند (حداقل ۴.۵)، و بعد `/70` روی متن ۱۰px هنوز ۴.۲۸
بود.

---

## سنجش‌ها

```
tsc --noEmit   → پاک
vitest         → ۱۲۴۰/۱۲۴۰ (۴۶ فایل)
build          → موفق
axe-core       → صفر (پس از دو دور اصلاح کنتراست)
سایت زنده      → ۵ اعلان با ارقام فارسی · دکمه روی دسکتاپ · راهنما روی iOS
```

**راستی‌آزمایی:** شش باگ عمداً برگردانده شد، هر شش گرفته شد:

| باگ | تستی که شکست |
|---|---|
| حذف گارد iOS | «آیفون نصب‌نشده راهنما می‌گیرد» |
| بازگشت زنگوله به همه‌ی نسخه‌ها | «فهرست فیلترشده» |
| حذف ایندکس یکتا | «تکرار جلوگیری می‌شود» |
| حذف `waitUntil` | «waitUntil استفاده می‌شود» |
| حذف گارد مالکیت اشتراک | «حذف فقط برای صاحبش» |
| مهم کردن همه‌ی نسخه‌ها | «تعداد مهم‌ها کمتر از یک‌سوم» |

⚠️ یک تست خودم هم اشتباه بود: `«a-b_c»` را به‌عنوان base64 معتبر
فرض کرده بودم در حالی که ۵ نویسه اصلاً معتبر نیست.

---

## صادقانه: چه چیزی هنوز نیست

- **هیچ اشتراک پوشی ثبت نشده** (`push_subscriptions` صفر ردیف).
  محیط تست headless به سرویس FCM گوگل دسترسی ندارد و `subscribe` با
  `AbortError` شکست می‌خورد. **جریان کامل روی گوشی واقعی امتحان
  نشده** — اولین بار که فعالش کردید لطفاً بگویید کار کرد یا نه.
- **پوش خودکار برای اعلان‌های جدید فرستاده نمی‌شود.** الان فقط
  «ارسال اعلان آزمایشی» هست. اتصال تولید اعلان به ارسال پوش، یک
  زمان‌بند سمت سرور می‌خواهد که Vercel در پلن رایگان محدودش می‌کند.
- **اعلان‌ها موقع باز کردن داشبورد ساخته می‌شوند**، نه با کرون. یعنی
  اگر یک هفته وارد نشوید، اعلان چک هم ساخته نمی‌شود. برای رفع واقعی،
  کرون‌جاب لازم است.
- **چک و CRM صفر ردیف دارند**، پس دو منبع از چهار منبع روی داده‌ی
  واقعی آزمایش نشده‌اند — فقط منطقشان.
- آیکون اعلان همان آیکون برنامه است، نه لوگوی کسب‌وکار.
