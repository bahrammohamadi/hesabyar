# گزارش — تقویم و فیلتر بازه‌ی تاریخ

نسخه: `26.0803` · کامیت `e7de50b` · ۱۲ مرداد ۱۴۰۵

---

## ۱. باگی که گزارش دادید

> «روی از تاریخ یا تا تاریخ کلیک می‌کنم، تو صفحه خالی می‌زنم پنجره‌ی انتخاب
> تاریخ بسته نمی‌شود؛ یا باید یک تاریخ انتخاب کنم یا باید دوباره توی باکس
> تاریخ کلیک کنم.»

**علت:** کامپوننت `DatePicker` هیچ شنونده‌ای برای کلیکِ بیرون نداشت. فقط سه راه
بسته‌شدن وجود داشت: انتخاب روز، دکمه‌ی «بستن»، یا کلیک دوباره روی همان دکمه.

### چه چیزی اضافه شد

| مورد | تصمیم | چرا |
|---|---|---|
| رویداد | `pointerdown` نه `click` | با `click`، کلیک روی میان‌برهایی مثل «این ماه» باعث می‌شد چیدمان بین `mousedown` و `mouseup` جابه‌جا شود و هدف از زیر انگشت در برود |
| فاز | `capture` | تقویم داخل پنل کشویی و مودال هم رندر می‌شود؛ آن لایه‌ها رویداد را در فاز حباب متوقف می‌کنند و شنونده‌ی معمولی هرگز اجرا نمی‌شد |
| Escape | می‌بندد + `stopPropagation` | بدون آن، یک Escape هم تقویم و هم پنل فاکتور زیرش را می‌بست و کار نیمه‌کاره از بین می‌رفت |

### یک مشکل دوم که فقط تست با مرورگر واقعی نشان داد

تقویم «از تاریخ» در `/sales` باز بود؛ کلیک روی ناحیه‌ی خالی **روی یک ردیف
جدول افتاد و پنل فاکتور فروش باز شد**. کاربری که فقط می‌خواست تقویم را ببندد،
ناگهان یک پنجره‌ی ناخواسته می‌گرفت.

حالا آن کلیک بلعیده می‌شود — همان قراردادی که `PortalMenu` در همین پروژه دارد.

⚠️ با یک استثنا: اگر مستقیم روی دکمه‌ی تقویم دیگری بزنید (جابه‌جایی «از تاریخ»
→ «تا تاریخ» که در فیلتر بازه مدام تکرار می‌شود)، کلیک بلعیده **نمی‌شود** تا
یک کلیک کافی باشد نه دو کلیک.

---

## ۲. 🔴 باگ جدی‌تری که حین کار پیدا شد

هنگام بررسی نوع ستون‌ها در دیتابیس زنده معلوم شد:

```
sales.date      → timestamp with time zone   (نه date)
purchases.date  → timestamp with time zone
```

و هر ۲۰ فاکتور موجود شما ساعت `08:30` ثبت شده‌اند.

فیلتر قبلی از `lte(تا تاریخ)` استفاده می‌کرد، یعنی «تا ساعت ۰۰:۰۰ آن روز».
شمارش واقعی روی داده‌ی خودتان:

```
بازه‌ی «۶ مرداد تا ۶ مرداد» (2026-07-28)
  lte  →  ۰ فاکتور     ❌ غلط
  lt+1 →  ۱ فاکتور     ✅ درست
```

یعنی **روز پایانی هر بازه‌ای بی‌صدا حذف می‌شد**. اگر «امروز» را انتخاب می‌کردید،
لیست خالی می‌آمد در حالی که همان لحظه فاکتور زده بودید.

اصلاح شد با `rangeBounds`/`applyRange` که به‌جای `lte`، از `lt(روز بعد)` استفاده
می‌کنند و روی هر دو نوع ستون `date` و `timestamptz` درست کار می‌کنند.

> نکته: تست قدیمی روی رشته‌ی `lte("date", range.to)` قفل شده بود — یعنی
> **خودِ باگ را تضمین می‌کرد**. آن تست هم اصلاح شد.

---

## ۳. فیلتر بازه به کجاها اضافه شد

| صفحه | ستون بازه | نوع فیلتر | چرا |
|---|---|---|---|
| گردش انبار | `created_at` | سمت سرور | `limit 100` داشت و شما ۷۳۰ رکورد دارید — ۸۶٪ داده اصلاً دیده نمی‌شد |
| مالی (هزینه/درآمد/دریافت/پرداخت/انتقال) | `date` | سمت سرور | `limit 100` + **جمع مبلغ بازه** اضافه شد |
| چک‌ها | `due_date` | سمت کلاینت | روی **سررسید** فیلتر می‌کند نه تاریخ ثبت، چون سؤال واقعی «این ماه چه چکی سررسید می‌شود؟» است. کوئری چک‌ها limit ندارد پس فیلتر محلی امن است |
| گزارش فعالیت | `created_at` | سمت سرور (API) | `limit 100` |
| مرجوعی فروش | `date` | سمت کلاینت | بدون limit |
| مرجوعی خرید | `date` | سمت کلاینت | بدون limit |

**قاعده‌ای که رعایت شد:** هر صفحه‌ای که `limit` دارد، فیلترش سمت سرور است و
سقف نتایج با فعال شدن بازه بالا می‌رود. فیلتر محلی روی فهرست limit‌دار غلط است
چون فقط همان ردیف‌های آخر را می‌بیند.

در API فعالیت‌ها، مقدار `from`/`to` قبل از رسیدن به دیتابیس با regex سخت‌گیرانه
(`^\d{4}-\d{2}-\d{2}$`) اعتبارسنجی می‌شود.

---

## ۴. یک ایراد دسترس‌پذیری که پیدا شد

با باز کردن تقویم در تست axe مشخص شد دکمه‌های «ماه قبل/بعد» **هیچ نام
دسترس‌پذیری نداشتند** (خطای `button-name` با شدت critical). این ایراد از قبل
وجود داشت و چون تقویم هرگز در تست‌ها باز نمی‌شد، دیده نشده بود.

اضافه شد: `aria-label` روی دکمه‌های ماه و فیلد تایپ، `role="dialog"` روی خود
تقویم، و `aria-expanded` روی دکمه‌ی بازکننده.

---

## ۵. چه چیزهایی واقعاً تست شد

بیلد سبز بودن دلیل نیست. آنچه واقعاً اجرا شد:

```
npx tsc --noEmit          → تمیز
npx vitest run            → ۲۶۰/۲۶۰ (۱۸ فایل)
npm run build             → Compiled successfully
```

**Playwright روی بیلد پروडکشن با داده‌ی واقعی شما:**

| سناریو | نتیجه |
|---|---|
| بسته‌شدن با کلیک بیرون در ۸ صفحه | ✅ |
| جابه‌جایی بین دو تقویم با یک کلیک | ✅ |
| کلیکِ بستن، پنل ناخواسته باز نمی‌کند | ✅ |
| کلیک بعدی عادی کار می‌کند (شنونده نشت ندارد) | ✅ |
| تغییر ماه / تایپ تاریخ نمی‌بندد | ✅ |
| Escape می‌بندد | ✅ |
| بازه‌ی یک‌روزه ۱ فاکتور می‌دهد (باگ `lte`) | ✅ |
| تقویم داخل پنل فروش: بسته می‌شود، پنل زنده می‌ماند | ✅ |
| Escape داخل پنل فقط تقویم را می‌بندد | ✅ |
| موبایل ۳۹۰ و تبلت ۸۳۴ — تپ، بدون اسکرول افقی | ✅ |
| axe-core روی ۷ صفحه با تقویم باز | ۰ تخلف |
| خطای کنسول | ندارد |

همین مجموعه بعد از دیپلوی **روی `tarazooapp.vercel.app` هم اجرا شد** و هر ۱۱
مورد پاس شد.

---

## ۶. داده‌ها

```
users 4 · orgs 2 · products 382 · sales 20 · sale_items 25
transactions 18 · stock_movements 730
```

بدون تغییر. `contacts` از ۵۴۳ به ۵۴۷ رسیده — بررسی شد: جدیدترین مشتری مربوط به
۲۸ تیر است، یعنی **کار خودتان بوده نه تست‌های من**. تمام تست‌ها فقط خواندنی
بودند و هیچ رکوردی ساخته یا حذف نشد.

---

## ۷. جاهایی که عمداً دست نخورد

- **گزارش‌ها** (`/reports/*`) از قبل `RangeFilter` مخصوص خودشان را دارند با
  همان `DatePicker`. رفع باگ کلیکِ بیرون آنجا هم اثر کرده چون همان کامپوننت است.
- **کاردکس کالا** و **موجودی به تاریخ** فیلد تاریخ دارند ولی ماهیتشان «تاریخ
  مبنا» است نه بازه — بازه آنجا معنا نمی‌دهد.
- `/sales/orders`، `/crm/rfm`، `/reports/sellers` و مشابه‌ها از قبل فیلتر تاریخ
  داشتند.

اگر جای دیگری هست که فکر می‌کنید بازه لازم دارد، بگویید اضافه کنم.
