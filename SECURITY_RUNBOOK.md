# راهنمای عملیات امنیتی — ترازو

این سند مراحل عملی پاسخ به رخدادهای امنیتی را توضیح می‌دهد.

> ⚠️ **در این فایل هیچ کلید واقعی نوشته نشده و نباید نوشته شود.**
> اگر روزی مجبور شدید کلیدی را جایی ثبت کنید، فقط ۸ کاراکتر اولش را بنویسید.

---

## ۱. چرخاندن کلید `service_role` در Supabase

### چه زمانی لازم است

- کلید در گیت، لاگ، اسکرین‌شات یا پیام چت دیده شده باشد
- کسی که به کلید دسترسی داشته از تیم جدا شود
- به‌عنوان اقدام دوره‌ای (توصیه: هر ۶ تا ۱۲ ماه)

### چرا فوری است

کلید `service_role` **تمام قوانین RLS را دور می‌زند**. هرکس آن را داشته باشد
می‌تواند همه‌ی جدول‌ها را بخواند، تغییر دهد و حذف کند — بدون نیاز به هیچ حسابی.
بازنویسی تاریخچه‌ی گیت کلید را **باطل نمی‌کند**؛ فقط جلوی دیده‌شدن دوباره‌اش را
می‌گیرد. اگر کلید افشا شده، **باید در داشبورد Supabase چرخانده شود.**

### روش پیشنهادی: مهاجرت به کلیدهای جدید (بدون قطعی سرویس)

Supabase دو نسل کلید دارد. کلیدهای نسل جدید (`sb_secret_…` و
`sb_publishable_…`) را می‌توان **جدا از کلیدهای قدیمی باطل کرد**، پس مهاجرت
به آن‌ها هم امن‌تر است و هم بدون قطعی انجام می‌شود.

**گام ۱ — ساخت کلید جدید**

1. وارد <https://supabase.com/dashboard> شوید و پروژه را انتخاب کنید.
2. مسیر: **Project Settings → API Keys**.
3. اگر بخش «API Keys (new)» را می‌بینید، روی **Create new secret key** بزنید.
4. نامی معنادار بگذارید (مثلاً `production-server`) و کلید را کپی کنید.
   این تنها باری است که کامل نمایش داده می‌شود.

**گام ۲ — به‌روزرسانی متغیر محیطی در Vercel**

1. <https://vercel.com> → پروژه `tarazoo` → **Settings → Environment Variables**.
2. مقدار `SUPABASE_SERVICE_ROLE_KEY` را با کلید جدید جایگزین کنید.
3. هر سه محیط را تیک بزنید: Production، Preview، Development.
4. ذخیره کنید.

**گام ۳ — انتشار مجدد**

متغیر محیطی فقط در زمان اجرا خوانده می‌شود، ولی برای اطمینان یک دیپلوی تازه
بزنید: **Deployments → آخرین دیپلوی → ⋯ → Redeploy**.

**گام ۴ — بررسی سلامت (پیش از باطل کردن کلید قدیمی)**

پس از پایان دیپلوی، این‌ها را دستی امتحان کنید:

- ورود به پنل با یک حساب واقعی
- باز شدن `/dashboard` و نمایش اعداد
- صفحه‌ی `/settings/users` (این صفحه مستقیماً از `service_role` استفاده می‌کند)
- پنل مدیریت `/admin/organizations`

اگر هر کدام خطای «Invalid API key» داد، یعنی کلید درست ثبت نشده — پیش از گام
بعد آن را حل کنید.

**گام ۵ — باطل کردن کلید قدیمی**

فقط وقتی گام ۴ کامل سبز بود:

1. برگردید به **Project Settings → API Keys**.
2. بخش **Legacy API keys** را باز کنید.
3. روی **Disable** کنار کلید `service_role` قدیمی بزنید.

> ⚠️ کلید `anon` قدیمی را همزمان غیرفعال نکنید مگر اینکه
> `NEXT_PUBLIC_SUPABASE_ANON_KEY` را هم به `sb_publishable_…` به‌روز کرده باشید.
> این کلید در سمت مرورگر استفاده می‌شود و غیرفعال کردنش کل اپ را از کار می‌اندازد.

**گام ۶ — تأیید نهایی**

کلید قدیمی باید دیگر کار نکند. بدون نوشتن کلید در فایل، این را در ترمینال
بررسی کنید (کلید را در متغیر محیطی موقت بگذارید):

```bash
read -rs OLD_KEY          # کلید قدیمی را بچسبانید؛ روی صفحه نمایش داده نمی‌شود
curl -s -o /dev/null -w "%{http_code}\n" \
  "https://<PROJECT-REF>.supabase.co/rest/v1/contacts?select=id&limit=1" \
  -H "apikey: $OLD_KEY" -H "Authorization: Bearer $OLD_KEY"
unset OLD_KEY
```

انتظار: **`401`**. اگر `200` گرفتید، کلید هنوز فعال است.

### اگر مجبورید همان کلید قدیمی را ریست کنید

در برخی پروژه‌های قدیمی گزینه‌ی «Reset service_role key» وجود دارد. این کار
**بلافاصله** کلید فعلی را باطل می‌کند، پس ترتیب معکوس است: اول کلید جدید را
بگیرید، سریع در Vercel ثبت کنید و redeploy بزنید. بین ریست تا پایان دیپلوی،
API سرور کار نمی‌کند (چند دقیقه قطعی).

---

## ۲. پاک‌سازی داده‌ی حساس از تاریخچه‌ی گیت

### آنچه باید بدانید

بازنویسی تاریخچه، **هش همه‌ی کامیت‌ها را عوض می‌کند**. هرکس clone دارد باید
دوباره clone کند. همچنین این کار کلید افشاشده را باطل نمی‌کند — بخش ۱ را
جداگانه انجام دهید.

### مراحل

```bash
# ۱) پشتیبان کامل، خارج از مخزن
tar czf /tmp/repo-backup-$(date +%F).tar.gz --exclude=node_modules .

# ۲) نصب ابزار
pip install git-filter-repo

# ۳) فایل قواعد جایگزینی (کلید واقعی را در فایل موقت بگذارید، نه در ریپو)
printf '%s==>***REMOVED-SERVICE-ROLE-KEY***\n' "$LEAKED_KEY" > /tmp/replacements.txt

# ۴) حذف مقادیر و فایل‌ها از همه‌ی کامیت‌ها
git filter-repo --force \
  --replace-text /tmp/replacements.txt \
  --path data/customers-ready.csv --path data/import-customers.sql \
  --path contacts_from_00_pdf.csv --invert-paths

# ۵) تأیید
git log --all -p | grep -c "eyJ"     # باید 0 باشد

# ۶) ریموت را دوباره اضافه کنید (filter-repo عمداً حذفش می‌کند)
git remote add origin <REMOTE-URL>
git push --force --all origin
git push --force --tags origin
```

### بعد از force-push

- به همه‌ی همکاران بگویید مخزن را دوباره clone کنند.
- در GitHub → **Settings → Branches**، اگر force-push مسدود است موقتاً بازش کنید.
- نسخه‌های کش‌شده‌ی GitHub ممکن است تا مدتی از طریق لینک مستقیم به SHA قدیمی
  در دسترس بمانند؛ برای پاک‌سازی کامل باید از پشتیبانی GitHub درخواست دهید.

---

## ۳. اگر داده‌ی مشتری افشا شد

1. **دامنه را مشخص کنید** — چه جدول‌هایی، چند رکورد، چه بازه‌ی زمانی.
2. **راه نشت را ببندید** — کلید را بچرخانید، RLS را فعال کنید، دسترسی را قطع کنید.
3. **لاگ‌ها را بررسی کنید** — Supabase Dashboard → Logs → API، دنبال درخواست‌های
   ناشناس با حجم غیرعادی بگردید.
4. **ثبت کنید** — تاریخ، ساعت، دامنه و اقدامات، در یک سند جدا.
5. **اطلاع‌رسانی** — اگر داده‌ی شخصی مشتریان درگیر است، به آن‌ها اطلاع دهید.

---

## ۴. چک‌لیست دوره‌ای

| کار | تناوب |
|---|---|
| بررسی RLS روی جدول‌های جدید | با هر مایگریشن |
| `npm audit --omit=dev` | ماهانه |
| بازبینی کاربران فعال و نقش‌هایشان | فصلی |
| چرخاندن کلید `service_role` | سالانه |
| تست بازیابی از پشتیبان | فصلی |
| اسکن مخزن برای کلید (`git log -p \| grep eyJ`) | پیش از هر انتشار عمومی |

---

## ۵. پیشگیری

**قبل از کامیت، کلید را بگیرید.** یک hook ساده:

```bash
cat > .git/hooks/pre-commit <<'HOOK'
#!/bin/sh
if git diff --cached | grep -qE "eyJhbGciOi[A-Za-z0-9_.-]{30,}"; then
  echo "❌ کامیت متوقف شد: شبیه یک JWT در تغییرات هست."
  echo "   اگر مطمئنید مشکلی نیست: git commit --no-verify"
  exit 1
fi
HOOK
chmod +x .git/hooks/pre-commit
```

**قواعد ثابت پروژه:**

- کلید فقط از `process.env` خوانده شود، هرگز نوشته نشود.
- `.env.local` در `.gitignore` باشد (هست).
- فایل‌های داده‌ی واقعی مشتری هرگز کامیت نشوند — الگوهایشان در `.gitignore` آمده.
- `SUPABASE_SERVICE_ROLE_KEY` هرگز با پیشوند `NEXT_PUBLIC_` تعریف نشود؛
  آن پیشوند مقدار را به سمت مرورگر می‌فرستد.

---

*آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۱۰ — همراه با گزارش `SECURITY_AUDIT_2026-07-31.md`*

---

## ✅ چرخاندن کلید — انجام‌شده در ۱۲ مرداد ۱۴۰۵

### چه اتفاقی افتاده بود

کلید `service_role` در کامیت `4763219` داخل فایل `health-check.js`
هاردکد شده بود و روی گیت‌هاب عمومی قرار داشت. تاریخچه در ۱۱ مرداد با
`git filter-repo` تمیز شد، اما **خود کلید همچنان معتبر بود** — یعنی
هرکسی که پیش از پاک‌سازی مخزن را کلون کرده بود، دسترسی کامل به
دیتابیس داشت (دور زدن تمام RLS).

تأیید پیش از اقدام: درخواست با آن کلید HTTP 200 و نام واقعی مشتریان
(«مائده فلاح»، «نامدار») را برگرداند.

### چرا مسیر معمول جواب نداد

کلیدهای `anon` و `service_role` از نوع **legacy** هستند و در API
سوپابیس تک‌تک قابل چرخاندن نیستند. مسیر درست، مهاجرت به کلیدهای
جدید است که از قبل روی پروژه ساخته شده بودند:

| قدیمی (legacy JWT) | جدید |
|---|---|
| `anon` | `sb_publishable_…` |
| `service_role` | `sb_secret_…` |

### ترتیبی که رعایت شد (مهم)

اول جایگزینی، بعد ابطال. ترتیب برعکس یعنی قطعی سرویس.

1. تأیید اینکه کلیدهای جدید همان رفتار را دارند:
   `sb_secret` → RLS را دور می‌زند · `sb_publishable` → RLS اعمال
   می‌شود و ورود کار می‌کند.
2. بیلد و اجرای محلی با کلید جدید → پنج مسیر وابسته به
   `service_role` سالم، صفر خطای API.
3. به‌روزرسانی `SUPABASE_SERVICE_ROLE_KEY` در Vercel → دیپلوی →
   تأیید روی سایت زنده.
4. به‌روزرسانی `NEXT_PUBLIC_SUPABASE_ANON_KEY` → دیپلوی → تأیید.
   ⚠️ این قدم حیاتی بود: غیرفعال‌کردن legacy کلید `anon` را هم
   باطل می‌کند و آن در مرورگر همه‌ی کاربران است. بدون مهاجرت
   هم‌زمان، سایت برای همه از کار می‌افتاد.
5. غیرفعال‌کردن legacy:
   ```
   PUT /v1/projects/<ref>/api-keys/legacy?enabled=false
   ```
   نکته: پارامتر در **query string** است نه بدنه. ارسال در بدنه
   خطای `expected string, received undefined` می‌دهد.
6. انتشار حدود ۲۰ ثانیه طول کشید؛ بار اول همچنان ۲۰۰ می‌داد.

### تأیید نهایی

```
کلید لو رفته      → HTTP 401  ✅
sb_secret جدید     → HTTP 200
sb_publishable     → HTTP 200 (ورود موفق)
سایت زنده          → ۵ مسیر سالم، صفر خطای API
داده                → ۴ کاربر · ۲ سازمان · ۵۴۳ مخاطب · ۳۸۲ کالا
                     ۲۰ فاکتور · ۷۳۰ گردش · ۱ سوپرادمین
```

### باقی‌مانده

- شاخه‌ی `backup/pre-filter-repo-2026-08-02` روی گیت‌هاب هنوز
  تاریخچه‌ی آلوده را دارد. کلید داخلش دیگر بی‌اثر است، ولی برای
  تمیزی می‌توان حذفش کرد.
- رمز `123456` روی حساب‌های `bahram@` و `test@` هنوز فعال است.
