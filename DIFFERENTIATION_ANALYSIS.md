# تحلیل وجه تمایز ترازو + نظم‌دهی منو

مرداد ۱۴۰۵ · مستقر روی `758d8a6`

---

## بخش اول: تحلیل تمایز

### روش

مزیت‌های ادعایی را **در کد خودمان جستجو کردم** و فقط چیزی نوشتم که
فایل پشتیبانش موجود است. تست `marketing-claims` این قاعده را برای
آینده تثبیت می‌کند: هر ادعا باید فایل متناظرش وجود داشته باشد.

### تمایز بنیادی (نه یک قابلیت، یک تفاوت معماری)

مستند رسمی سپیدار می‌گوید وب‌سرویسش روی `localhost:7373` بالا می‌آید و
«برای دسترسی از اینترنت باید IP static از ISP تهیه شود». فروشنده‌ی
افزونه‌ی هلو هم می‌گوید سینک فقط وقتی کار می‌کند که «سیستمِ دارای
نرم‌افزار روشن و به اینترنت متصل باشد».

**یعنی کل صنعت افزونه‌نویسی (۷۵۰ هزار تا ۲۵ میلیون تومان) برای عبور از
دیواری ساخته شده که ما نداریم.**

### هفت تمایز، هر کدام با پشتوانه‌ی کد

| تمایز | پشتوانه | چرا رقبا ندارند |
|---|---|---|
| ابری از روز اول | کل معماری | روی ویندوز مغازه نصب‌اند |
| نصب روی گوشی | `public/sw.js` | نسخه‌ی موبایل ندارند |
| ثبت فاکتور با صدا | `voice-order.tsx` | هیچ‌کدام ندارند |
| اسکن با دوربین | `barcode-scanner.tsx` | بارکدخوان فیزیکی می‌خواهند |
| خروجی اکسل یک‌کلیکی | `settings/backup` | پشتیبان دستی از فایل دیتابیس |
| ویترین آنلاین | `app/shop/[slug]` | افزونه‌ی جدا و گران |
| **اعلان روی گوشی** | مهاجرت ۰۰۴۵ | هشدارشان فقط پای کامپیوتر دیده می‌شود |
| **ارسال فاکتور به مشتری** | `invoice-share.tsx` | چاپ یا PDF، نه ارسال از داخل برنامه |
| **قیمت تمام‌شده‌ی درست** | مهاجرت ۰۰۴۶ | کرایه حمل را فقط به جمع اضافه می‌کنند |

سه مورد آخر امروز اضافه شدند.

### 🔴 یک متن که از محصول عقب مانده بود

در `MarketingPieces.tsx` کامنتی بود:

> «⚠️ اسکن با دوربین موبایل هنوز پیاده‌سازی نشده؛ متن عمداً فقط
> بارکدخوان را ذکر می‌کند تا قابلیت نداشته تبلیغ نشود.»

ولی `barcode-scanner.tsx` مدت‌هاست ساخته شده. یعنی **متن سایت از
محصول عقب مانده بود** — برعکس ادعای دروغ، ولی باز هم اشتباه: مزیتی که
داریم را پنهان می‌کرد.

---

## بخش دوم: نظم منو

### 🔴 یافته‌ی اصلی: بیست صفحه‌ی گم‌شده

مسیرهای واقعی برنامه را با لینک‌های سایدبار مقایسه کردم:

```
صفحات موجود   ۷۵
در منو         ۵۵
یتیم           ۲۰   ← وجود دارند، کار می‌کنند، هیچ لینکی ندارند
```

بدترینش `/settings/general` بود — همان صفحه‌ای که **فرم هویت برند،
آپلود لوگو و فعال‌سازی اعلان گوشی** را در دو نوبت گذشته در آن ساختم.
کاربر فقط با تایپ دستی نشانی می‌توانست پیدایش کند.

پنج صفحه‌ی CRM (`/crm`, `/crm/interactions`, `/crm/rfm`,
`/crm/automation`, `/crm/loyalty`) هم همین وضع را داشتند.

### چه شد

| تغییر | چرا |
|---|---|
| «باشگاه مشتریان» → **«مشتریان و باشگاه»** با ۸ آیتم | پنج صفحه‌ی CRM از یتیمی درآمدند |
| **«کسب‌وکار، برند و ظاهر»** به تنظیمات اضافه شد | صفحه‌ی برند در دسترس شد |
| «بدهکاران / بستانکاران» → **دو منوی جدا** | یک لینک برای دو مفهوم متضاد |
| **«داده و ابزار»** گروه جدید | پشتیبان، ورود اکسل و بررسی دوربین *عملیات*اند نه تنظیم |

گروه تنظیمات از ۱۰ به ۱۱ آیتم رسیده بود؛ تست سقف را گرفت و باعث شد
تقسیم معنایی انجام شود — نه صرفاً عددی.

### تست نگهبان

`tests/nav-coverage.test.ts` درخت مسیرها را می‌پیماید و شکست می‌خورد اگر:

- صفحه‌ای نه در منو باشد نه در فهرست «یتیم عمدی با دلیل مستند»
- لینکی در منو به صفحه‌ای اشاره کند که وجود ندارد
- گروهی بیش از ده آیتم داشته باشد
- دو برچسب تکراری باشند

فهرست «یتیم عمدی» ۱۴ ورودی دارد که هرکدام **دلیل نوشته‌شده** دارند
(مثلاً `/reports/sales` تبی داخل صفحه‌ی گزارش‌هاست).

---

## سه تست موجود که عقب مانده بودند

`notification-bell.test.ts` فرض می‌کرد `unseenReleases` **همیشه**
جدیدترین نسخه را برمی‌گرداند. آن فرض با فیلتر «فقط مهم‌ها» از بین رفت
— یعنی همان چیزی که خودتان خواستید.

بررسی کردم و **تست عقب مانده بود نه کد**؛ بازنویسی شدند تا ادعای درست
را بسنجند: «هرچه برمی‌گردد باید مهم باشد».

---

## سنجش‌ها

```
tsc --noEmit   → پاک
vitest         → ۱۲۸۵/۱۲۸۵ (۴۹ فایل)
build          → Vercel موفق
axe-core       → صفر
سایت زنده      → هر سه گروه منو باز می‌شوند · هر پنج آیتم جدید موجود
                 · هر سه مزیت جدید روی صفحه‌ی اصلی
```

پنج باگ عمداً برگردانده شد، هر پنج گرفته شد.

---

## صادقانه

- **چیدمان بصری منو عوض نشد**، فقط ساختارش. اگر «ریدیزاین» یعنی ظاهر
  جدید (رنگ، فاصله، آیکون)، آن کار جداست و انجام نشده.
- **صفحات یتیمِ CRM محتوایشان را بررسی نکردم.** لینک دارند و باز
  می‌شوند، ولی اینکه هرکدام چقدر کامل‌اند را نسنجیدم.
- فهرست «یتیم عمدی» بر پایه‌ی خواندن کد است؛ ممکن است یکی‌شان واقعاً
  باید در منو باشد.
- تعداد آیتم‌های منو از ۵۵ به ۶۲ رسید. بیشتر شد ولی هیچ‌چیز پنهان
  نماند — که به‌نظرم معامله‌ی درستی است.

---

## قدم بعدی

1. **پیش‌فاکتور با تبدیل یک‌کلیکی** — `sales_orders` آماده است
2. **چک: هشدار سررسید و خرج کردن**
3. **قالب فیش ۸۰ میلی‌متری**
