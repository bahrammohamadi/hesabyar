# گزارش: مزیت‌های رقابتی در صفحه‌ی اصلی

مستقر روی `e23a794` · بخش `#advantages`

---

## روش کار

سایت‌های حسابداری و CRM ایرانی را بررسی کردم (هلو، سپیدار، محک، چرتکه،
لیام، حسابفا، فرداد، معین)، فهرست مزیت‌های ممکن را درآوردم، و بعد
**تک‌تکشان را در کد خودمان جستجو کردم** تا فقط چیزی بنویسم که واقعاً
وجود دارد.

---

## 🔴 دو ادعای دروغ که روی سایت زنده بود

### ۱. «فروش اقساطی»

در کارت «لوازم خانگی و دیجیتال» نوشته بود. ما فقط **نسیه** داریم — یک
مبلغ بدهی بدون زمان‌بندی. دفترچه‌ی اقساط با سررسید، کارمزد، جریمه‌ی
دیرکرد و ضامن (که معین و چرتکه دارند) اصلاً پیاده‌سازی نشده.

→ به «فروش نسیه» تغییر کرد.

### ۲. «پنل پیامکی رایگان»

در پلن شش‌ماهه. جستجوی کد نشان داد تنها چیز موجود، یک لینک `sms:` روی
شماره‌ی مخاطب است که **خودِ گوشی** بازش می‌کند — نه پنل ارسال.

🔴 **و اینجا یک درس گرفتم:** اول فقط `plans.ts` را اصلاح کردم و فکر
کردم تمام شد. ولی بعد از دیپلوی، متن هنوز روی سایت زنده بود. علت:
**پلن‌ها از جدول `plans` دیتابیس خوانده می‌شوند** و `plans.ts` فقط
داده‌ی پشتیبان است. اگر به build موفق اکتفا کرده بودم، ادعای دروغ سر
جایش می‌ماند.

→ با تأیید شما یک `UPDATE` روی همان یک ردیف زدم:

```
پیش از تغییر: ["محصولات نامحدود","مدیریت کامل انبارداری","پنل پیامکی رایگان","۱۵٪ تخفیف اقتصادی"]
پس از تغییر:  ["محصولات نامحدود","مدیریت کامل انبارداری","پشتیبان‌گیری کامل اکسل","۱۵٪ تخفیف اقتصادی"]
```

تعداد کل پلن‌ها پیش و پس: **۴ = ۴**. ردیف‌های دیگر دست‌نخورده.

گفتید «نمی‌دونم»، پس گزینه‌ی امن را انتخاب کردم: جایگزینی با قابلیتی که
**واقعاً داریم** و ارزش فروش هم دارد. اگر بعداً پیامک اضافه شد،
برگرداندنش یک خط است.

---

## بخش جدید: «چه فرقی با بقیه دارد؟»

شش مزیت، هرکدام به‌صورت **«معمولاً / در ترازو»** نه فهرست ساده. دلیلش:
کسی که الان هلو یا محک دارد نمی‌خواهد بداند چه چیزهایی هست — می‌خواهد
بداند **چه چیزی عوض می‌شود**.

| مزیت | مبنای واقعی |
|---|---|
| ابری از روز اول | مستند رسمی سپیدار: وب‌سرویس روی `localhost:7373` و «باید IP static بخرید» |
| نصب روی گوشی | `public/sw.js` |
| ثبت فاکتور با صدا | `components/shared/voice-order.tsx` |
| اسکن با دوربین | `components/shared/barcode-scanner.tsx` |
| خروجی کامل با یک کلیک | `app/(app)/settings/backup/page.tsx` |
| ویترین آنلاین رایگان | `app/shop/[slug]/page.tsx` |

قوی‌ترینشان اولی است و از تحقیق قبلی آمد: هلو و سپیدار روی ویندوزِ
مغازه‌اند و سایت خودشان اعتراف می‌کند سینک فقط وقتی کار می‌کند که آن
کامپیوتر روشن باشد.

### بخش «صادقانه: چه چیزهایی هنوز نداریم»

هر رقیبی صفحه‌ی «چرا ما بهتریم» دارد و **هیچ‌کدام** نمی‌گوید چه ندارد.
این بخش نام می‌برد: مؤدیان · درگاه پرداخت · اپلیکیشن اندروید در بازار ·
پنل پیامکی · دفترچه‌ی اقساط · حقوق و دستمزد.

مؤدیان عمداً اول فهرست است: از دی ۱۴۰۴ الزام قانونی شده و جریمه‌اش ۱۰٪
کل فروش. اگر کسی به امیدش ثبت‌نام کند و نباشد، حق دارد شاکی باشد.

---

## تست نگهبان

`tests/marketing-claims.test.ts` قاعده را برای آینده تثبیت می‌کند: فایل
تبلیغاتی می‌تواند نام یک قابلیت نداشته را بیاورد، **فقط در بافت انکار**.

⚠️ **دو تا از ادعاهای خودِ تست اول ضعیف بودند** و وقتی باگ را عمداً
برگرداندم سبز ماندند:

| ضعف | چرا | اصلاح |
|---|---|---|
| بررسی سلب ادعا در کل فایل | «پنل پیامکی رایگان» را برگرداندم؛ چون جای دیگری از همان فایل «حذف شد» بود، سبز ماند | حالا **همان خط** سنجیده می‌شود |
| رجکس عدد فقط `\d` | «۵۰ هزار کاربر» با ارقام فارسی رد می‌شد. متن سایت همیشه فارسی است، پس ادعا عملاً بی‌اثر بود | `[۰-۹\d]` اضافه شد |

پس از اصلاح، پنج بازگشت عمدی هر پنج گرفته شدند.

---

## سنجش‌ها

```
tsc --noEmit   → پاک
vitest         → ۱۱۴۲/۱۱۴۲ (۴۴ فایل)
build          → موفق
سرریز چیدمان   → صفر در ۳۹۰ / ۸۲۰ / ۱۴۴۰
axe-core       → صفر در هر سه
سایت زنده      → «پنل پیامکی رایگان» = ۰ · «فروش اقساطی» فقط در فهرست نداریم
```

یک باگ bidi هم از روی اسکرین‌شات گرفته شد: «IP ثابت» به‌صورت «ثابت IP»
رندر می‌شد. به «نشانی اینترنتی ثابت» تغییر کرد که هم دقیق‌تر است و هم
این مشکل را ندارد.

---

## صادقانه: چه چیزی نکردم

- **صفحه‌ی `/features` و `/pricing` را بازبینی نکردم.** تست ادعاها روی
  کل پوشه‌ی `(marketing)` اجرا می‌شود و چیز دیگری نگرفت، ولی خودم
  چشمی مرورشان نکردم.
- **`plans.ts` و جدول `plans` هنوز دو منبع جدا هستند.** الان هماهنگ‌اند،
  ولی هیچ چیزی جلوی واگرایی دوباره را نمی‌گیرد. تست فقط فایل را
  می‌بیند، نه دیتابیس را.
- **ادعای «ویترین آنلاین رایگان» کمی سخاوتمندانه است:** صفحه هست ولی
  سبد خرید ندارد. متن عمداً «ویترین» می‌گوید نه «فروشگاه اینترنتی».
- مقایسه‌ی قیمت با رقبا نکردم.
