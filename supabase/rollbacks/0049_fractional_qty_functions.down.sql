-- بازگشت 0049 — توابع مالی به مقدار صحیح برمی‌گردند
--
-- 🔴 هشدار: اگر پس از این مهاجرت فاکتوری با مقدار اعشاری ثبت شده
-- باشد، برگرداندن توابع باعث می‌شود ویرایش همان فاکتورها مقدار را
-- گرد کند و موجودی انبار خراب شود.
--
-- پیش از اجرا بررسی کنید:
--   select count(*) from sale_items     where qty <> round(qty);
--   select count(*) from purchase_items where qty <> round(qty);
-- اگر صفر نبود، این فایل را اجرا نکنید.
--
-- برای بازگشت واقعی، تعاریف توابع را از مهاجرت ۰۰۴۶ (آخرین نسخه‌ی
-- پیش از این تغییر) بردارید و اجرا کنید. عمداً اینجا کپی نشده‌اند
-- تا نسخه‌ی قدیمی‌تر به‌اشتباه روی نسخه‌ی جدید ننشیند.
