-- =============================================================
-- Hesabyar Rollback 0014 - بازگشت غیرمخرب Stock Movements
-- نوع: DOWN migration دستی و ایمن
-- سیاست ایمنی: هیچ داده‌ای حذف نمی‌شود.
-- =============================================================

-- -------------------------------------------------------------
-- ۱) حذف view موجودی
-- View داده فیزیکی ندارد؛ حذف آن داده stock_movements یا product_variants را پاک نمی‌کند.
-- -------------------------------------------------------------
drop view if exists public.v_product_stock;

-- -------------------------------------------------------------
-- ۲) حذف تابع کمکی RPC
-- حذف تابع داده‌ای را حذف نمی‌کند.
-- -------------------------------------------------------------
drop function if exists public.fn_add_stock_movement(uuid, uuid, text, numeric, text, text, text);

-- -------------------------------------------------------------
-- ۳) حذف ایندکس‌های کمکی 0014
-- حذف ایندکس داده را حذف نمی‌کند؛ فقط ساختار کمکی query را برمی‌گرداند.
-- -------------------------------------------------------------
drop index if exists public.idx_stock_movements_ref;
drop index if exists public.idx_stock_movements_created_at;
drop index if exists public.idx_stock_movements_warehouse;

-- -------------------------------------------------------------
-- ۴) ستون‌های warehouse_id و balance_after حذف نمی‌شوند.
-- دلیل: ممکن است بعد از اجرای migration توسط سیستم استفاده شده باشند و حذف ستون باعث data loss شود.
-- -------------------------------------------------------------

-- -------------------------------------------------------------
-- ۵) constraint جدید reason به legacy برگردانده نمی‌شود.
-- دلیل: اگر بعد از migration مقدارهای جدید مثل adjust/waste ثبت شده باشد، constraint قدیمی fail می‌شود.
-- در صورت نیاز به rollback کامل constraintها، ابتدا کوئری زیر را اجرا کنید:
-- select reason, count(*) from public.stock_movements group by reason order by reason;
-- -------------------------------------------------------------

-- =============================================================
-- پایان DOWN migration 0014
-- =============================================================
