-- =============================================================
-- Hesabyar Rollback 0017 - حذف غیرمخرب Workflow Engine
-- ایمنی: هیچ سند، تراکنش، audit یا stock_movement حذف نمی‌شود.
-- =============================================================

DROP FUNCTION IF EXISTS public.fn_transition_document(text, uuid, text);

-- =============================================================
-- پایان DOWN migration 0017
-- =============================================================
