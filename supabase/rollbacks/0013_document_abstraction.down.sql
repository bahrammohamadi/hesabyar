-- =============================================================
-- Hesabyar Rollback 0013 - بازگشت غیرمخرب Document Abstraction
-- نوع: DOWN migration دستی و ایمن
-- سیاست ایمنی: هیچ داده عملیاتی حذف نمی‌شود.
-- =============================================================

-- -------------------------------------------------------------
-- ۱) حذف read-modelها
-- Viewها داده فیزیکی ندارند؛ حذف آن‌ها داده sales/purchases/items را پاک نمی‌کند.
-- ابتدا v_document_lines حذف می‌شود چون از نظر مفهومی به documentها وابسته است.
-- -------------------------------------------------------------
drop view if exists public.v_document_lines;
drop view if exists public.v_documents;

-- -------------------------------------------------------------
-- ۲) جدول document_registry حذف نمی‌شود.
-- دلیل: registry داده پیکربندی معماری است و حذف آن خلاف سیاست عدم حذف داده است.
-- اگر rollback کامل لازم شد، باید بعد از backup و تأیید دستی جداگانه انجام شود.
-- -------------------------------------------------------------

-- -------------------------------------------------------------
-- ۳) ستون‌های افزوده‌شده به sales/purchases حذف نمی‌شوند.
-- ستون‌های warehouse_id/reversed_at/reversed_by ممکن است بعد از اجرا توسط سیستم استفاده شده باشند.
-- حذف ستون‌ها می‌تواند باعث از دست رفتن داده شود.
-- -------------------------------------------------------------

-- -------------------------------------------------------------
-- ۴) constraintهای status به حالت قبلی برگردانده نمی‌شوند.
-- دلیل: بازگردانی constraint legacy ممکن است اگر statusهای جدید paid/settled/reversed ثبت شده باشند fail شود.
-- در صورت نیاز به rollback کامل constraintها، ابتدا باید گزارش تعداد statusهای جدید گرفته شود.
-- -------------------------------------------------------------

-- کوئری پیشنهادی فقط‌خواندنی قبل از rollback constraintها:
-- select status, count(*) from public.sales group by status order by status;
-- select status, count(*) from public.purchases group by status order by status;

-- =============================================================
-- پایان DOWN migration 0013
-- =============================================================
