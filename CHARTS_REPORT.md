# گزارش: نمودارهای فارسی و خواناتر

**تاریخ:** ۱۹ مرداد ۱۴۰۵ · **نسخه:** ۲۶.۰۸۱۰g · بدون مهاجرت دیتابیس

---

## 🔴 آنچه اندازه‌گیری کردم، نه حدس زدم

درخواست شما «نمودارها جذاب‌تر بشه» بود. به‌جای اینکه سلیقه‌ای رنگ عوض کنم، اول
در مرورگر واقعی نگاه کردم که **دقیقاً چه چیزی رندر می‌شود**:

```
تیک‌های محور Y در داشبورد و /reports:
    0k · 850k · 1.7M · 2.55M · 3.4M
```

**ارقام لاتین با پسوند انگلیسی**، در برنامه‌ای که همه‌جایش فارسی است — حتی
تاریخ‌های همان نمودار شمسی بودند (`۰۴/۲۱`).

بدتر: تابعی به نام `compactAxisNumber` که دقیقاً «۱.۷ م» می‌سازد **از قبل وجود
داشت**، ولی فقط در یکی از سه فایل نمودار استفاده می‌شد.

### ریشه: دو فایل از سه فایل، ChartKit را دور می‌زدند

| فایل | وضعیت |
|---|---|
| `reports/overview-v2` | ✅ از ChartKit استفاده می‌کرد |
| `dashboard/DashboardSalesChart` | ❌ نسخه‌ی دست‌ساز |
| `reports/page` | ❌ نسخه‌ی دست‌ساز |

نتیجه: گرادیان محلی، `contentStyle` محلی، بدون اسکلت بارگذاری، و بدون احترام به
«کاهش حرکت».

---

## نتیجه‌ی پس از اصلاح

```
داشبورد:  ۰ · ۸۵۰ هـ · ۱.۷ م · ۲.۵ م · ۳.۴ م     رقم لاتین: هیچ ✅
گزارش‌ها:  ۰ · ۸۵۰ هـ · ۱.۷ م · ۲.۵ م · ۳.۴ م     رقم لاتین: هیچ ✅

راهنمای شناور:  «۰۴/۲۷ — فروش: ۲,۳۸۰,۰۰۰ تومان»
                جهت: rtl · رقم لاتین: هیچ ✅
```

### تغییرات

- **`ChartTooltip` مشترک** به ChartKit اضافه شد. با `contentStyle` فقط *ظاهر
  جعبه* قابل تنظیم است و مقدارها با رقم لاتین رندر می‌شوند؛ با کامپوننت
  سفارشی هم رقم فارسی می‌شود و هم تاریخ شمسی.
- **خلاصه‌ی عددی** بالای نمودار داشبورد: جمع دوره و پرفروش‌ترین روز. کاربر
  معمولاً دنبال «چقدر فروختم» است، نه شکل منحنی — خواندن عدد از روی نمودار کار
  اضافه است.
- **اسکلت بارگذاری و حالت خالی** طراحی‌شده به‌جای اسپینر و صفحه‌ی سفید.
- **`isAnimationActive`** روی هر سه فایل. Recharts انیمیشن را در جاوااسکریپت
  اجرا می‌کند و بلوک سراسری CSS خنثی‌اش نمی‌کند — یعنی وعده‌ی دسترس‌پذیری نصفه
  بود.
- **فاصله‌ی تیک محور X روی موبایل** تا برچسب‌ها روی هم نیفتند.
- دو import مرده (`CHART_TOOLTIP_STYLE` و `formatNumber`) حذف شد.

---

## 🔴 باگی که فقط از روی تصویر پیدا شد

بعد از افزودن خلاصه، در اسکرین‌شات دیدم:

```
بیشترین روز
۳۳۷,۵۰۰۰۵/۰۳      ← مبلغ ۳۳۷٬۵۰۰ و تاریخ ۰۵/۰۳ به هم چسبیده
```

فقط `mr-1.5` بین دو عنصر inline بود و چون هر دو رقم فارسی‌اند، در چیدمان
راست‌به‌چپ مرزشان دیده نمی‌شد و یک عدد واحد به نظر می‌رسید.

**نه `tsc`، نه `build` و نه `axe` چنین چیزی را نمی‌گیرند** — فقط نگاه‌کردن به
تصویر. با `flex` و یک جداکننده‌ی «·» رفع شد:

```
۳۳۷,۵۰۰ · ۰۵/۰۳   ✅
```

---

## تستی که کل این دسته را می‌بندد

مهم‌ترین تست، فایل‌های نمودار را **از روی فایل‌سیستم پیدا می‌کند** (هر `.tsx`
که `recharts` دارد) و روی همه‌شان شرط می‌گذارد:

- الگوی `${v / 1000000}M` و `${v / 1000}k` ممنوع
- هر نموداری که `tickFormatter` دارد باید `compactAxisNumber` بخواند
- هیچ `contentStyle=` نمانده باشد
- هر `<Area>` / `<Line>` / `<Bar>` باید `isAnimationActive` داشته باشد

یعنی اگر فردا نمودار چهارمی اضافه شود و ChartKit را دور بزند، **تست خودکار
می‌شکند** — نه اینکه شش ماه بعد در یک اسکرین‌شات دیده شود.

همین تست بلافاصله یک نقص واقعی گرفت: نمودارهای `/reports` هنوز
`isAnimationActive` نداشتند. **کد را اصلاح کردم، نه تست را.**

---

## راستی‌آزمایی تست‌ها

**پنج مورد عمداً برگردانده شد و تست شکست:**

| باگ برگردانده‌شده | تستی که گرفت |
|---|---|
| فرمت‌کننده‌ی لاتین `${v/1e6}M` | `🔴 هیچ نموداری فرمت‌کننده‌ی لاتین ندارد` |
| `contentStyle` دست‌ساز | `🔴 هیچ contentStyle نمانده` |
| حذف `isAnimationActive` | `انیمیشن به prefers-reduced-motion احترام می‌گذارد` |
| `compactAxisNumber` رقم لاتین بدهد | ۳ تست شکست |
| حذف `dir="rtl"` از Tooltip | `🔴 جهت راست‌به‌چپ دارد` |

---

## وضعیت نهایی

```
tsc --noEmit   پاک
vitest         ۱۰۵۳ / ۱۰۵۳  (۴۰ فایل) — از ۱۰۳۰
next build     Compiled successfully
axe-core       صفر ایراد در ۲ صفحه × ۳ اندازه = ۶ ترکیب
سرریز افقی     هیچ‌کدام
خطای کنسول     صفر
رقم لاتین      صفر (محور و Tooltip، هر دو صفحه)
```

**داده‌ی شما لمس نشد** — نه مهاجرتی، نه نوشتنی در دیتابیس.

---

## صادقانه: چه چیزی نکردم

- **نوع نمودارها عوض نشد.** همان خطی، میله‌ای و دایره‌ای‌اند. «جذاب‌تر» را به
  معنای *خوانا و درست* گرفتم نه *پرزرق‌وبرق*؛ نمودار سه‌بعدی یا انیمیشن سنگین
  در یک ابزار کاری روزمره کمکی نمی‌کند.
- **نمودار تازه‌ای اضافه نشد.** اگر نمودار مشخصی می‌خواهید (مثلاً مقایسه‌ی
  ماه‌به‌ماه یا سهم هر فروشنده)، بگویید بسازم.
- **`reports/overview-v2` دست نخورد** — از قبل درست بود.
- **رنگ‌ها عوض نشد.** همه از توکن معنایی می‌آیند تا حالت تیره سالم بماند.

اگر منظورتان از «جذاب‌تر» چیز دیگری بود — مثلاً نمودار خاصی که در نرم‌افزار
دیگری دیده‌اید — بگویید تا همان را بسازم.

## باقی‌مانده

⬜ داشبورد بر اساس بیشترین استفاده · ⬜ APK با TWA (وابسته به دامنه)
⬜ ویرایش قلم‌به‌قلم فاکتور توسط پشتیبانی · ⬜ پرچم قابلیت
⬜ ورود فاکتورها و تراکنش‌های گذشته از اکسل · ⬜ آیکون maskable
