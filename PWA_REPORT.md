# گزارش: نصب ترازو روی گوشی (PWA)

**تاریخ:** ۱۹ مرداد ۱۴۰۵ · **نسخه:** ۲۶.۰۸۱۰f · بدون مهاجرت دیتابیس

---

## 🔴 آنچه پیدا شد: «PWA» فقط ظاهری بود

در گزارش‌های قبلی نوشته شده بود «مانیفست آماده است». بررسی کردم:

| | وضعیت واقعی |
|---|---|
| مانیفست | ✅ بود |
| آیکون ۱۹۲ و ۵۱۲ | ✅ بودند |
| **سرویس‌ورکر** | ❌ **فایلش اصلاً وجود نداشت** |
| **ثبت سرویس‌ورکر** | ❌ هیچ‌جا صدا زده نمی‌شد |

کروم **بدون سرویس‌ورکر دکمه‌ی «نصب» را نشان نمی‌دهد**. یعنی اپ با اینکه همه‌ی
نشانه‌های PWA را داشت، عملاً قابل نصب نبود. این دقیقاً همان فاصله‌ی «ظاهری» با
«واقعاً کار می‌کند» است.

---

## سیاست کش: محافظه‌کارانه‌ترین حالت ممکن

این مهم‌ترین تصمیم این کار بود. وسوسه‌ی PWA این است که «همه‌چیز را کش کن تا
آفلاین کار کند». **برای یک نرم‌افزار حسابداری این فاجعه است.**

نمایش موجودی کهنه از خطای شبکه *بسیار* خطرناک‌تر است: کاربر ممکن است کالایی را
بفروشد که ندارد، یا فاکتوری را ببیند که باطل شده.

پس:

```
هیچ پاسخ API                  → هرگز کش نمی‌شود
هیچ صفحه‌ی HTML                → هرگز کش نمی‌شود
دارایی نسخه‌دار (_next/static) → کش می‌شود (نامشان هش دارد، کهنگی ممکن نیست)
```

### اندازه‌گیری واقعی پس از بازدید چند صفحه و فراخوانی API

```
tarazoo-static-v1: ۳۳ مورد
    /offline.html
    /icons/icon-192.png
    /_next/static/css/...
    /_next/static/chunks/...

API کش‌شده:  هیچ ✅
HTML کش‌شده: هیچ ✅
```

درخواست به دامنه‌ی دیگر (Supabase) هم دست‌نخورده رد می‌شود، و فقط `GET` پردازش
می‌گردد — `POST` و `DELETE` هرگز از کش نمی‌آیند.

---

## 🔴 باگی که فقط با درخواست واقعی پیدا شد

پس از ساختن صفحه‌ی آفلاین:

```
GET /offline.html  →  307  →  /login
```

`matcher` در middleware فایل `sw.js` را مستثنا کرده بود ولی `offline.html` را
نه. یعنی سرویس‌ورکر موقع قطع شبکه صفحه‌ای را نشان می‌داد که **خودش نیازمند شبکه
و ورود بود** — دقیقاً در بدترین لحظه‌ی ممکن.

نه `tsc` و نه `next build` این را نمی‌گیرند. فقط یک `curl` واقعی.

پس از اصلاح: `200` و صفحه‌ی فارسی نمایش داده می‌شود. **در تست واقعی با قطع شبکه
تأیید شد.**

---

## دو مسیر نصب، هیچ دکمه‌ی توخالی

رویداد `beforeinstallprompt` استاندارد نیست و فقط در کروم/اج وجود دارد. سافاری
iOS **هیچ راه برنامه‌نویسی‌شده‌ای** برای نصب ندارد.

| پلتفرم | رفتار |
|---|---|
| کروم/اج | دکمه‌ی واقعی «نصب» |
| سافاری iOS | راهنمای گام‌به‌گام دستی |
| مرورگر داخل اینستاگرام | **هیچ‌چیز** — نصب ممکن نیست |
| از قبل نصب‌شده | **هیچ‌چیز** |

حالت سوم مهم است: دکمه‌ای که کلیک شود و کار نکند بدتر از نبودنش است — درسی که
از دکمه‌ی ورود صوتی روی iOS گرفتیم.

راهنمای iOS می‌گوید **«نوار پایین سافاری»** نه بالای صفحه. از iOS 15 نوار نشانی
پایین است؛ همان اشتباهی که در راهنمای میکروفون کردیم و کاربر دنبال دکمه‌ای در
بالای صفحه می‌گشت که آنجا نبود.

### آزمون واقعی با User-Agent آیفون

```
پیشنهاد نصب نشان داده شد        ✅
راهنما باز شد (Add to Home Screen) ✅
می‌گوید «نوار پایین»             ✅
پس از «بعداً» پنهان شد           ✅
در localStorage ثبت شد           ✅ (۳۰ روز دوباره نمی‌پرسد)
```

---

## اصلاح مانیفست

`purpose: "any maskable"` روی یک فایل — یک دام شناخته‌شده. آیکون maskable باید
حاشیه‌ی امن داشته باشد چون اندروید آن را در قالب دایره می‌برد. اعلام یک فایل
به‌عنوان هر دو یعنی یا لبه‌هایش بریده می‌شود یا حاشیه‌ی خالی بزرگ می‌ماند.

آیکون فعلی حاشیه‌ی امن ندارد، پس فقط `any` اعلام شد. **اعلام نکردن maskable
بی‌خطر است** (اندروید خودش پس‌زمینه می‌گذارد)؛ اعلام نادرستش آیکون را خراب
نشان می‌دهد.

به‌علاوه: میان‌برهای فشار طولانی روی آیکون (فروش جدید، کالاها، مشتریان) — و یک
تست بررسی می‌کند هر سه مسیر واقعاً وجود دارند.

---

## راستی‌آزمایی تست‌ها

**شش مورد عمداً برگردانده شد و تست شکست:**

| باگ برگردانده‌شده | تستی که گرفت |
|---|---|
| کش‌کردن پاسخ API | `هیچ پاسخ API کش نمی‌شود` |
| پیشنهاد نصب به اپِ نصب‌شده | `اپِ نصب‌شده دیگر پیشنهاد نصب نمی‌بیند` |
| برگرداندن `"any maskable"` | `🔴 any و maskable با هم اعلام نمی‌شوند` |
| پیشنهاد در مرورگر اینستاگرام | `🔴 مرورگر داخل اینستاگرام نصب پیشنهاد نمی‌دهد` |
| مقدار خراب localStorage | `🔴 باعث پنهان‌شدن دائمی نمی‌شود` |
| راهنمای «بالای صفحه» | `🔴 می‌گوید نوار پایین، نه بالا` |

یک تست هم فایل‌های آیکون را **واقعاً باز می‌کند** و ابعادشان را از هدر PNG
می‌خواند — اعلام آیکونی که فایلش نیست، نصب را خاموش می‌کند و خطایش فقط در
DevTools دیده می‌شود.

---

## وضعیت نهایی

```
tsc --noEmit   پاک
vitest         ۱۰۳۰ / ۱۰۳۰  (۳۹ فایل) — از ۹۹۲
next build     Compiled successfully
سرویس‌ورکر     active · scope: /
مانیفست        200 · standalone · ۳ آیکون · RTL
آفلاین         صفحه‌ی فارسی با قطع واقعی شبکه تأیید شد
کش             صفر API · صفر HTML · فقط دارایی نسخه‌دار
axe-core       صفر ایراد (داشبورد دسکتاپ/موبایل + صفحه‌ی آفلاین)
```

**داده‌ی شما لمس نشد** — نه مهاجرتی، نه نوشتنی در دیتابیس.

---

## صادقانه: چه چیزی نیست

- **این APK نیست.** یک PWA است: روی صفحه‌ی گوشی آیکون می‌گیرد و مثل برنامه باز
  می‌شود، ولی از **گوگل‌پلی نصب نمی‌شود**. برای APK واقعی باید با TWA بسته‌بندی
  شود که به کلید امضا و حساب توسعه‌دهنده‌ی گوگل (۲۵ دلار) نیاز دارد — و
  عملاً به دامنه‌ی اختصاصی هم وابسته است.
- **کار آفلاین نمی‌کند.** عمداً. صفحه‌ی آفلاین فقط پیام می‌دهد؛ هیچ داده‌ای
  آفلاین نشان داده نمی‌شود چون در حسابداری، عدد کهنه از خطا بدتر است.
- **اعلان (Push Notification) ندارد.**
- **آیکون maskable ندارد** — روی بعضی گوشی‌های اندروید داخل دایره‌ی سفید
  می‌نشیند به‌جای اینکه تمام‌صفحه باشد. برای رفعش به یک نسخه‌ی آیکون با
  حاشیه‌ی امن نیاز است.

## باقی‌مانده

⬜ APK با TWA (وابسته به دامنه و حساب گوگل‌پلی) · ⬜ نمودارهای جذاب‌تر
⬜ داشبورد بر اساس بیشترین استفاده · ⬜ ویرایش قلم‌به‌قلم فاکتور توسط پشتیبانی
⬜ پرچم قابلیت · ⬜ ورود فاکتورها و تراکنش‌های گذشته از اکسل
