# گزارش پنل سوپرادمین — `/admin/organizations`

تاریخ: ۱۱ مرداد ۱۴۰۵ · روش: تست واقعی روی بیلد پروداکشن با داده‌ی واقعی
(Playwright + axe-core + کوئری مستقیم روی دیتابیس زنده). حدس و «بیلد پاس شد»
مبنا قرار نگرفته است.

---

## خلاصه‌ی یک‌خطی

یادداشت «Super-admin panel: not started» در گزارش‌های قبلی **غلط بود**.
صفحه، API، RPCها، RLS و نما همگی از قبل پیاده‌سازی شده و کار می‌کردند.
مشکل واقعی «دسترسی‌پذیری» بود، نه «پیاده‌سازی»: هیچ لینکی به صفحه وجود
نداشت و راه امنی برای افزودن سوپرادمین جدید مستند نشده بود.

---

## ۱) چه چیزهایی از قبل کار می‌کردند (تأییدشده با تست)

| بخش | وضعیت | شاهد |
|---|---|---|
| فهرست سازمان‌ها | ✅ کار می‌کند | `GET /api/admin/organizations` → ۲۰۰، سه سازمان واقعی |
| نمایش وضعیت تأیید | ✅ | نشان‌های «در انتظار تأیید / تأیید شده / رد شده / معلق» |
| فیلتر وضعیت | ✅ | همه=۴، در انتظار=۲، تأیید شده=۲ (با یک سازمان تستی) |
| تأیید سازمان | ✅ | `POST` → ۲۰۰، توست موفق، نشان به «تأیید شده» تغییر کرد |
| رد سازمان | ✅ | `POST` → ۲۰۰، `rejection_note` در دیتابیس ثبت شد |
| دیالوگ تأیید | ✅ | قبل از هر دو عمل باز می‌شود |
| به‌روزرسانی خودکار فهرست | ✅ | `invalidateQueries` → `GET` مجدد بعد از هر عمل |
| ردپای ممیزی | ✅ | `approved_by` و `approved_at` درست پر می‌شوند |
| شمارنده‌ی اعضا/فاکتور/پلن | ✅ | «مزون پوشاک»: ۳ عضو، ۲۰ فاکتور، پلن پایه |
| ایمیل مالک | ✅ | از `auth.users` خوانده می‌شود (در نما نیست) |
| نمای موبایل و تبلت | ✅ | کارت‌های `lg:hidden`، بدون سرریز افقی در ۳۹۰px |
| گارد API | ✅ | کاربر غیرادمین → ۴۰۳ |
| گارد دیتابیس | ✅ | RPCها مستقل از API هم `is_platform_admin` را چک می‌کنند |
| RLS | ✅ | با کلید anon هم `v_admin_organizations` و هم `platform_admins` آرایه‌ی خالی می‌دهند |

**نتیجه:** هیچ بخشی از منطق تأیید/رد ناقص نبود.

---

## ۲) دو مشکلی که گزارش کردید — هر دو تأیید شد

### مشکل ۱: صفحه از UI قابل کشف نبود
تأیید شد. اسکن DOM: ۸۰ لینک در سایدبار، **هیچ‌کدام** `/admin`.

**رفع:** بخش «مدیریت پلتفرم» در انتهای سایدبار، مشروط به `is_platform_admin`.

نکته‌ی طراحی: آیتم عمداً **بیرون از `NAV`** تعریف شد. `NAV` با
`permissionForHref` فیلتر می‌شود و آن تابع برای مسیر ناشناخته `null`
برمی‌گرداند؛ `can(null)` هم `true` است. یعنی اگر `/admin` داخل `NAV`
می‌رفت، **به همه‌ی کاربران** نشان داده می‌شد. ضمناً `BottomNav` روی
`NAV` حلقه می‌زند و آیتم در نوار پایین موبایل هم ظاهر می‌شد.

### مشکل ۲: فقط ایمیل ثابت در 0021 سیدشده بود
تأیید شد — و مشخص شد **حساب شما همان حساب سیدشده است**:

```
platform_admins → b9254367-… → bahram@hesabyar.app
```

پس شما از قبل دسترسی داشتید و با لینک مستقیم هم Forbidden نمی‌گرفتید.
ولی نکته‌ی شما درست است: هیچ راه مستند و امنی برای افزودن ادمین **بعدی**
وجود نداشت. migration 0025 دقیقاً همین را حل می‌کند.

---

## ۳) چیزهایی که اضافه شد

### الف) گارد سمت سرور — یک شکاف که خودتان نگفته بودید

قبلاً **هر کاربر واردشده‌ای** می‌توانست `/admin/organizations` را باز کند.
صفحه با تمام هدر، فیلتر و ساختار رندر می‌شد و فقط بعد از پاسخ ۴۰۳ یک
کلمه‌ی «Forbidden» می‌دید. داده‌ای لو نمی‌رفت، اما وجود پنل و شکل آن
برای همه آشکار بود.

حالا `app/(app)/admin/layout.tsx` قبل از رندر `is_platform_admin` را
می‌پرسد و در غیر این صورت `notFound()` می‌دهد — نه `redirect`، تا به
کاربر غیرمجاز نگوییم «اینجا چیزی هست که اجازه‌اش را نداری».

خطای RPC هم مثل «مجاز نیست» رفتار می‌کند (fail closed).

### ب) migration 0025 — طبق خواسته‌ی شما، بدون seed خودکار

سه چیز اضافه می‌کند:

- `grant_platform_admin(uuid, role, note)`
- `revoke_platform_admin(uuid)`
- `v_platform_admins` — فهرست ادمین‌ها همراه ایمیل

و یک راهنمای کپی‌کردنی در انتهای فایل (داخل بلوک کامنت، اجرا نمی‌شود).

چرا تابع و نه `INSERT` خام:

| خطر | رفتار تابع |
|---|---|
| uuid اشتباه | پیام روشن فارسی، نه خطای foreign key |
| نقش نامعتبر | رد می‌شود؛ فقط `admin` یا `support` |
| اجرای دوباره | بی‌خطر است (`on conflict do update`) |
| غلط تایپی در ایمیل | صفر سطر برمی‌گرداند، هیچ کاربری ادمین نمی‌شود |
| حذف آخرین ادمین | مسدود — پلتفرم بدون مدیر نمی‌ماند |

امنیت: توابع **`security definer` نیستند** و `EXECUTE` از `anon` و
`authenticated` گرفته شده. تست واقعی با توکن کاربر `test`:

```
POST /rest/v1/rpc/grant_platform_admin → 403 permission denied
```

و جدول `platform_admins` بعد از تلاش، همچنان فقط یک سطر داشت.

### ج) رفع کنتراست — باگ سراسری

axe-core روی این صفحه: دکمه‌ی توپر «رد کردن» کنتراست **۳.۶۲** داشت
(نیاز WCAG AA: ۴.۵). این یک باگ توکن سراسری بود و **همه‌ی دکمه‌های
`variant="danger"` در کل اپ** را درگیر می‌کرد.

`--destructive` از `0 84% 60%` به `0 84% 48%` رفت → کنتراست **۴.۶۶**.
مقدار محاسبه‌شده است، نه حدسی: کم‌ترین تیرگی‌ای که از آستانه عبور
می‌کند تا رنگ برند کمترین تغییر را ببیند. حالت تاریک از قبل ۱۰.۰۲ بود
و دست نخورد.

---

## ۴) باگی که فقط با تست واقعی پیدا شد

نسخه‌ی اول `grant_platform_admin` ستون خروجی‌ای به نام `user_id` داشت.
PL/pgSQL آن را متغیر محلی می‌شناسد و عبارت `on conflict (user_id)` با
این خطا شکست می‌خورد:

```
ERROR: column reference "user_id" is ambiguous
```

تست‌های اولیه‌ی من فقط مسیرهای خطا را زده بودند (uuid نامعتبر، نقش
نامعتبر) که هرگز به `INSERT` نمی‌رسند — همه سبز بودند. باگ فقط وقتی
ظاهر شد که یک **درج موفق واقعی** انجام شد.

رفع: ستون‌های خروجی پیشوند `admin_` گرفتند. یک تست رگرسیون هم اضافه شد.

---

## ۵) کاری که باید خودتان انجام دهید

**هیچ.** حساب `bahram@hesabyar.app` از قبل سوپرادمین است. کافی است
وارد شوید — «مدیریت کسب‌وکارها» انتهای سایدبار هست.

الگوی SQL برای وقتی که خواستید ادمین **جدیدی** اضافه کنید، در انتهای
`supabase/migrations/0025_platform_admin_grant_helper.sql` آماده است:

```sql
-- ۱) ببینید چه کسی الان ادمین است
select * from public.v_platform_admins;

-- ۲) شناسه‌ی کاربر را پیدا کنید
select id, email from auth.users where email = 'NAME@hesabyar.app';

-- ۳) دسترسی بدهید (با ایمیل، بدون کپی دستی uuid)
select g.*
from auth.users u
cross join lateral public.grant_platform_admin(u.id, 'admin', 'توضیح') g
where u.email = 'NAME@hesabyar.app';

-- ۴) تأیید
select * from public.v_platform_admins;
```

سپس آن کاربر یک‌بار صفحه را رفرش کند.

> ⚠️ migration 0025 را من روی دیتابیس زنده **اجرا کرده‌ام** (فقط ساخت
> تابع و نما — هیچ سطر داده‌ای تغییر نکرد). لازم نیست دوباره اجرایش
> کنید؛ اجرای دوباره هم بی‌خطر است.

---

## ۶) خلاصه‌ی تست‌ها

```
bahram (ادمین)      لینک دیده می‌شود · ۳ سازمان · ریل ۶۸px · موبایل ۳۹۰px بدون سرریز
test (غیرادمین)     لینک نیست · مسیر مستقیم → ۴۰۴ · بدون نشت «Forbidden»
اعطای دسترسی        test از ۴۰۴ → دسترسی کامل، فقط با اجرای SQL راهنما
سلب دسترسی          test برگشت به ۴۰۴
حذف آخرین ادمین     مسدود شد (خطای عمدی)
ارتقای سطح دسترسی   با توکن authenticated → ۴۰۳، جدول بدون تغییر
چرخه‌ی تأیید/رد      روی سازمان یک‌بارمصرف · POST ۲۰۰ · توست · نشان · refetch
نوار پایین موبایل    ۰ لینک ادمین (نباید داشته باشد)
axe-core            ۰ ایراد در ۷ صفحه
vitest              ۵۰/۵۰ (۷ تست جدید)
tsc --noEmit        تمیز
next build          موفق
```

**داده‌ی واقعی دست‌نخورده:**
```
contacts 543 · products 382 · variants 385 · sales 20 · sale_items 25
transactions 18 · stock_movements 730 · orgs 3 · platform_admins 1
```
سازمان واقعیِ در انتظار («Vv») همچنان `pending` است — عمداً روی آن
عملیاتی انجام نشد. تست تأیید/رد روی یک سازمان ساختگی انجام و بلافاصله
حذف شد.

---

## ۷) نکته‌های باز

- نقش `support` در جدول تعریف شده اما `is_platform_admin()` بین آن و
  `admin` تفاوتی نمی‌گذارد — هر دو دسترسی کامل دارند. اگر دسترسی
  فقط-خواندنی می‌خواهید، باید در همان تابع تفکیک شود.
- `GET /api/admin/organizations` برای هر مالک یکتا یک
  `getUserById` جدا می‌زند (N+1). با ۳ سازمان بی‌اهمیت است؛ در چند صد
  سازمان باید یک‌جا خوانده شود.
- تغییر `--destructive` سراسری است. ظاهر همه‌ی دکمه‌های قرمز کمی
  تیره‌تر شده — این عمدی و لازمِ رعایت WCAG بود.
- **کلید `service_role` لو رفته همچنان زنده است** (آخرین بررسی: HTTP
  ۲۰۰). این مستقل از این کار است و طبق گفته‌ی خودتان در دست شماست.
