# گزارش — یکسان‌سازی پنجره‌ی خرید با فروش

نسخه `26.0803` · کامیت `cad17d3` · ۱۲ مرداد ۱۴۰۵

---

## اول بررسی کردم، بعد ساختم

درخواست شما: «پنجره خرید و مشتری جدید هم در همه جا مثل پنجره فروش جدید باشد».

بررسی مسیرهای واقعی:

| مسیر | وضعیت قبل |
|---|---|
| «مشتری جدید» از داشبورد | ✅ پنل مشترک `ContactPanel` |
| «مشتری جدید» از صفحه‌ی اشخاص | ✅ همان پنل |
| `/contacts/new-customer` | ✅ همان پنل |
| **«خرید جدید» از `/purchases`** | ❌ Modal محلی ۲۰۰ خطی |
| **`openDocument("purchase")`** | ❌ «ایجاد فاکتور خرید از این مسیر ممکن نیست» |
| **«خرید کالا» در داشبورد** | ❌ فقط لینک به `/purchases` |
| **دکمه‌ی شناور موبایل** | ❌ لینک — و اصلاً رندر نمی‌شد |

**مشتری جدید از قبل یکسان بود.** مشکل فقط خرید بود.

---

## چه چیزی کم داشت

مودال قدیمی خرید این‌ها را نداشت:

- بارکدخوان با دوربین
- ورود صوتی
- انتخاب تاریخ سند
- تخفیف
- تفکیک پرداخت نقدی/کارتی
- چیدمان موبایلش هم با فروش فرق داشت

نکته‌ی جالب: تابع `create_purchase` در دیتابیس **از قبل** `p_date`، `p_discount_type`
و `p_discount_value` را می‌پذیرفت. مودال فقط هیچ‌وقت پاسشان نمی‌داد — یعنی
تاریخ هر خرید همیشه «همین الان» ثبت می‌شد، حتی اگر فاکتور مال هفته‌ی پیش بود.

---

## چه ساختم

`PurchaseCreateForm` قرینه‌ی `InvoiceCreateForm`، با **همان اجزای بصری** —
نه یک کپی دوم. اجزای POS حالا یک prop به نام `variant` می‌گیرند.

> چرا کپی نکردم: ریشه‌ی شکایت شما دقیقاً وجود دو پیاده‌سازی موازی بود که
> جدا از هم تغییر می‌کردند. کپی سوم همان مشکل را بازتولید می‌کرد.

### چیزی که فقط خرید دارد

هر ردیف کالا سه فیلد دارد: **قیمت خرید، قیمت فروش، سود٪** — همان‌طور که خواستید.

محاسبه دوطرفه است و هر دو جهت با کالای واقعی تست شد:

```
قیمت خرید ۱۰۰٬۰۰۰ + سود ۵۰٪  →  قیمت فروش ۱۵۰٬۰۰۰  ✅
قیمت فروش ۲۰۰٬۰۰۰ دستی        →  سود ۱۰۰٪            ✅
```

تایپ درصد، قیمت فروش را می‌سازد — چون فروشنده می‌گوید «۴۰ درصد روش بکش»،
نه اینکه عدد نهایی را از قبل بداند.

### چیزی که عمداً حذف شد

- **لیست قیمت**: مفهومی سمت فروش است و روی قیمت خریدِ تأمین‌کننده اثری ندارد.
- **هشدار «موجودی کافی نیست»**: در خرید موجودی *زیاد* می‌شود؛ آن پیام فقط
  کاربر را می‌ترساند.

---

## 🔴 مهاجرت ۰۰۳۰ نزدیک بود ثبت خرید را کاملاً از کار بیندازد

برای تفکیک نقدی/کارتی، تابع `create_purchase` را با دو پارامتر جدید نوشتم.

`create or replace` روی امضای **متفاوت** جایگزین نمی‌کند — یک overload دوم
می‌سازد. هر دو نسخه کنار هم نشستند و PostgREST نتوانست انتخاب کند:

```
PGRST203 — Could not choose the best candidate function
```

**یعنی اگر دیپلوی می‌کردم، ثبت خرید در کل برنامه از کار می‌افتاد** — هم مسیر
جدید و هم صفحه‌ی `/purchases/[id]` که هنوز امضای قدیمی را صدا می‌زند.

این را با صدا زدن endpoint واقعی پیدا کردم، نه با خواندن SQL. مهاجرت حالا اول
نسخه‌ی قبلی را `drop` می‌کند. بعدش هر دو شکل فراخوانی دوباره تست شدند.

### یک باگ مالی هم درست شد

تابع قبلی `method` تراکنش را همیشه `'cash'` می‌گذاشت. یعنی اگر کالا را با کارت
می‌خریدید، در گزارش مالی «نقدی» ثبت می‌شد و مانده‌ی صندوق غلط درمی‌آمد.
حالا برای هر روش پرداخت یک تراکنش جدا با `method` درست ساخته می‌شود.

فراخوانی‌های قدیمی که فقط `p_paid` می‌فرستند، رفتار قبلی‌شان حفظ شده.

### یک کار تکراری هم حذف شد

نسخه‌ی قبلی بعد از RPC یک `update` جداگانه روی `product_variants` می‌زد. هم
تکراری بود (خودِ RPC این کار را می‌کند) و هم **بیرون از تراکنش**: اگر شکست
می‌خورد، خرید ثبت شده بود ولی قیمت فروش به‌روز نشده.

---

## 🔴 دو باگ چیدمانی که با اندازه‌گیری پیدا شدند

هر دو **از قبل در فروش هم بودند** و کسی اندازه نگرفته بود.

**۱. گرید فرم ستون صریح نداشت.** اندازه‌ی پیش‌فرض ستون `auto` است که کفش
`min-content` می‌شود، پس محتوای عریض خودِ گرید را از پنل بیرون می‌برد:

```
داخل پنل ۵۶۰px (فضای مفید ۵۱۹px):
  فروش : ۷۱۴px → ۲۷ عنصر بیرون از پنل
  خرید : ۸۵۴px → ۳۳ عنصر
```

**۲. ردیف‌های جدول با `lg:grid` کنترل می‌شدند** که به عرض **پنجره** نگاه
می‌کند. روی دسکتاپ ۱۴۴۰px آن شرط فعال بود و جدول چندستونی داخل پنل ۵۶۰px
رندر می‌شد.

هر دو با container query حل شدند — همان راه‌حلی که قبلاً برای پوسته‌ی فرم
به کار رفته بود. حالا **صفر عنصر بیرون‌زده** در هر سه اندازه، و فروش هم سود برد.

---

## دکمه‌ی شناور موبایل — کد مرده بود

`MobileFab` در پروژه نوشته شده بود ولی **هیچ‌جا رندر نمی‌شد**. `BottomNav` فقط
ناوبری است، پس در موبایل هیچ راه یک‌کلیکی برای «ساختن» چیزی وجود نداشت.

با اجازه‌ی شما فعالش کردم و هر چهار میان‌بر (فروش، خرید، مشتری، کالا) حالا
مستقیم پنل باز می‌کنند، بدون تغییر صفحه.

سر راه یک باگ لایه‌بندی داشت: پرده‌ی بستن روی خود دکمه‌ها می‌افتاد و کلیک را
می‌بلعید. دو تلاش اول جواب نداد (`z-index` صریح، بعد `z-0`/`z-10`) چون ظرف
والد یک stacking context می‌سازد. راه‌حل نهایی: پرده با portal مستقیم به
`body` می‌رود — همان الگوی `PortalMenu` در همین پروژه.

---

## تست واقعی

```
npx tsc --noEmit  → تمیز
npx vitest run    → ۲۸۴/۲۸۴ (۱۹ فایل)
npm run build     → Compiled successfully
```

**یک خرید واقعی ثبت شد و در دیتابیس بازرسی شد** (روی سایت زنده، بعد از دیپلوی):

| بررسی | نتیجه |
|---|---|
| ۱ فاکتور + ۱ قلم ساخته شد | ✅ |
| **۲ تراکنش**: نقدی ۶۰٬۰۰۰ + کارتی ۴۰٬۰۰۰ | ✅ |
| گردش انبار `+۳` از نوع `in` | ✅ |
| مبلغ کل ۵۰٬۰۰۰×۳ = ۱۵۰٬۰۰۰ | ✅ |
| قیمت خرید کالا ۵۰٬۰۰۰ شد | ✅ |
| قیمت فروش کالا ۷۰٬۰۰۰ شد (سود ۴۰٪) | ✅ |
| **بعد از حذف، شمارش‌ها به خط پایه برگشتند** | ✅ |

بعدش **یک فروش واقعی هم ثبت شد** تا مطمئن شوم تغییر اجزای مشترک، فروش را
نشکسته — پاس شد و آن هم پاک شد.

سایر تست‌ها: Playwright روی ۳۹۰/۸۳۴/۱۴۴۰ · axe صفر تخلف · بدون اسکرول افقی ·
بدون خطای کنسول · انتخابگر کالا پنل را نمی‌بندد.

---

## داده‌های شما

```
products 382 · variants 385 · contacts 547 · sales 20 · sale_items 25
transactions 18 · stock_movements 730 · purchases 0
```

همه به خط پایه برگشتند. کالای «شومیز کتیبه» که در تست استفاده شد، دقیقاً به
قیمت ۹۷۰٬۰۰۰ و موجودی ۲۰ بازگردانده شد.

### یک خبر خوب

حین تست دیدم `users` از ۴ به ۵ و `orgs` از ۲ به ۳ رفته. بررسی کردم — **کار من
نبود**: یک کاربر واقعی امروز ساعت ۰۷:۴۶ ثبت‌نام کرده:

```
navidyazdani1987@gmail.com  ·  «یزدانی»
سازمان «حسابداری بازرگانی»  ·  تریال تا ۱۷ مرداد
ورود مجدد: ۰۸:۱۹
```

یعنی ثبت‌نام آنی و تریال ۱۴ روزه در عمل کار کرد و کاربر بعد از ثبت‌نام
**برگشته** — همان چیزی که نگرانش بودید. به حسابش دست نزدم.

---

## باقی‌مانده

- نوار آب‌وهوا و قیمت دلار — هنوز منتظر تصمیم شما درباره‌ی منبع API هستم
- رابط تیکت پشتیبانی (جدول‌ها و RLS هست، UI نیست)
- چیدمان داشبورد بر اساس پرکاربردترین موارد
- PWA در برابر اپ اندروید بومی
