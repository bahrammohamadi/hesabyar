# بررسی فهرست درخواست‌ها — وضعیت واقعی

**تاریخ:** ۱۴ مرداد ۱۴۰۵ · بررسی‌شده با خواندن کد و تست روی سرور واقعی، نه از روی حافظه.

---

## ✅ از قبل انجام شده (۱۶ مورد)

| درخواست | وضعیت واقعی |
|---|---|
| بارکدخوان / اسکن با دوربین | `components/shared/barcode-scanner.tsx` — در فرم فروش و خرید فعال است |
| فاکتور با صدا | `components/shared/voice-order.tsx` — در هر دو فرم |
| نوار آب‌وهوا و قیمت دلار | `market-ticker.tsx` — ۷۵۶ نماد از tgju + ۳۶ شهر |
| شمارنده‌ی گرافیکی دوره‌ی تست | `trial-countdown.tsx` — حلقه‌ی SVG رنگی در هدر |
| تغییر قیمت و تعداد در سبد فروش | در `InvoiceCreateForm` هست |
| پنجره‌ی خرید مثل فروش | `PurchaseCreateForm.tsx` — بارکد، صدا، تاریخ، تخفیف |
| فیلتر بازه‌ی تاریخ | `DateRangeFilter` در ۶ صفحه: فروش، خرید، مرجوعی‌ها، چک‌ها، فعالیت |
| منوی سه‌نقطه که از کادر بیرون می‌زد | `portal-menu.tsx` — جای‌گذاری هوشمند بالا/پایین + اسکرول داخلی |
| گارد ایمیل فیک | **۸۲۰۴ دامنه‌ی یکبارمصرف** در دیتابیس + الگوی تشخیص |
| جستجوی کاربر در پنل ادمین | `/api/admin/users/search` با ایمیل، نام، تلفن، نام کسب‌وکار |
| نسخه‌ی خودکار نرم‌افزار | `scripts/gen-version.mjs` — الان `1.247` با هش کامیت |
| تقویم فارسی | همه‌جا `toJalali`؛ سه صفحه‌ی گزارش هنوز ورودی میلادی دارند (زیر ⬜) |
| راهنمای تکراری کنار فیلدها | حل شده: موبایل فقط دکمه‌ی «؟»، دسکتاپ فقط متن |
| PWA / پایه‌ی اپ اندروید | `manifest.webmanifest` موجود |
| **کلید F2** | **کار می‌کند** — تست شد روی `/dashboard`، `/sales`، `/products` هر سه پنل را باز کرد |
| دو دکمه‌ی «فروش جدید» با ظاهر متفاوت | هر دو یک پنل باز می‌کنند؛ URL و ساختار یکسان است |

### درباره‌ی F2
با Playwright روی هر سه صفحه تست شد و هر بار پنل فروش باز شد
(`?panels=invoice:sale:create:new`). اگر برای شما کار نمی‌کند، احتمالاً
مرورگر یا افزونه‌ای کلید F2 را می‌قاپد. روی فایرفاکس F2 «caret browsing»
است. می‌توانم میانبر دوم (مثلاً `Ctrl+Shift+S`) اضافه کنم.

---

## 🔧 انجام‌شده در همین نوبت

### فعال‌سازی فوری حساب — مهم‌ترین مورد فهرست شما

شما گفتید: «وقتی می‌ره تا من تأیید کنم دیگه شاید برنگرده».

**روی سرور واقعی بازتولید شد و ریشه‌اش از حدس شما هم بدتر بود:**

```
ثبت‌نام واقعی →  email_confirmed_at = null
                organizations      = صفر ردیف
```

کاربر پیام «لینک تأیید به ایمیلت ارسال شد» می‌گرفت و همان‌جا رها می‌شد —
نه نشستی، نه سازمانی. و بدتر:

```
rate_limit_email_sent = 2     ← دو ایمیل در ساعت، برای کل پروژه
```

یعنی **سومین نفری که در یک ساعت ثبت‌نام می‌کرد اصلاً ایمیلی نمی‌گرفت** و
برای همیشه پشت در می‌ماند. این فقط ریزش کاربر نبود؛ یک نقص کارکردی بود.

**حالا:** ثبت‌نام → ورود فوری → فرم کوتاه (نوع کسب‌وکار، نام، موبایل) →
پنل فعال با ۱۴ روز اعتبار. تأیید ایمیل *بعداً* با یک نوار بالای صفحه.
همان الگویی که Notion و Slack و Linear استفاده می‌کنند.

**امنیت جایگزین** (چون تأیید پیش از ورود برداشته شد):
- کد ۶ رقمی با `randomInt` رمزنگاشتی، نه `Math.random`
- فقط چکیده‌ی SHA-256 با نمکِ شناسه‌ی کاربر ذخیره می‌شود — کد خام هرگز
- مقایسه‌ی زمان‌ثابت، اعتبار ۱۵ دقیقه، سقف ۵ تلاش
- سقف نرخ روی ارسال و بررسی، سقف ساخت حساب برای هر IP
- گارد ۸۲۰۴ دامنه‌ی موقت سر جایش

**دو باگ حین کار پیدا شد:**

🔴 `bootstrap_org` **دو نسخه** در دیتابیس داشت — یکی بازمانده‌ی نسخه‌ی
اولیه که هیچ مهاجرتی پاکش نکرده بود. چون همه‌ی پارامترها پیش‌فرض دارند،
فراخوانی با یک آرگومان (همان چیزی که PostgREST می‌فرستد) مبهم می‌شد:
`ERROR 42725: function bootstrap_org(unknown) is not unique`.

🔴 فایل `inventory/out/page.tsx` **از پروژه حذف شده بود** و باگ ۴۰۴ که
قبلاً رفع شده بود برگشته بود. تستِ نگهبانی که همان موقع نوشتم گرفتش.

---

## ⬜ باقی‌مانده (به ترتیب اولویت)

### ۱. پنل سوپرادمین کامل‌تر
الان: کسب‌وکارها، کاربران، اعلان‌ها، تیکت، آمار مصرف، نقش‌ها، ممیزی، ورود داده.
**نیاز:** دسترسی به فاکتورهای هر کسب‌وکار، تأیید/حذف/اصلاح فاکتور،
وضعیت فنی سرویس، خطاهای زنده، پرچم قابلیت، و **تغییر نام و نام خانوادگی
توسط کاربر و سوپرادمین** (درخواست صریح شما).

### ۲. مرتب‌سازی تنظیمات و کاربران
`settings/page.tsx` الان ۶۴۳ خط و چند بخش در یک صفحه است.

### ۳. تقویم فارسی در سه صفحه‌ی گزارش
`reports/sellers`، `reports/profitability`، `reports/customer-profitability`
هنوز `<input type="date">` میلادی دارند.

### ۴. نمودارهای جذاب‌تر

### ۵. داشبورد بر اساس بیشترین استفاده
نیاز به شمارش استفاده‌ی هر بخش و مرتب‌سازی پویا.

### ۶. خروجی / پشتیبان کامل
مکمل «ورود اطلاعات»؛ در راهنما به کاربر گفته‌ام پشتیبان بگیرد ولی دکمه‌اش نیست.

### ۷. دو مورد از کامیکس (تأییدشده توسط شما)
- **صفحه‌ی عمومی فروشگاه** — آدرس، تلفن، ساعت کاری، چند کالا؛ لینکش را
  در اینستاگرام می‌گذارند
- **تحلیل صنفی در سایت معرفی** — کاربر صنفش را انتخاب می‌کند و می‌بیند
  برنامه چه مشکلی از او حل می‌کند

### ۸. اپ اندروید
پیشنهاد: اول PWA (مانیفست آماده است)، بعد APK واقعی با TWA.

### ۹. SMTP اختصاصی
وابسته به خرید دامنه. تا آن زمان ایمیل فارسی و سقف ۲ در ساعت پابرجاست.

---

## وضعیت فنی

```
tsc --noEmit  → پاک
vitest run    → ۶۷۱/۶۷۱ (۲۹ فایل)
next build    → موفق
axe           → صفر ایراد در صفحات آزموده‌شده
```
