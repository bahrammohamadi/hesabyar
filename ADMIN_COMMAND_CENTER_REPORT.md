# گزارش: پنل فرماندهی سوپرادمین

**تاریخ:** ۱۸ مرداد ۱۴۰۵ · **مهاجرت:** `0039_admin_command_center.sql` · **نسخه:** ۲۶.۰۸۰۹

---

## درخواست کاربر

> «مثل تصویر پنل سوپر ادمین نوع دسترسی ها ش متفاوت باشه متناسب با نوع کاربری
> من فقط نمیخوام ببینم کی عضو شده میخوام به خیلی چیزای اینچنینی **تایید حذف
> فاکتور اصلاح** خلاصه همه چیز بعلاوه یسری امکاناتی که **سرور ها یا ادمین ها**
> دارن داشته باشم»

سه شکاف از این درخواست باقی مانده بود که این کار می‌بندد:

| خواسته | وضعیت پیش از این کار |
|---|---|
| دسترسی به فاکتورهای هر کسب‌وکار | ❌ هیچ صفحه‌ای نبود |
| تأیید / حذف / اصلاح فاکتور | ❌ سوپرادمین حتی از نظر فنی نمی‌توانست |
| وضعیت فنی سرویس | ❌ نبود |
| خطاهای زنده | ❌ فقط `console.error` روی Vercel |

---

## ۱) فاکتورهای همه‌ی کسب‌وکارها — `/admin/invoices`

فهرست فاکتورهای فروش همه‌ی کسب‌وکارها با:

- **جستجو** روی شماره فاکتور، نام کسب‌وکار، نام و تلفن مشتری (ستون یکپارچه‌ی `search_blob`)
- **فیلتر بازه‌ی تاریخ شمسی** با `DateRangeFilter` موجود
- **فیلتر وضعیت** (۷ وضعیت، هم‌راستا با قید دیتابیس)
- **خلاصه**: تعداد، جمع مبلغ بدون باطل‌شده‌ها، تعداد باطل‌شده

### 🔴 چرا «ابطال» و نه «حذف»؟

کاربر «حذف فاکتور» خواسته بود. **حذف واقعی سطر انجام نمی‌شود و نباید بشود.**

فاکتور به سه چیز گره خورده: موجودی انبار، مانده‌ی حساب مشتری، گردش صندوق.
حذف سطر یعنی موجودی انبار برای همیشه غلط می‌ماند و شماره‌ی فاکتور در دنباله
سوراخ می‌شود — دقیقاً همان الگویی که حسابرس به آن مشکوک می‌شود. هیچ نرم‌افزار
حسابداری جدی‌ای این کار را نمی‌کند.

**ابطال (storno)** اثر مالی و انباری را کاملاً خنثی می‌کند، سند را با برچسب
«باطل‌شده» نگه می‌دارد و ردّ ممیزی می‌گذارد. نتیجه‌ی عملی برای کاربر یکی است،
بدون خرابی داده. این در خود رابط هم به کاربر گفته می‌شود، نه فقط در کد.

### اندازه‌گیری واقعی روی سازمان یکبارمصرف

```
موجودی اولیه:                    ۱۰ عدد
پس از فروش ۳ عدد:                 ۷ عدد
پس از admin_cancel_sale:         ۱۰ عدد  ✅ برگشت کامل
فراخوانی دوم (تکرار):            ۱۰ عدد  ✅ بی‌اثر، نه دو بار برگشت
تراکنش خنثی‌ساز:                    ۱ ردیف ✅ نه دو تا
ردیف ممیزی:                        ۲ ردیف ✅ هر دو تلاش ثبت شد
```

سازمان تست بلافاصله پاک شد و **همه‌ی شمارش‌ها با خط پایه مو‌به‌مو یکی است**
(orgs=4، sales=26، items=33، sm=752، tx=24، products=383، variants=386،
contacts=549).

### 🔴 منطق مشترک، نه کپی

`cancel_sale` موجود دو گارد داشت که برای ادمین پلتفرم غلط بودند:
`has_permission('sales.create')` و `org_id in user_org_ids()`. سوپرادمین عضو
سازمان مشتری نیست، پس **اصلاً نمی‌توانست فاکتور را باطل کند.**

به‌جای کپی‌کردن منطق، مکانیک ابطال در `apply_sale_cancellation` استخراج شد و
هر دو مسیر آن را صدا می‌زنند. دو نسخه یعنی روزی یکی اصلاح می‌شود و دیگری نه، و
آن‌وقت موجودی انبار بسته به اینکه *چه کسی* فاکتور را باطل کرده فرق می‌کند.

تابع مکانیک **هیچ بررسی دسترسی ندارد**، پس صریحاً از همه گرفته شده است:
```
has_function_privilege('authenticated', 'apply_sale_cancellation(...)')  →  false  ✅
has_function_privilege('authenticated', 'admin_cancel_sale(...)')        →  false  ✅
```

### گاردها — همه با فراخوانی واقعی HTTP آزموده شدند

| سناریو | نتیجه |
|---|---|
| کاربر غیرادمین | `دسترسی اصلاح فاکتور وجود ندارد` |
| `p_actor` تهی | `دسترسی اصلاح فاکتور وجود ندارد` |
| دلیل کوتاه‌تر از ۵ نویسه | ۴۰۰ — `ثبت دلیل الزامی است` |
| عملیات نامعتبر (`delete`) | ۴۰۰ — `عملیات نامعتبر` |
| فاکتور ناموجود | ۴۰۴ |
| شناسه‌ی بدشکل | ۴۰۰ |
| ابطال معتبر | ۲۰۰ — `فاکتور باطل شد…` |
| ابطال دوباره | ۲۰۰ — `از قبل باطل شده بود؛ تغییری اعمال نشد` |

آخری مهم است: پیام **صادق** است و وانمود نمی‌کند کاری انجام شده.

---

## ۲) وضعیت فنی سرویس — `/admin/system`

قاعده‌ی حاکم: **هر عددی که نشان داده می‌شود یک اندازه‌گیری واقعی است.**
صفحه‌ی وضعیتی که همیشه سبز است، بدتر از نداشتنش است — چون موقع خرابی هم به آن
نگاه می‌کنیم و خیالمان راحت می‌شود.

خروجی واقعی از دیتابیس زنده:

```
دیتابیس   Postgres ۱۷.۶ · ۳۵ از ۵۰۰ مگابایت (۷٪) · ۹ اتصال · طولانی‌ترین کوئری ۰ ثانیه
حجم داده  ۲۶ فاکتور · ۶ کاربر · ۵۴۹ شخص · ۳۸۳ کالا · ۷۵۲ حرکت انبار · ۲۴ تراکنش
احراز     ۰ تأییدنشده · ۰ قفل‌شده · ۰ هرگز وارد نشده
فعالیت    آخرین فروش ۱۴۰۵/۰۵/۱۵ · ۰ تیکت باز
```

نوار مصرف فضا از **۷۵٪** قرمز می‌شود نه ۹۰٪: در پلن رایگان وقتی به سقف بخوریم
نوشتن متوقف می‌شود و آن لحظه دیگر برای واکنش دیر است.

---

## ۳) 🔴 خطاهای زنده — شکافی که کسی ندیده بود

`safeError()` تا امروز فقط `console.error` می‌زد. روی Vercel آن لاگ در پلن فعلی
چند ساعت بیشتر نمی‌ماند و **از داخل خود محصول اصلاً دیده نمی‌شد**.

یعنی وقتی کاربر می‌گفت «خطا داد و یک کد بهم داد»، آن کد ۸ رقمی **هیچ‌جا قابل
جستجو نبود** — یک وعده‌ی توخالی به کاربر.

### حلقه‌ی کامل، اثبات‌شده انتها به انتها

با یک روت آزمایشی موقت که عمداً خطای دیتابیس می‌سازد:

```
۱. کاربر می‌گیرد:  {"error":"خطای داخلی سرور…","ref":"7170af3a"}
۲. ادمین جستجو می‌کند با همان 7170af3a  →  پیدا می‌شود:
     context: admin/errprobe
     message: column v_admin_invoices.column_that_does_not_exist does not exist
     code:    42703
     path:    /api/admin/errprobe   status: 500
```

روت آزمایشی بلافاصله حذف و جدول خطاها پاک شد.

### تصمیم‌های طراحی

- **`safeError` حالا `async` است.** هر ۴۸ فراخوان به شکل `return safeError(...)`
  داخل تابع `async` بودند، پس هیچ‌کدام نشکست. یک تست خودکار تضمین می‌کند اگر
  روزی کسی آن را بدون `return` صدا بزند، بیلد بشکند.
- **شکست ثبت خطا، پاسخ کاربر را نمی‌شکند.** محتمل‌ترین دلیلِ خطا این است که
  دیتابیس در دسترس نیست — یعنی دقیقاً همان لحظه‌ای که ثبت هم شکست می‌خورد.
- **هیچ policy نوشتنی وجود ندارد.** با `insert` باز، هر کاربر واردشده‌ای
  می‌توانست جدول را با ردیف جعلی پر کند و خطاهای واقعی را زیر نویز پنهان کند.
- **پاک‌سازی دستی است، نه خودکار.** `pg_cron` روی پلن رایگان نداریم. تظاهر به
  پاک‌سازی خودکار بدتر از نبودنش بود.

---

## دسترسی‌ها

دو مجوز تازه، هر کدام با دلیل:

| مجوز | نقش‌ها | چرا جدا؟ |
|---|---|---|
| `system.health` | مدیر ارشد، پشتیبانی، مالی | عدد است و بی‌خطر؛ مالی هم برای سنجش ظرفیت لازمش دارد |
| `errors.view` | مدیر ارشد، پشتیبانی | متن خام خطا می‌تواند نام جدول و داده‌ی مشتری داشته باشد |

`invoice.view` و `invoice.modify` از مهاجرت ۰۰۲۸ در ماتریس بودند ولی **هیچ روتی
از آن‌ها استفاده نمی‌کرد — مجوزهای مرده.** حالا معنا پیدا کردند.

اندازه‌گیری زنده پس از مهاجرت:
```
system.health(مدیر ارشد)  → true      errors.view(کاربر عادی) → false  ✅
errors.view(مدیر ارشد)    → true      tickets.reply           → true   ✅ (رگرسیون نشده)
```

صفحه‌ی وضعیت با نقشی که فقط `system.health` دارد **۴۰۳ نمی‌گیرد** — بخش سلامت
را می‌بیند و بخش خطاها پیام توضیحی نشان می‌دهد.

---

## راستی‌آزمایی تست‌ها

نوشتن تست سبز کافی نیست. **هر پنج باگ عمداً برگردانده شد و تست شکست:**

| باگ برگردانده‌شده | تستی که گرفت |
|---|---|
| حذف `tickets.reply` از ماتریس (تکرار باگ ۰۰۳۳) | `🔴 هیچ سطری از ماتریس قبلی جا نیفتاده` |
| `grant` دادن `apply_sale_cancellation` به `authenticated` | `🔴 تابع مکانیک به هیچ نقش عمومی grant نشده` |
| `security_invoker = true` روی نمای ادمین (تکرار ۰۰۲۸/۰۰۳۶) | `نمای v_admin_invoices با security_invoker ساخته نشده` |
| برگرداندن `safeError` به `sync` | `async است` |
| `lte("date")` به‌جای `applyRange` | `🔴 فیلتر بازه از helper مشترک استفاده می‌کند` |

سپس همه بازگردانده شدند و `git diff` تأیید کرد فایل‌ها دست‌نخورده‌اند.

---

## باگی که فقط در تست واقعی پیدا شد

`next build` و `tsc` هر دو تمیز بودند، ولی پاسخ HTTP واقعی نشان داد:

> ثبت دلیل الزامی است (حداقل **5** نویسه)

رقم لاتین در یک رابط تماماً فارسی. با `toFaDigits` اصلاح شد و تست گرفتش.

---

## وضعیت نهایی

```
tsc --noEmit          پاک
vitest                ۸۰۱ / ۸۰۱  (۳۳ فایل) — از ۷۴۴ به ۸۰۱
next build            Compiled successfully
axe-core              صفر ایراد در دسکتاپ، تبلت و موبایل (هر دو صفحه)
سرریز افقی            هیچ‌کدام در ۱۴۴۰ / ۸۲۰ / ۳۹۰ پیکسل
خطای کنسول            صفر
داده‌ی زنده            دست‌نخورده — همه‌ی شمارش‌ها با خط پایه یکی
```

---

## آنچه انجام *نشد* (صادقانه)

- **«تأیید فاکتور»** — کاربر گفته بود «تایید حذف فاکتور اصلاح». «حذف» به ابطال
  تبدیل شد و «اصلاح» فعلاً فقط ابطال است. **ویرایش قلم‌به‌قلم فاکتور از سمت
  پشتیبانی پیاده نشد**؛ تابع `update_sale_invoice` مثل `cancel_sale` به
  `memberships` گره خورده و بازکردنش برای ادمین کار جداگانه‌ای است. ابطال +
  صدور مجدد توسط خود کاربر، مسیر امن‌تری است.
- **فاکتور خرید** — جدول `purchases` روی داده‌ی زنده خالی است، پس نمای ادمین
  فعلاً فقط فروش را پوشش می‌دهد.
- **پرچم قابلیت (feature flag)** — از فهرست این مرحله بیرون ماند.

## باقی‌مانده‌ی کلی

⬜ مرتب‌سازی تنظیمات و کاربران (`settings/page.tsx` هنوز ۶۴۳ خط)
⬜ خروجی/پشتیبان کامل · ⬜ نمودارهای جذاب‌تر · ⬜ داشبورد بر اساس بیشترین استفاده
⬜ صفحه‌ی عمومی فروشگاه · ⬜ تحلیل صنفی · ⬜ اپ اندروید (PWA اول)
