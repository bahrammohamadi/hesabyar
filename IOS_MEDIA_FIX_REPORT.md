# رفع مشکل میکروفون و دوربین در آیفون

**تاریخ:** ۱۴ مرداد ۱۴۰۵ · **کامیت:** `0634e18`

---

## مشکلی که گزارش کردید

در آیفون پیام زیر می‌آمد و هیچ کاری از دستتان برنمی‌آمد:

> **دسترسی به میکروفون مسدود است**
> ۱. روی آیکون قفل 🔒 کنار نشانی سایت بزنید…

**سه علت جداگانه داشت و هر سه بازتولید شد.**

---

## 🔴 علت ۱: `getUserMedia` دو بار پشت سر هم صدا زده می‌شد

تابع `runStart()` یک بار در ابتدا مجوز می‌خواست و **چند خط بعد دوباره**.
سافاری موبایل درخواست دوم را «خارج از ژست کاربر» می‌بیند و بی‌درنگ
`NotAllowedError` می‌دهد — **بدون اینکه هرگز پنجره‌ای به شما نشان دهد**.

نتیجه: پیام «دسترسی مسدود است» می‌دیدید و هر کاری در تنظیمات می‌کردید فرقی
نمی‌کرد، چون **مجوز اصلاً مسئله نبود**.

## 🔴 علت ۲: زنجیره‌ی «ژست کاربر» شکسته بود

سافاری آیفون مجوز رسانه را فقط در پاسخ *مستقیم* به لمس می‌دهد. جریان قبلی:

```
لمس دکمه → setState → رندر → useEffect → await permissions.query → getUserMedia
```

هر `await` میان راه، زنجیره را قطع می‌کند. وقتی نوبت به `getUserMedia`
می‌رسید، دیگر ژستی در کار نبود.

**حالا:** در آیفون پنجره باز می‌شود و منتظر می‌ماند تا خودتان دکمه‌ی
میکروفون را لمس کنید؛ آن لمس مستقیماً مجوز را می‌گیرد. روی دسکتاپ و
اندروید رفتار خودکار قبلی حفظ شد — آنجا محدودیتی نیست و یک لمس اضافه فقط
مزاحمت است.

## 🔴 علت ۳: راهنما در آیفون **غلط** بود

پیام می‌گفت «روی آیکون قفل کنار نشانی سایت بزنید».

**در سافاری آیفون چنین آیکونی وجود ندارد.** شما دنبال چیزی می‌گشتید که
نبود. مسیر واقعی آیفون از دکمه‌ی **«aA»** در نوار نشانی می‌گذرد:

| دستگاه | راهنمای جدید |
|---|---|
| آیفون / آیپد | دکمه‌ی «aA» ← Website Settings ← Microphone ← Allow |
| دسکتاپ | آیکون قفل 🔒 ← میکروفون ← اجازه دادن |
| داخل اینستاگرام/تلگرام | «…» ← Open in Safari |

---

## ➕ موردی که خودتان نگفتید ولی مهم بود

**مرورگر داخل اینستاگرام و تلگرام.**

بیشتر کاربران ایرانی لینک را از اینستاگرام باز می‌کنند. آنجا
`webkitSpeechRecognition` **وجود دارد** — پس هر بررسی فنی می‌گوید
«پشتیبانی می‌شود» — ولی در عمل بی‌صدا شکست می‌خورد.

بدترین حالت ممکن: دکمه هست، کاربر می‌زند، **هیچ اتفاقی نمی‌افتد و هیچ
خطایی هم داده نمی‌شود**.

حالا *پیش از* هر تلاش هشدار داده می‌شود که صفحه را در مرورگر اصلی باز کند.

**آیپد** هم اضافه شد: از iPadOS 13 خودش را «Macintosh» معرفی می‌کند، پس
تنها نشانه‌ی باقی‌مانده وجود صفحه‌ی لمسی است.

---

## 🔴 باگی که خودم ساختم و گرفتمش

سه `useMemo` را اشتباهاً **بعد از** `if (!open || !mounted) return null`
گذاشتم. وقتی پنجره بسته بود اجرا نمی‌شدند و به‌محض باز شدن اجرا می‌شدند —
یعنی تعداد هوک‌ها بین دو رندر فرق می‌کرد:

```
Minified React error #310
```

کل صفحه با «خطای برنامه» می‌ترکید. **نه `tsc` گرفت، نه `next build`، نه
هیچ تست واحدی** — فقط رندر واقعی با شناسه‌ی آیفون. تستی اضافه شد که مطمئن
شود هیچ هوکی بعد از `return` زودهنگام نیاید.

---

## تست واقعی روی production build

| سناریو | نتیجه |
|---|---|
| **آیفون** — مجوز خودکار درخواست نمی‌شود | ✅ ۰ بار |
| **آیفون** — پس از لمس، دقیقاً یک بار | ✅ ۱ بار |
| **آیفون** — راهنمای «aA» نمایش داده می‌شود | ✅ |
| **آیفون** — متن گمراه‌کننده‌ی «قفل» حذف شده | ✅ |
| **اینستاگرام** — هشدار پیش از تلاش | ✅ |
| **دسکتاپ** — شروع خودکار حفظ شده (رگرسیون) | ✅ ۱ بار |

تست‌ها با برگرداندن عمدی هر سه باگ راستی‌آزمایی شدند و هر بار شکستند.

---

## دوربین (بارکدخوان) هم اصلاح شد

همان پیام غلط «قفل» را داشت. حالا از همان منبع مشترک
(`lib/utils/platform.ts`) راهنما می‌گیرد تا دو جا از هم جدا نیفتند.

---

## وضعیت

```
tsc --noEmit  → پاک
vitest run    → ۶۹۳/۶۹۳ (۳۰ فایل، ۲۲ تست جدید)
next build    → موفق
```

## نکته‌ی باقی‌مانده

تشخیص گفتار در سافاری روی سرورهای اپل پردازش می‌شود و برای فارسی دقتش
کمتر از کروم اندروید است. این محدودیت خود اپل است و از سمت ما قابل تغییر
نیست. اگر دقت برایتان مهم است، روی اندروید نتیجه‌ی بهتری می‌گیرید.
