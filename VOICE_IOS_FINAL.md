# ورود صوتی در آیفون — راه‌حل قطعی

**کامیت:** `1e81e00` · روی سایت زنده تأیید شد

---

## چرا دو بار قبل حل نشد

هر دو بار یک علت واقعی پیدا و رفع شد (دو بار `getUserMedia`، زنجیره‌ی
ژست، راهنمای غلط) — ولی **هیچ‌کدام مانع اصلی نبودند**.

### مانع اصلی

سافاری تشخیص گفتار را پشت یک کلید آزمایشی پنهان کرده که **پیش‌فرض
خاموش** است:

```
Settings → Safari → Advanced → Experimental Features → Speech Recognition API
```

وقتی خاموش است:
- `webkitSpeechRecognition` اصلاً وجود ندارد
- هیچ کدی نمی‌تواند روشنش کند
- **هیچ API‌ای وضعیتش را گزارش نمی‌دهد** — از داخل کد قابل تشخیص نیست

### و بدتر

تابع `isVoiceSupported` فقط همان API را می‌سنجید. روی آیفون شما دکمه‌ی
**«افزودن با صدا» اصلاً نمایش داده نمی‌شد** — پس حتی راه‌های نجاتی که در
دو نوبت قبل اضافه کرده بودم، هرگز در دسترس شما نبودند.

با شبیه‌سازی همان حالت بازتولید شد: دکمه در DOM نبود و تست با timeout شکست.

---

## راه‌حل: میکروفون کیبورد

به‌جای جنگیدن با API مرورگر، از **دیکته‌ی خودِ آیفون** استفاده می‌کنیم:

1. روی کادر می‌زنید → کیبورد باز می‌شود
2. دکمه‌ی میکروفون 🎙️ کنار دکمه‌ی فاصله را لمس می‌کنید
3. می‌گویید: «سه عدد شومیز آبی»
4. متن در کادر می‌نشیند و «جستجوی کالا» را می‌زنید

**هیچ مجوز وبی نمی‌خواهد. هیچ تنظیمی لازم نیست. همیشه کار می‌کند.**

متن از همان `parseUtterance` قبلی می‌گذرد، پس نتیجه دقیقاً یکی است.

---

## اگر می‌خواهید مستقیم کار کند

آن کلید سافاری را روشن کنید و سافاری را ببندید و باز کنید:

```
تنظیمات ← Safari ← Advanced ← Experimental Features ← Speech Recognition API
```

آن‌وقت میکروفون درون‌برنامه‌ای هم کار می‌کند و لازم نیست از کیبورد
استفاده کنید. دکمه‌ی «یا میکروفون مرورگر را امتحان کنید» همان‌جاست.

---

## صفحه‌ی تشخیص

**تنظیمات ← بررسی میکروفون و دوربین**

اگر باز هم مشکلی بود، این صفحه وضعیت دقیق دستگاه شما را نشان می‌دهد:
اتصال امن، وجود API، وضعیت مجوز، و یک **آزمون واقعی میکروفون**. با یک
دکمه کپی می‌شود تا در تیکت پشتیبانی بفرستید.

دو بار از راه دور حدس زدم؛ دفعه‌ی بعد با داده تصمیم می‌گیریم.

---

## یک باگ که TypeScript گرفت

هنگام نوشتن صفحه‌ی تشخیص، `navigator.mediaDevices?.getUserMedia` را
به‌عنوان بررسی وجود نوشتم. TypeScript درست هشدار داد: آن متد در تعریف
نوع همیشه موجود است. چیزی که واقعاً ممکن است نباشد خودِ `mediaDevices`
است — مرورگر در اتصال غیرامن حذفش می‌کند.

---

## وضعیت

```
tsc --noEmit  → پاک
vitest run    → ۷۱۰/۷۱۰
next build    → موفق
تست زنده      → صفر ایراد در بدترین حالت شبیه‌سازی‌شده
```
